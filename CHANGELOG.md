# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Changed
- Setup scripts (`scripts/setup.ps1`, `scripts/setup.sh`) now install **every**
  optional feature by default, best-effort per group — a dep that can't build on
  the machine is reported and skipped instead of aborting the whole install.
  The GPU `[train]` stack is installed only when a CUDA GPU is detected (force
  with `-Train` / `--train`); `-Minimal` / `--minimal` does a base-only install;
  `-Extras` / `--extras` still picks a specific subset. The scripts also run the
  Playwright Chromium download and flag a missing `ffmpeg` for voice.
- `[gateway]` extra no longer forces `matrix-nio[e2e]` (→ `python-olm`, a C
  extension with no Windows wheel). Base `matrix-nio` is used; the Matrix adapter
  already degrades gracefully without libolm. This lets `[gateway]` install
  cleanly on Windows.

### Added
- `[matrix-e2e]` optional extra for Matrix end-to-end encryption
  (`matrix-nio[e2e]`), split out of `[gateway]` for platforms where libolm is
  buildable.

## [0.3.0] - 2026-06-05

### Fixed
- TUI: background curator / review forks no longer leak their tool calls into the foreground transcript (#31).
- TUI: the `<think>` reasoning trace no longer leaks a literal `_(thought collapsed)_` marker into the transcript (#32).
- TUI: parallel same-tool calls get a unique per-call id, so the activity lane no longer mass-evicts pending calls or mis-attributes args under concurrent dispatch (#33).
- Launch: an unreachable `OLLAMA_HOST` can no longer hang startup for the full request timeout — the Ollama client bounds connect short (read / write stay generous for long generations) and the reachability probe uses a short per-request timeout (#35).
- Launch: a hung / unresponsive MCP server can no longer brick startup — each connect is bounded by a configurable `startup_timeout` (default 10s) and skipped with a warning if it doesn't respond (#36).

### Added
- TUI: `Ctrl+O` toggles inline model reasoning (a forward-looking verbose mode); the reasoning trace rides along on `StreamEndEvent.thinking` (#32).
- TUI: user-invoked sub-agent (Agent tool) tool calls now render nested + dimmed under a new `subagent` write origin, instead of being silently suppressed (#37).
- Config: `mcp_startup_timeout` (default 10s) plus a per-server `startup_timeout` override; `connect_timeout` on the Ollama provider (#35, #36).
- README restructured into a scannable hero → highlights → quickstart with collapsed long-form depth; repo About description + topics refreshed (#38, #41).

### Added (continued)
- `athena eval <cases.jsonl>`: composes T7-02 batch with a scoring pass over each per-run envelope; per-case scorer overrides; optional baseline regression diff. Closes the trio T7-01 / T7-02 / T7-03 — headless primitive → batch runner → eval battery. The closed training loop is finally testable head-to-head: score model X on a labeled case set, then score model Y, diff to see whether training helped (T7-03). `athena/eval/scorers.py` defines the Scorer Protocol — `__call__(actual, expected, *, context) -> Score` — and a module-level registry (same pattern as athena's provider registry). Four built-in scorers registered at import: `exact` (string equality after strip; strict, case-sensitive; pinned to quote BOTH sides in fail details), `contains` (expected substring present in actual; empty expected explicitly fails to prevent accidental pass-everything), `regex` (`re.search` with inline flag support like `(?i)hello`; invalid pattern fails cleanly without raising; empty pattern fails), `json_path` (actual parsed as JSON; expected = `{"path": "data.users[0].id", "value": <expected>}`; dotted paths with `[N]` indices via `_resolve_path`; failure modes — invalid JSON / missing path / value mismatch — each fail with a specific details string identifying what went wrong). `register_scorer` + `get_scorer` + `list_scorers` are the public surface; `get_scorer("nope")` raises `KeyError` with `"available: <names>"` so typos fail fast not silently no-op. Custom scorers register at import time in operator-supplied modules. `athena/eval/summary.py` defines the data shapes: `EvalCase(task, expected, case_id?, scorer?, cwd?, timeout_s?, model?, extras)` — `.from_dict` tolerates unknown keys preserving them in `extras` so custom scorers can read them off context; required `task` + `expected`; empty/missing raise `ValueError`. `EvalScore(case_id, run_id, task_excerpt, actual_excerpt, passed, score, scorer, details, run_status, envelope_path)` is one scored result with the underlying RunResult.status preserved (so failed batch runs are counted separately from failed-scorer cases). `EvalSummary(eval_id, batch_id, started/finished/duration, output_dir, total/passed/failed/errored, pass_rate, avg_score, by_scorer, baseline_id?, regressions?, improvements?, cases[])` aggregated outcome; `baseline_id` + `regressions` + `improvements` ONLY appear in `to_dict` when set (clean summary when no baseline). `mint_case_id()` → `e-<uuid12>`, `mint_eval_id()` → `v-<uuid12>` (same family as `r-<uuid12>` run IDs / `b-<uuid12>` batch IDs / `t-<uuid12>` task IDs). `athena/eval/runner.py:run_eval(cases, *, cfg, workspace_default, output_dir, default_scorer, eval_id, baseline_dir, force, batch_progress, score_progress, _run_fn) -> EvalSummary` — two-phase: (1) batch_run via T7-02's engine — **the case's case_id becomes the batch's run_id so eval ↔ batch ↔ envelope all key off the SAME identifier** (joins across baselines work); (2) score each per-run envelope: load assistant_text → call the case's scorer (or default) → EvalScore. **THREE distinct outcomes per case**: `passed` = scorer returned True; `failed` = run completed (status=ok) but scorer returned False; `errored` = run didn't complete (status=error/timeout/interrupted/invalid) — the scorer wasn't invoked at all, details says so explicitly, and the scorer name is still recorded so `by_scorer` accounting stays consistent. The distinction matters for diagnosis — "the model gave a wrong answer" is upstream of "the model didn't respond." Scorer exceptions caught → `Score(passed=False, score=0.0, details="scorer raised <Type>: <msg>")` so a buggy custom scorer doesn't crash the eval. Resume-safety inherits from the batch — pre-existing per-run envelopes are SKIPPED by the batch (assistant_text re-scored cheaply); scoring ALWAYS re-runs so iterating on a scorer doesn't require re-running the model. `force=True` overrides. `_load_baseline(baseline_dir)` reads `<baseline>/eval-summary.json` defensively (missing/unreadable/malformed → None + warning + skip diff, no error). `_diff_against_baseline(current, baseline)` joins by `case_id`: regressions = passed-in-baseline + failed-now; improvements = failed-in-baseline + passes-now. Cases without stable case_ids can't be matched (silently excluded); operators pin case_ids in their JSONL for stable diffs. Persists `<output_dir>/eval-summary.json` + `<output_dir>/scores.jsonl` + the underlying batch's manifest + per-run envelopes. `athena/cli/eval.py` is the CLI subcommand (`athena eval` in `_SUBCOMMANDS`): flags `-o`/`--output-dir` (default `<profile_dir>/eval/<eval_id>/`), `--eval-id`, `--scorer NAME` (default `exact`), `--baseline DIR`, `--force`, `--json`, `-q`/`--quiet`, `--profile`, `-C`/`--cwd`. Default scorer validated up front (typo'd `--scorer NAME` → exit 2 with "available scorers: ..." list — fail fast before any work fires). Cases file parse errors carry line numbers. **Exit codes**: 0 = every case passed (or skipped resume); 1 = any failed or errored; 2 = validation. So `athena eval cases.jsonl && deploy.sh` is a reliable CI gate. Progress: default emits `[  N/M] PASS|FAIL|ERR  case_id  scorer=NAME  task-excerpt` per case on stderr; `--quiet` suppresses; `--json` puts the final summary on stdout (single-line, parseable); stdout stays empty in non-JSON mode. Final summary (non-JSON) shows pass-rate + counts + (if baseline set) regression/improvement counts with case_ids enumerated. 74 tests across `tests/eval/`: 42 scorers in `test_scorers.py` (registry: list includes built-ins, get unknown raises with helpful message, register+get round-trip, empty name rejected; ~24 per-scorer: exact strips whitespace + case-sensitive + fail-details-quote-both-sides; contains pass/fail/empty-expected-explicit-fail; regex match/no-match/invalid-pattern-clean-fail/empty-pattern/inline-flags; json_path simple/nested/value-mismatch-with-both-values-in-details/invalid-JSON/missing-path/dict-expected-required/top-level-array/complex-expected-value; Score to_dict + 4-decimal rounding; EvalCase from_dict required + all-fields + extras-preserved + empty-task-rejected + missing-expected-rejected + complex-types; mint_case_id + mint_eval_id format + uniqueness; EvalScore + EvalSummary to_dict shapes; baseline fields ONLY when baseline_id set; JSON round-trip; excerpt strips-newlines + truncates-with-… + handles-None); 18 runner in `test_runner.py` (parse_cases_file basics + blanks/# / line-numbered JSON errors + missing-expected line-numbered + missing-file FileNotFoundError; happy paths all-pass with exact / mixed pass+fail; per-case scorer override beats default with `contains` recorded in scores.jsonl; errored runs counted separately in `errored` not `failed` with scorer NOT invoked + details says so; json_path scorer end-to-end through the real eval flow; by_scorer histogram for mixed exact + contains; case_id minting when absent + envelope filename derives; **baseline regression detection** for passed→failed flip + improvement detection for failed→passed flip; missing baseline summary file SKIPS diff with no error and baseline_id=None; unmatched case_ids in either direction generate NO phantom regressions/improvements; score_progress callback fires per case with (passed, done, total) in order; empty cases → empty summary with files still written; unknown default_scorer rejected before any batch work fires); 14 CLI in `test_cli.py` (validation: missing file → 2, unknown --scorer → 2 with "available scorers" list, bad JSON → 2 with line number, empty file → 0 with empty summary; happy paths: all-pass → exit 0 with "N/M passed", any-failure → exit 1, error counted separately + "N errored" in summary stderr line; --json single-line on stdout; default output-dir under `<profile>/eval/<eval_id>/`; **baseline diff via CLI surfaces regressions + improvements on stderr** with case_ids enumerated; missing baseline summary handled cleanly; progress lines emitted by default with [N/M] + PASS/FAIL/ERR + case_id + scorer + task excerpt; --quiet suppresses progress but keeps the summary line; per-case scorer override flows through CLI with by_scorer counts correct). Reference: `docs/reference/eval.md` covers the input JSONL shape (with worked examples for each scorer including json_path + regex with inline flags + a per-case scorer override + a longer-running case with cwd/timeout/model overrides), built-in scorers table, full output structure (eval-summary as the CI artifact + scores.jsonl + manifest + per-run envelopes), exit-code table, **the three failure categories** (passed / failed-scorer / errored-run) and why the distinction matters for diagnosis, baseline regression diff with a "gate model upgrades" recipe, four composition recipes (CI gate via GitHub Actions YAML; sweep across models with the same cases; **training-loop integration as the closed-loop gate** that promotes a trained tag only if regressions == 0 AND pass_rate ≥ 0.90; iterate on a scorer cheaply by reusing per-run envelopes), how to write a custom scorer with full example, test coverage map. T7-03 ships the closed training-loop gate; the trio is complete.
- Codex CLI delegate wire-up: `athena delegate setup-codex` detects the codex binary + (with confirmation) appends the canonical config snippet to `~/.athena/config.toml` — one-command first-time setup of `delegate_to_cli` against OpenAI's Codex CLI. `athena delegate verify` sanity-checks the current `cli_delegate_command` regardless of vendor (codex / aider / claude-code / etc.). The generic `delegate_to_cli` already handled codex via the `cli_delegate_command = "codex exec --quiet {task}"` template (T6-03); this adds the friction-free setup path + an inspection tool. `athena/delegate/codex.py`: `RECOMMENDED_COMMAND = "codex exec --quiet {task}"` (the canonical non-interactive form); `detect_codex(*, binary_name="codex") -> CodexDetection` returns a NamedTuple (found / path / version / error) — never raises so callers branch on absence cleanly; `recommended_config_snippet(*, sandbox=True)` returns the canonical config block; `write_config_snippet(*, config_path, sandbox, overwrite)` appends to the config (creates file + parent dir if missing); **REFUSES to write when `cli_delegate_command` is already configured unless `overwrite=True`** (the operator's existing config shouldn't be silently clobbered). Vendor-isolated to this one file — a future `aider.py` / `cursor_cli.py` sibling follows the same shape. Error message on missing binary names BOTH the npm install path and the brew install path. `athena/cli/delegate.py`: new `athena delegate` subcommand with two verbs. `athena delegate verify [--json]` shlex-splits the `cli_delegate_command` template, checks the binary via `shutil.which`, best-effort captures `<binary> --version` with 10s timeout; emits human-readable text on stdout by default or a structured `{ok, enabled, sandbox, binary, location, version, template, reason?}` JSON when `--json`; **exit 0 when ok AND enabled, exit 1 otherwise** — so `athena delegate verify && delegate_task` works in scripts. `athena delegate setup-codex` with `--config-path`, `--no-sandbox`, `--detect-only`, `--dry-run`, `--overwrite`, `--yes`, `--json`; default flow detects → shows snippet → prompts → writes; `--yes` skips prompt (CI mode); `--dry-run` shows what would be written without writing. Registered in `_SUBCOMMANDS` as `"delegate"`. 20 tests in `tests/delegate/test_codex.py`: detection (not found → CodexDetection with install hints; found + version captured; found but `--version` times out → found=True with version=None; custom binary_name passed through); snippet shape (all required fields; sandbox off when requested; RECOMMENDED_COMMAND value pinned); write_config_snippet (creates new file; appends to existing while preserving prior keys; REFUSES when cli_delegate_command already set without overwrite; overwrite appends anyway leaving TOML last-value-wins behavior; creates parent dirs); CLI plumbing (verify when not configured → exit 1; verify with missing binary → FAIL + exit 1; verify happy path → OK + exit 0 with binary+location+version; verify --json emits structured payload; setup-codex when not installed → exit 1 with install hints; setup-codex --dry-run shows snippet without writing; setup-codex --yes writes config without prompting; setup-codex --detect-only reports without writing). Reference: `docs/reference/cli-delegation.md` adds a "Wiring up Codex (one-command setup)" section covering install steps (npm / brew / Rust), the setup flags, the canonical config snippet, and the verify command with JSON mode. Generic delegate_to_cli unchanged — Codex is the first vendor to get a paved on-ramp.
- `athena batch <tasks.jsonl>`: iterates the T7-01 headless primitive over a JSONL of tasks; per-run envelopes land on disk; a batch manifest aggregates the run. The engine under cron jobs, the Phase 7 training-loop trajectory generator, and the future T7-03 eval-battery (T7-02). Input: one JSON object per line with `task` (required) + optional `run_id` / `cwd` / `timeout_s` / `model` overrides; blank lines and `#` comments ignored. `athena/batch/manifest.py` defines `BatchEntry` (input row; tolerates extra keys), `ManifestEntry` (per-run summary with task+error excerpts capped at 240/200 chars), `BatchManifest` (aggregated outcome with batch_id + timings + total/completed/skipped + by_status histogram + per-run entries list). `mint_batch_id()` yields `b-<uuid12>` — same family as `r-<uuid12>` run IDs and `t-<uuid12>` task IDs. `athena/batch/runner.py:batch_run(...)` is the serial engine. **Resume-safety by default**: entries whose envelope file already exists at `<output_dir>/<run_id>.json` are SKIPPED (existing envelope read back into the manifest, counted in `skipped`); `force=True` overrides. So re-running after a Ctrl+C / crash / partial completion picks up only un-finished entries. **Manifest ALWAYS written in the finally block** so even a `KeyboardInterrupt` mid-batch leaves a parseable manifest with `status=interrupted` for un-started entries. `parse_tasks_file()` validates up front (per-line `ValueError` with line numbers — fail fast before any work fires). `_safe_filename()` strips non-`[A-Za-z0-9._-]` chars from operator-supplied run_ids so they can't escape `output_dir` or break paths on Windows. `athena/cli/batch.py` is the CLI subcommand (`athena batch` in `_SUBCOMMANDS`): flags `-o`/`--output-dir`, `--batch-id`, `-j`/`--parallel N`, `--force`, `--json`, `-q`/`--quiet`, `--profile`, `-C`/`--cwd`. **Parallel mode**: ThreadPoolExecutor with workers each constructing their own Agent inside run_headless — no shared state (distinct session_id, browser context, approval ContextVar); workers share credential pool + cross-session cache + audit logs (thread-safe at the athena level). Workers may finish in any order; **the manifest is re-sorted to input JSONL line order before write** (pinned by `test_parallel_runs_complete_in_input_order` over 10 entries × `--parallel 4`). Stable batch output makes diffing two runs of the same input set useful. Cancellation safety in the parallel path too — Ctrl+C synthesizes interrupted placeholders + writes partial manifest. **Exit codes**: 0 = every entry ended ok (skipped counts as ok — resume scenarios); 1 = any entry ended error / timeout / interrupted / invalid; 2 = validation failed BEFORE any work (missing file / bad JSON / missing task field). So `athena batch ... && deploy.sh` works reliably in CI. Progress: default emits `[  N/M] STATUS r-id  Ds  task-excerpt` per entry on stderr; `--quiet` suppresses; `--json` puts final manifest on stdout (single-line, parseable) — stdout stays empty in non-JSON mode so `2>/dev/null` strips noise cleanly. 37 tests across `tests/batch/`: 23 engine in `test_runner.py` (parse_tasks_file basics + comment/blank handling + line-numbered errors + missing-file FileNotFoundError; _safe_filename pass-through + replacement + never-empty; mint_batch_id format; happy paths write envelopes + manifest; per-entry cwd/model/timeout overrides propagate; mixed-status by_status histogram + insertion order + error_excerpt; resume-safety skips existing + force=True overrides + run_fn NOT called on skipped; progress fires per entry including for skipped; minted/passed-through batch_id; empty batch writes empty manifest; long task/error excerpted); 14 CLI in `test_cli.py` (validation: missing file → 2, bad JSON → 2 with line, empty file → 0 with manifest; serial happy path writes envelopes + manifest; default output-dir under `<profile>/batch/<batch_id>/`; --json single-line manifest on stdout parseable; resume-safety preserves existing + --force overwrites; **exit 1 on any failure**, exit 0 on all-skipped; **parallel run completes with manifest entries in INPUT ORDER** over 10 entries × --parallel 4; stderr progress emitted by default with [N/M] + status + run_id + duration + task excerpt, --quiet suppresses; stdout stays empty without --json). Stub `run_headless` via monkeypatch — both the runner module AND the package-level re-export (since batch_run imports from the package). Reference: `docs/reference/batch.md` covers input JSONL shape, output structure (per-run envelopes + manifest in input order), exit-code table, **four composition recipes** (training-trajectory generation overnight; cron-driven nightly run; eval battery as proto-T7-03; resume-after-interrupt by re-pointing at the same output dir), concurrency model (per-worker Agent + shared athena-level pools), explicit non-goals (no per-entry dependencies, no streaming, no cost gating, no distributed batch), and the test coverage map. T7-02 ships the engine; T7-03 eval-battery will wrap it as a labeled-set + scorer pipeline.
- Headless run primitive: `athena -p "<task>" --json` emits a structured `RunResult` envelope on stdout while routing TTY chatter to stderr, so batch drivers / cron jobs / future eval harnesses can consume athena programmatically. The existing one-shot path (`agent.run_turn(...)` always returns 0) was useful at the terminal but unusable as the unit a batch runner iterates over; T7-01 turns it into a batch-friendly engine without changing the agent loop (T7-01). `athena/headless/result.py:RunResult` is the envelope shape: `{run_id, status, exit_code, started_at, finished_at, duration_s, task, workspace, model, profile, session_id, tool_calls, tokens, cost_est, assistant_text, error}` with `Status = Literal["ok", "error", "timeout", "interrupted", "invalid"]`. `.exit_code()` maps to **stable POSIX exit codes**: 0=ok, 1=error, 2=invalid, 124=timeout (matches `timeout(1)`), 130=interrupted (matches `128 + SIGINT(2)`) — shell scripts branch reliably across modes. `.to_dict()` is JSON-safe + includes the computed `exit_code` field so a parser doesn't have to re-derive from status + caps `assistant_text` at 8000 chars with a truncation marker pointing at the `session_id` (full text lives in the session JSONL on disk). `.to_json(indent=None)` produces a single-line envelope by default — what `--json` mode emits + what batch parsers expect. `mint_run_id()` yields `r-<uuid12>` (same shape family as the task store's `t-<uuid12>` so an operator can tell them apart at a glance). `athena/headless/runner.py:run_headless(task, *, cfg, workspace, model, run_id, timeout_s, on_info, agent, _agent_factory)` is the wrapper around the existing `agent.run_turn(...)` call site — every error mode produces a RunResult, NEVER a Python exception (the CLI dispatcher needs a guaranteed exit code). Validates up front (no Agent construction): empty / whitespace-only task → status=invalid; workspace doesn't exist or isn't a directory → status=invalid. Timeout (when `timeout_s > 0`) via a `threading.Timer` that fires `_thread.interrupt_main()` on expiry — the agent's existing turn-cancel paths catch the `KeyboardInterrupt` at the same call sites the interactive UI uses. The runner distinguishes timed-out interrupts from user interrupts via a `timed_out` sentinel and maps them to `status=timeout` vs `status=interrupted` accordingly. `agent.close()` called in a `finally` so a crashed run still releases the session store + the browser context + the cross-session cache entry cleanly. Stats from `agent.stats` populate the envelope: `tokens.{prompt, completion, cache_read, cache_creation}` (matches OpenAI naming in the envelope even though athena's internal field is `eval_tokens`); `tool_calls` as `[{name, count}]` in descending-count order; `cost_est` via the existing `athena.ui.estimated_cost_usd` pricing table (graceful 0.0 when the model is unknown — envelope still ships). The `agent=` kwarg lets the CLI dispatcher pass a pre-built Agent so MCP loading + the Ollama reachability check happen once (otherwise we'd construct twice); tests inject a stub via the `_agent_factory` test seam. **THE CRITICAL DESIGN DECISION**: `run_id` is a BATCH correlation key, NOT injected into `Agent.session_id` — they're distinct concepts that live side-by-side in the envelope. The agent allocates its own session_id naturally; the RunResult records BOTH. Avoids the `resume_session_id` "this is restoring an existing session" semantics confusion. Query the audit log / session JSONL via `session_id`; aggregate a batch's CSV via `run_id`. **The envelope shape is STABLE** — future additions go via new optional fields, NEVER field removals — so any downstream parser can rely on it. `athena/__main__.py` gains four flags wired through `run_headless` for the `-p` path: `--json` (emit envelope on stdout, route UI to stderr — `subprocess.run(...).stdout` is always parser-friendly), `--run-id <id>` (operator-supplied correlation; auto-minted otherwise), `--timeout SECONDS` (wall-clock cap with graceful interrupt), `--task FILE` (alt to `-p` for long / shell-unfriendly prompts; wins over `-p` when both set so a batch driver can override defaults at the command line). When `--json` is set with no task → status=invalid envelope on stdout + exit 2 ("requires a task"). `_json_invalid_envelope(error, args, cfg, workspace)` helper builds a status=invalid RunResult JSON for the early validation paths (missing --task file, --json without task) so even those failures emit a properly-shaped envelope — **a batch caller parsing `--json` output always gets a valid envelope regardless of where in the dispatch the failure happened**. Backwards-compatible: existing `athena -p "<task>"` (no `--json`) keeps working exactly as before — exit 0 on success, human-readable output, no envelope on stdout (pinned by `test_legacy_dash_p_still_returns_zero_on_success` which asserts the stdout is NOT parseable as JSON). 41 tests across `tests/headless/`: 13 result-envelope (run_id format + uniqueness; parametrized exit_code per status; to_dict has all required keys + computed exit_code; to_json round-trips + single-line by default + indent=2 for inspection; assistant_text capped with session-id truncation marker; short text not truncated; default field values), 16 runner-core (success returns ok + populates every field including tokens dict and tool_calls in desc-count order; run_id minted/passed-through; timestamps; status=invalid for empty/whitespace task / nonexistent workspace / non-directory workspace each with right error message; agent exception → status=error with RuntimeError type+message in error field; KeyboardInterrupt → status=interrupted; on_info callback fires with starting message + run_id; agent.close always called on success AND error; empty stats → zero tokens with stable envelope shape), 12 CLI integration (argparse accepts --json + --run-id + --timeout; envelope single-line on stdout + parsable + nothing-else-on-stdout; --task FILE reads with multiline + quotes preserved; missing file → invalid+exit 2+valid JSON envelope; --task wins over -p when both set; --json without task → invalid+exit 2+"requires a task"; agent exception → exit 1 with structured envelope; **legacy -p path returns 0 + NO JSON on stdout** — the backwards-compat invariant pinned with `pytest.raises(json.JSONDecodeError)`). Reference: `docs/reference/headless.md` covers the exit-code table with shell-script branching example, full envelope shape with per-field semantics + the stability promise, the `--run-id` correlation model (operator's key vs athena's internal session_id), `--timeout` graceful-interrupt mechanics, `--task FILE` recipe, four composition recipes (one-off shell script with `jq`; manual iteration over a tasks list as proto-batch; cron job invocation; eval-harness pipeline), and explicit out-of-scope items (T7-02 batch_runner for concurrent batch + manifest; T7-03 eval-battery for formalised pipelines; streaming envelope; resume-on-failure; per-tool JSON details — those live in audit log + session JSONL keyed by session_id). The engine is ready; T7-02 batch_runner iterates this primitive (T7-01).
- `document_analyze` tool: extract clean text, a heading/section outline, tables, and metadata from PDF / DOCX; born-digital pages parsed directly, scanned PDF pages routed to OCR (T4-06) and merged so mixed documents come back whole; embedded figures optionally described via vision (T4-01); normalized output across formats; per-call artifact + JSONL audit row (T4-05). One tool, five modes: `text` (reading-order text only), `structure` (text + outline; default), `tables`, `metadata`, `full` (everything + optional figure descriptions). PyMuPDF for PDF + python-docx for DOCX with library choices isolated to the extractor modules (`athena/document/extractors/pdf.py` + `docx.py`); future formats (PPTX / RTF / ODT) land alongside one per file with the same Protocol shape. **The scanned-page OCR fallback is the load-bearing piece**: PDF extractor flags any page with text-layer chars < 20 (after strip) as `scanned_pages`; the tool rasterizes each via PyMuPDF at `cfg.document_rasterize_dpi` (200 default — OCR sweet spot), hands PNG bytes to T4-06's `ocr_recognize` (the composable helper path, not the @tool JSON layer), splices the OCR'd text into `page_texts[N]`, and rebuilds `result.text` from per-page slices joined by form-feed. OCR'd pages land in `ocr_pages` so the model distinguishes born-digital from OCR'd content. **THE LOAD-BEARING GRACEFUL-DEGRADE INVARIANT** pinned by THREE separate tests: no OCR backend on this host → `scanned_pages` stays populated, `ocr_pages` empty, NO error (`test_no_ocr_flags_scanned_pages`); `cfg.document_ocr_fallback=False` → OCR path never built (`test_ocr_fallback_disabled_by_cfg`); OCR returns empty text on a scanned page → page stays flagged but doesn't enter `ocr_pages` so the operator sees "we tried, no text came out" (`test_ocr_empty_result_doesnt_flag_ocr_pages`). The unavailable-≠-error invariant runs through the whole T4 stack. **Figure description via vision** opt-in via mode=full AND `cfg.document_describe_figures=True` AND `cfg.vision_enabled=True`. `_describe_figures` rasterizes each unique page containing a figure (cached per page so multi-figure pages render once), hands PNG bytes to vision_analyze describe via a temp file, fills `figure.description` in the result. Default off — figure description adds latency + tokens and isn't needed for every read. Mode-aware result trimming via `_trim_for_mode`: text mode returns text + pages + scanned/ocr lists; structure adds outline; tables returns only tables; metadata returns only metadata; full returns everything. Every mode carries the top-level identity (mode + path + sha256 + artifact_path) so the model branches predictably regardless of which mode ran. The PERSISTED artifact under `<profile>/documents/<stem>_<sha8>.json` is ALWAYS the full normalized result regardless of mode — the model gets the trimmed view, the operator gets the complete parse on disk for grep / inspection / re-use. Defensive paths: `document_analyze_enabled=False` short-circuits structured; unknown extract mode rejected; missing path / missing file rejected; unsupported document type (.txt, .xlsx, etc.) → structured `{"available": false, "reason": "unsupported document type"}` (NOT error — same invariant); extractor exception → `{"available": true, "error": "extract failed: ..."}` so the model sees what went wrong without a Python traceback; rasterize failure on a scanned page → page stays in `scanned_pages` but doesn't enter `ocr_pages`; OCR exception → debug log + page stays flagged but unOCR'd; vision exception during figure describe → figure keeps its bbox but description stays None. `@tool(name="document_analyze", toolset="vision")` — same toolset as vision / video / audio / OCR so the model sees the quintet grouped under "media analysis"; `athena/tools/__init__.py` imports the module on agent startup, mirroring T4-01/02/04/06's wiring. Config additions (every default safe): `document_analyze_enabled=True`, `document_default_extract="structure"`, `document_ocr_fallback=True`, `document_describe_figures=False`, `document_rasterize_dpi=200`, `document_output_dir=None` (None → `<profile>/documents/`). 35 new tests across `tests/document/`: 13 extractors (PDF: text in reading order across pages with form-feed; metadata title+author+page_count+format; outline level+page+title preserved; per-page text dict indexed 1-based; scanned-page flagged when text layer empty; rasterize returns valid PNG bytes; out-of-range page raises; normalized() round-trips JSON. DOCX: text+headings in reading order; outline captures Title + Heading-N at the right levels; table extraction returns correct rows; metadata title+author+format; normalized() JSON-safe). 22 tool layer (registration under vision toolset + schema has 5 modes; gate disabled / missing path / missing file / unknown mode / unsupported document type all rejected as structured payloads; PDF structure mode includes text+outline; text mode trim works; metadata mode only has metadata; full carries everything; artifact persists; DOCX structure + tables modes; **scanned-page → OCR → merged** with OCR'd text in result.text + ocr_pages populated; no OCR + cfg flag off + OCR empty → flagged but not in ocr_pages (3 graceful-degrade tests); figures: not called when no figures present; CALLED per unique figure page (caches rasterized page across multi-figure pages); not called when describe_figures=False; not called in non-full modes regardless of vision setting; audit row written per call with all expected fields; artifact persisted with valid normalized JSON). Test fixtures synthesised via PyMuPDF + python-docx writers — same reproducibility story as the rest of athena's media fixtures (no binary blobs in the repo); three PDF synthesisers (text, outline, scanned-page) plus one DOCX synthesiser (Title + Heading-N + table). Allowlisted in `tests/safety/test_no_raw_writes.py` for the artifact-write path — same operational-artifact justification as audio/video/vision hash-logs. Reference: `docs/reference/document.md` covers modes table, full result shape, the scanned-page OCR fallback with the graceful-degrade invariant explicitly enumerated, figure description workflow, hash-log + persistent artifact shape, config block, dependencies (PyMuPDF + python-docx + optional T4-06 OCR + optional T4-01 vision), explicit non-goals (no bulk format support, no layout reconstruction, no form filling, no DOCX-page OCR, no streaming).
- OCR backend: an `ocr` tool reads text (with per-block bounding boxes + confidence) from images and scanned pages via a manifest-resolved engine (local-preferred → on-device); fills the OCR non-goal T4-01 vision explicitly declared, and gives T4-05 document_analyze its scanned-page backend (T4-06). Vision *describes* an image; OCR *reads the text in* it — different jobs, different backends, same manifest-driven routing. `Capabilities` gains `ocr: bool = False` — same conservative-default pattern as audio_transcription (T4-04) / video_generation (T6-05) / social_search (T6-02). Pair with `is_local=True` for on-device OCR (the privacy win the rest of the local-first media stack establishes). `athena/ocr/contract.py` defines the normalized contract every adapter satisfies: `OCRBlock(text, bbox, confidence)` with bbox as `(x0, y0, x1, y1)` in pixel coordinates (top-left origin) + confidence 0..100 (tesseract convention; cloud engines reporting 0..1 are scaled at the adapter boundary so the tool's min_confidence filter compares against this number directly); `OCRResult(blocks, language?)` with `joined_text(separator)` concatenating non-empty blocks in recognition order (default newline matches tesseract's reading flow) and `to_dict(with_boxes=True)` that omits the `blocks` list when `with_boxes=False` (text-only result for callers that don't need bbox info); `OCRBackend` Protocol surface (`is_available()` / `recognize(path, *, langs, with_boxes) -> OCRResult`). `athena/ocr/backends/tesseract_local.py:TesseractLocalBackend` registered via `@register_provider` declaring `Capabilities(ocr=True, is_local=True, tool_calls=False, streaming=False)` — capability-only, NOT a chat backend; `stream_chat` raises NotImplementedError; same shape as T4-04's audio_whisper_local and T6-05's stub_video_local. Engine isolation: ALL pytesseract / tesseract-binary specifics live in this one file; real cloud OCR adapters (Vision API, AWS Textract) land alongside as separate modules with the same Protocol shape. `is_available()` returns True iff BOTH (a) pytesseract is importable AND (b) the tesseract binary can be located — either via `shutil.which("tesseract")` (PATH lookup) or via `cfg.ocr_tesseract_cmd` if set explicitly (common on Windows where the binary isn't on default PATH). Without the binary, athena startup is never blocked; the broker finds no OCR backend and the ocr tool surfaces "no OCR configured" cleanly — the unavailable-≠-error invariant. `recognize()` runs `pytesseract.image_to_data` when `with_boxes=True` (one row per word + bbox + confidence) or `image_to_string` when False (faster, text-only). Coalesces per-word rows into block-level OCRBlocks via `_coalesce_blocks`: groups by (page, block_num, par_num) tuple, takes the bbox covering all words in the group, averages confidences, preserves recognition order. Rows with `conf=-1` (tesseract's "no confidence" marker) are skipped so they don't poison the average — pinned by `test_coalesce_blocks_skips_negative_confidence_rows`. Defensive on every engine failure: returns empty `OCRResult` + WARNING log rather than raising; the tool's "no text" path takes over gracefully. `athena/ocr/backends/__init__.py` imports `tesseract_local` inside a try/except so a missing optional dep doesn't break athena startup. `athena/ocr/tools.py:ocr_recognize(path, *, cfg, backend, languages, with_boxes, min_confidence)` is the composable end-to-end helper consumed by T4-05 document_analyze and (eventually) T4-01 vision when "what does the text say" routes here. Returns raw `OCRResult` (bypasses the tool's JSON formatting). `min_confidence` defaults to `cfg.ocr_min_confidence` when None; 0 = no filter (default); 60+ filters noisy recognitions. No backend → empty `OCRResult` (no error). Missing file → `FileNotFoundError`. Engine failure → swallowed at the backend layer + empty result. `@tool(name="ocr", toolset="vision")` — same toolset as vision / video / audio so the model sees the quartet grouped under "media analysis". Tool result is structured JSON across every branch: `{available: true, backend, path, text, blocks?, language?}` on success; `{available: false, reason: "no OCR backend configured"}` when unavailable; `{available: false, error: "file not found: ..."}` / `{available: false, error: "path is required"}` / `{available: false, error: "ocr_enabled=False; ..."}` for validation. Config additions (every default safe): `ocr_enabled=True`, `ocr_backend_prefer="local"`, `ocr_languages=["eng"]`, `ocr_min_confidence=0`, `ocr_tesseract_cmd=None`. 35 tests across `tests/ocr/`: 17 backend (capability declaration with safe default + supports() lookup + TesseractLocalBackend declares ocr+is_local+NOT-chat; MediaRegistry routes ocr to local + can() True; OCRBlock + OCRResult to_dict including with_boxes=False omits blocks + joined_text skips empty blocks + empty result; stub backend protocol contract; tesseract is_available returns bool; chat methods raise; `_coalesce_blocks` groups per-word rows correctly + skips conf=-1 rows); 18 tools (registered under vision toolset + schema has path/languages/with_boxes/min_confidence; ocr_enabled=False short-circuits; missing path/file rejected; no-backend branch via monkeypatch returns structured unavailable; recognize returns text+blocks+language+backend name; with_boxes=False omits blocks; languages flow from cfg + per-call override wins; min_confidence drops low-confidence blocks AND adjusts joined text accordingly; cfg.ocr_min_confidence honored; 0 keeps everything; backend exception → structured empty result not crash; ocr_recognize composable helper returns OCRResult directly + applies min_confidence + handles no-backend + raises FileNotFoundError on missing). Reference: `docs/reference/ocr.md` covers the OCR-vs-vision split with explicit examples, tool result shape across success + four failure branches, arguments table, confidence-as-the-honesty-knob discussion, manifest-driven backend resolution, the composable helper for T4-05 / T4-01 callers, installation per OS (scoop/brew/apt) including multi-language tessdata, config block, consumers (T4-05 + T4-01), and explicit non-goals (no layout reconstruction, no handwriting in default backend, no bulk preprocessing, no streaming). T4-01 vision-analyze.md's "OCR not in T4-01" non-goal is now struck through with a "closed by T4-06" callout pointing at the OCR reference.
- `audio_analyze` tool: timestamped transcription with optional speaker diarization + coarse content classification, backed by a manifest-resolved STT backend (local-preferred → on-device). Closes the audio gap T4-02 originally punted on — `video_analyze mode=analyze` now extracts the audio track via ffmpeg and runs `transcribe_track` on it; the transcript lands in the result under a `transcript` field aligned to the per-frame analyses by absolute file-seconds (T4-04). `Capabilities` gains `audio_transcription: bool = False` — same conservative-default + manifest-routing pattern as T6-02 `social_search` and T6-05 `video_generation`. The in-tree backend is `athena/audio/backends/faster_whisper_local.py` — Whisper-class STT via `faster-whisper` (CTranslate2-backed, runs well on CPU with int8). Declares `Capabilities(audio_transcription=True, is_local=True, tool_calls=False, streaming=False)` so the broker picks it under default local-preferred config. LAZY model load: importing the module costs nothing; the `WhisperModel` is constructed on first `transcribe()` call and cached in a process-wide singleton behind a `threading.Lock` so repeat calls don't reload. Model selectable via `cfg.audio_whisper_model` ("base" default ~74 MB; "tiny" / "small" / "medium" / "large-v3" also valid); device + compute type via `cfg.audio_whisper_device` (auto/cpu/cuda) + `cfg.audio_whisper_compute_type` (auto/int8/float16/float32). Defensive on transcribe failure: returns `TranscribeResult(segments=[])` + WARNING log rather than raising. Diarization: faster-whisper proper doesn't ship a speaker diarizer (pyannote.audio's job — separate heavy dep); the backend honors `diarize=True` per the `AudioBackend` Protocol contract by returning segments with `speaker=None`, which the tool's `to_dict` omits cleanly. A future `pyannote_diarizer.py` backend can declare a separate capability and compose. `athena/audio/job.py` defines the normalized contract every adapter satisfies: `Segment(start, end, text, speaker?)` with timestamps in **absolute file-seconds** (not chunk-relative — the tool layer pushes per-chunk offsets through `chunk_offset_s` so segments come back ready-aligned); `TranscribeResult(segments, language?, duration?, content_type?)` with `to_dict()` that round-trips JSON-safe + omits optional fields when unset (so the tool's JSON output stays clean); `ContentType` literal type (`speech | music | silence | mixed | unknown`); `AudioBackend` Protocol surface (`is_available()` / `transcribe(path, *, language, diarize, chunk_offset_s) -> TranscribeResult` / `classify(path) -> ContentType`). `athena/audio/backends/__init__.py` imports faster_whisper_local inside a try/except so a missing optional dep doesn't break athena startup — the broker simply finds no audio backend and the tool surfaces "no backend configured" cleanly. `athena/audio/tools.py` is the model-facing entry point — one tool, four modes (`transcribe` / `diarize` / `classify` / `full`); `@tool(toolset="vision")` so the model sees vision + video + audio grouped under "media analysis". Long-audio handling is the load-bearing piece: `_chunk_boundaries(duration, *, chunk_s, overlap_s)` produces overlapping windows with step = `chunk_s - overlap_s`, defensive on `chunk_s<=0` (returns one chunk, no infinite loop on bad config). `_stitch(chunk_results, *, chunk_windows, overlap_s)` merges per-chunk segments in time order and de-duplicates at chunk seams: a segment in the overlap region of chunk N+1 whose text + rounded-start matches a segment from chunk N is dropped. **The load-bearing seam-integrity invariant**: words spoken at chunk boundaries appear once in the output, not twice — pinned by `test_stitch_dedupes_seam_repeat`. And the converse: genuine repeat phrases later in the audio are NOT dropped just because the text matches earlier text — pinned by `test_stitch_keeps_segments_outside_overlap_even_if_text_repeats`. `transcribe_track(path, *, cfg, backend, language, diarize, progress)` is the composable end-to-end helper: short audio → single backend call at offset 0; long audio → N backend calls at monotonically-increasing chunk offsets, then stitch. `progress(i, n)` fires per chunk including `progress(1, 1)` for the single-shot path so a UI surface can confirm completion uniformly. The transcript artifact writer emits two files to `cfg.audio_output_dir` (default `<profile>/audio/`): `<stem>_<sha8>.json` (full result, machine-readable) and `<stem>_<sha8>.txt` (one line per segment with `[start-end]` timestamps + optional speaker prefix — grep-friendly operator view). Both are deterministic for a given source so reruns overwrite predictably. Every call writes a JSONL audit row to `<profile>/audio_audit.jsonl` with mode + path + sha256 + segment_count + language + duration + transcript_path + diarize — same provenance shape as T4-01 vision / T4-02 video; allowlisted in `tests/safety/test_no_raw_writes.py` under the same operational-artifact justification. **Diarization is opt-in via TWO gates**: `cfg.audio_diarization_enabled=False` (default — operator-level decision) AND `mode in {"diarize", "full"}` (per-call decision). Both must agree for the backend to receive `diarize=True` (pinned by `test_diarize_mode_passes_diarize_false_when_disabled`). Defensive paths: `audio_analyze_enabled=False` short-circuits with structured payload; unknown mode → structured error; missing path / missing file → structured error; no backend → structured `{"available": false, "reason": "no audio backend configured"}` (NOT an error — the spec's "unavailable != error" invariant); `backend.transcribe` raising → structured `"transcribe failed: <exception type>: <message>"` so the model can reason about it instead of seeing a Python traceback. **Video integration (closes the T4-02 audio gap)**: `_default_audio_transcribe_fn(cfg)` extracts the video's audio track via `ffmpeg -vn -ac 1 -ar 16000 -acodec pcm_s16le` (mono + 16 kHz matches whisper-class model inputs; smaller files = faster transcription) into a temp WAV, calls `transcribe_track()`, cleans up the temp WAV (the persistent artifact is the transcript JSON under `<profile>/audio/`). Returns None when `cfg.audio_analyze_enabled=False` or ffmpeg isn't on PATH; the video tool's analyze mode threads `audio_fn=None` through and omits the `transcript` field accordingly. **Audio failure must not break video analysis** — the analyze code wraps the audio_fn call in try/except and surfaces the analyze result cleanly with frames + per-frame analyses intact even when audio raises (pinned by `test_analyze_mode_handles_audio_fn_exception`). The video tool's `_run` accepts an `_audio_fn` parameter so tests inject stubs; production routes via `_default_audio_transcribe_fn(cfg)`. T4-02's `docs/reference/video-analyze.md` "Audio inspection out of scope" non-goal is now struck through with a "closed by T4-04" callout. Config additions (every default safe): `audio_analyze_enabled=True`, `audio_backend_prefer="local"`, `audio_diarization_enabled=False`, `audio_chunk_seconds=30.0`, `audio_chunk_overlap_s=2.0`, `audio_whisper_model="base"`, `audio_whisper_device="auto"`, `audio_whisper_compute_type="auto"`, `audio_output_dir=None` (None → `<profile>/audio/`). 54 new tests across `tests/audio/`: 14 backend (capability declaration with safe defaults; supports() lookup; FasterWhisperLocalBackend declares audio_transcription + is_local + NOT-a-chat-backend; MediaRegistry routes audio_transcription to local; can() returns True; Segment + TranscribeResult to_dict shapes including the `speaker=None → field absent` invariant; stub backend chunk_offset_s pushes timestamps forward; is_available + classify behave; chat methods on the capability-only provider raise NotImplementedError); 19 tools (registered under vision toolset + schema lists all modes; audio_analyze_enabled=False short-circuits with no work; unknown mode / missing path / missing file rejected; no-backend branch via monkeypatch returns structured unavailable; transcribe happy path returns segments + transcript artifact + sha256 + audit row; transcript .txt sidecar one-line-per-segment; diarize-mode dual-gate (cfg flag + mode); segments carry/omit speaker based on backend support; classify + full modes set content_type; transcribe_track returns TranscribeResult directly; no-backend returns empty result without raising; missing-file raises FileNotFoundError; backend exception → structured error); 17 chunking (boundary computation across short / exactly-at-chunk / long-with-overlap / zero-chunk-defensive / zero-overlap-contiguous; stitch passthrough for one chunk; concatenates distinct segments; dedupes seam repeats; keeps repeat-text segments outside overlap region; carries language from first detection; sets duration from last window; end-to-end short audio = 1 backend call at offset 0; long audio = N calls with monotonically-increasing offsets; absolute timestamps correct after stitching; progress callback fires per chunk; progress(1,1) for single-chunk path); 4 video integration (analyze mode includes transcript when audio_fn available; omits transcript when audio_fn returns None; defensive: audio_fn raising still returns clean analyze result; cfg.audio_analyze_enabled=False short-circuits the default factory so analyze runs without trying audio). Reference: `docs/reference/audio-analyze.md` covers modes, hash-log + transcript artifact shape, manifest backend resolution + faster-whisper config knobs, long-audio chunking with the seam-integrity invariant, diarization two-gate opt-in, video integration end-to-end, tool result shape across success and four failure branches, config block, dependencies (`pip install faster-whisper`), explicit non-goals (no real-time / streaming, no TTS, no diarization in the default backend yet, coarse content classification only). Local-first stays the privacy win: with the default backend, recordings never leave the machine.
- Gateway credentials migrated off cleartext-in-config.toml. Every platform secret (Discord `bot_token`, Slack `bot_token` + `app_token`, Telegram `bot_token`, Matrix `access_token`, iMessage `password`, Email `imap_password` + `smtp_password`) now resolves through `athena.gateway.credentials.resolve_credential(settings, key, *, platform, required=True)` which prefers `<key>_path` (a 0o600 file containing just the secret — no quoting, no JSON wrapping, trailing whitespace stripped) and falls back to the legacy cleartext `<key>` with a **one-shot deprecation warning per (platform, key) pair**. The file-on-disk shape keeps tokens out of dotfile backups, out of `cat ~/.athena/config.toml` over a shared screen, and out of any tool that picks up dotfiles. The resolver is read per call so **rotation is a single file rewrite — no athena restart needed**. Defensive: `<key>_path` set but file unreadable / empty → warning + falls back to cleartext; both missing + `required=True` → `ValueError` naming BOTH possible config shapes ("set either `<key>_path` (a 0o600 file with the secret) or `<key>` (cleartext, deprecated)") so the operator knows exactly what to fix; both missing + `required=False` → None for optional secrets. The deprecation nudge fires **once per process** per (platform, key) pair via a `_warned: set` + threading.Lock memo, so a flapping reconnect after a transient SDK disconnect doesn't spam the log. **Token-never-in-logs invariant** pinned by `test_token_never_in_logs_via_path` and `test_token_never_in_logs_via_cleartext` which capture the full DEBUG log across both branches and assert the secret material never appears (the DEBUG line on the file-path success emits `path=<path> len=<N>` so an operator can confirm "the file was read" without knowing the value; the deprecation nudge names the config key and the migration path, not the value). 15 tests in `tests/gateway/test_credentials.py`: file-path branch (returns stripped contents / strips trailing whitespace / wins over cleartext when both set / handles `~` expansion); cleartext-fallback branch (returns value / fires deprecation warning once per (platform, key) pair / different platforms warn independently / different keys warn independently); fallback chain (missing file falls back to cleartext / empty file falls back to cleartext with a warning); missing-both behavior (required=True raises ValueError naming both shapes / required=False returns None / empty string in cleartext doesn't count as present); never-in-logs proof across both branches. `athena/cli/gateway.py:_instantiate_adapter` rewritten to route every long-lived secret through `resolve_credential`; per-platform required-field checks still apply for non-secret keys (homeserver / user_id / device_id for matrix; rest_url / account_number for signal; imap_host / imap_user / smtp_host / smtp_user / from_address for email) but the secret-shaped fields now have both config shapes available. Reference: `docs/reference/gateway-credentials.md` covers the migration recipe (PowerShell + bash variants) per platform, the per-platform key table, the resolver semantics + warning ordering, the rotation flow (rewrite the file, optional SDK reconnect), the test-coverage map, and the relation to `docs/reference/social-routing.md`'s X bearer-token migration that this resolver borrows its shape from. Full gateway suite (401 tests) still green after the refactor.
- App-only Bearer token auth path for the social provider (X / Twitter v2 + any other vendor that ships a single-string app token instead of OAuth 2.0 user-context). `SocialProvider.is_available()` and `SocialProvider.social_search()` both check `cfg.social_bearer_token_path` FIRST and skip the OAuth adapter entirely when configured; OAuth falls back when not. The token file is just the token — no quoting, no JSON wrapping, no Bearer prefix — and lives at `~/.athena/social/bearer_token.txt` (0o600) by default. `_read_bearer_token(cfg)` is the only function outside the OAuth adapter that touches token-shaped bytes; logs the path + length at DEBUG, never the token material. **Token-never-in-logs invariant** pinned by `test_bearer_token_never_in_logs` which captures the full DEBUG log across a search call and asserts the token doesn't appear in the captured text. Bearer path takes priority over OAuth (pinned by `test_bearer_takes_priority_over_oauth` with an OAuth stub that raises if consulted). Defensive: empty / missing / unreadable file → None silently; OAuth fallback fires; both paths absent → `is_available()` returns False and `search_x` surfaces "no provider configured" instead of crashing. **Operator-visible HTTP error bodies**: `_get` now catches `urllib.error.HTTPError` separately from generic transport failure and reads the response body (capped at 500 bytes) into the WARNING log — so when X returns `{"title":"CreditsDepleted","detail":"...","type":"..."}` on a 402, the operator sees the structured reason directly in the log instead of just "HTTP Error 402". Two new config fields: `social_bearer_token_path: str | None = None` (the path with a documented "never paste the token into config.toml directly" note); the existing `social_search_url` / `social_search_query_param` / `social_search_extra_params` / `social_post_url_template` keys carry over unchanged. 14 new tests in `tests/social/test_bearer_token.py`: helper-level (`_read_bearer_token` returns None when unset / file missing / file empty / strips whitespace / handles `~` expansion / never logs the token), provider-level (`is_available` True with bearer only / False without either / OAuth fallback when no bearer / OAuth never consulted when bearer present), end-to-end (`social_search` uses bearer auth without touching OAuth via a stub that raises if reached; OAuth fallback path still works after the refactor; no-token → empty list with NO transport call; **token never appears in DEBUG capture** across the full search path; bearer takes priority — pinned with an OAuth stub spy `.calls` counter staying at 0). Reference: `docs/reference/social-routing.md` adds an "Auth path A — app-only Bearer token" section ahead of the existing OAuth content, with PowerShell + bash recipes for writing the token to the 0o600 file (the token never traverses the chat / agent log via the recipe), the X-specific config snippet, the same load-bearing safety properties as the OAuth path (single-call-site / DEBUG-log redaction / never-in-logs test), the **"rotate immediately on suspected leak"** rule (regenerate in the dev portal before doing anything else; old value stops working at regeneration; rewrite the bearer_token.txt with the new value via the same recipe; no athena restart needed since the file is read per call), and X-specific notes including the current `/2/tweets/search/recent` tier requirement (X 402 `CreditsDepleted` on free tier — athena returns `[]` and surfaces the structured reason in the log). The OAuth path is preserved unchanged for vendors that require user-context (or for a future posting capability) — renamed "Auth path B — OAuth 2.0 user-context" in the docs.
- Computer use refactored onto athena's existing ContextVar approval system: the three-tier permission gate (observe / input / destructive) now routes every action through `athena.safety.approval_guard.request_approval_sync` + the active `approval_callback` instead of a bespoke confirm callback. Net effect — **input is impossible in any autonomous / fork run** via `ApprovalDeniedInBackground`, prompts surface through the same UI the rest of athena uses (interactive `ui.confirm` / ACP `permission_request` / gateway adapters), and the approval grant cache is now the real `_approval_grants` ContextVar (no parallel boolean) (T6-04R, replacing T6-04's bespoke gate). `athena/safety/approval_guard.py` gains two new primitives: `request_approval_sync(resource_id, prompt, *, auto_approve_in_background=False) -> bool` — sync sibling of the existing async `request_approval`; shares the same `_approval_grants` ContextVar so semantics are identical (FOREGROUND + cache hit → return cached bool no prompt; FOREGROUND + miss → call sync prompt, cache + return; background + `auto_approve_in_background` → True with prompt NOT called; background otherwise → raise `ApprovalDeniedInBackground` BEFORE prompting). Required because athena's tool dispatch is sync; calling the async version through `asyncio.run` would isolate grants inside an ephemeral context. AND `clear_grants() -> None` — drops every cached grant in the current ContextVar permanently, unlike `reset_approvals(scope_fresh_approvals())` which is a scope round-trip that immediately restores the prior state. Used by the panic kill switch + per-turn cleanup paths that want a clean slate. `athena/computer/permission.py:PermissionGate` rewritten: constructor no longer takes `confirm`; `check()` dispatches by tier through approval_guard. Cache policy is the load-bearing T6-04R design: **input tier** → stable `"computer_input"` resource_id, grant caches per turn / per scope (the user isn't re-prompted on every keystroke within an approved task); **destructive tier** → per-action resource_id (`f"computer_destructive::{sha256(action.describe())[:16]}"`), the cache NEVER hits — every destructive action freshly prompts AND distinct destructive actions get distinct resource_ids so a "Delete file" grant CANNOT satisfy a "Submit form" check (pinned by `test_destructive_actions_get_distinct_resource_ids`). The full check() ordering, in priority: observe always passes → panic engaged refuses without prompting → denylisted app refuses without prompting → not in allowlist / `observe_only` mode / unknown mode all refuse without burning a prompt → goal-loop active + `cfg.computer_deny_during_goal_loop=True` refuses without prompting (the RECON gap the design called out — the /goal continuation runs in FOREGROUND so approval_guard alone wouldn't catch it) → background-origin (BACKGROUND_REVIEW / CURATOR / MIGRATION / SYSTEM) raises `ApprovalDeniedInBackground` inside `request_approval_sync`, gate catches + returns False WITHOUT invoking the approval callback (cheaper + safer than relying on AUTO_DENY) → destructive/input routes through approval_guard with the tier-appropriate resource_id. `computer_use_panic(cfg=None)` + `computer_use_unpanic()` — the kill switch as a process-wide flag (`_panic_engaged`, threading.Lock-protected, idempotent). Panic engages the flag AND drops every cached grant via `clear_grants()`. The gate consults the flag at the TOP of every check; engaged → every input/destructive call refused WITHOUT prompting (so a hostile UI can't dance around the panic by re-prompting). Disengaging via `computer_use_unpanic()` doesn't restore the dropped grants — fresh approval prompts have to rebuild them. `reset_session()` now wraps `clear_grants()` — the ContextVar IS the source of truth, no parallel boolean. `goal_loop_active(cfg)` reads `goal/state.load_state(profile_dir(cfg.profile))`; defensive — any exception in the lookup returns False so a goal-module bug never auto-engages the deny. `_classifier_resource_id(action)` is the SHA-256 short hash so semantically distinct destructive actions get distinct resource_ids; payload is `action.describe() or "?"` encoded UTF-8 with `errors="replace"` so a hostile target_desc with arbitrary bytes can't break the hash. `_KNOWN_PERMISSION_MODES = frozenset({"observe_only", "per_action", "per_session"})` — unknown modes (typos in the config file) get treated like observe_only defensively. **Config addition:** `computer_deny_during_goal_loop: bool = True` — the /goal autonomous continuation loop runs in FOREGROUND origin (T5-07 design re-uses the main agent context), so approval_guard's background-deny doesn't catch it; this is the computer-use-specific extra check. Default True — the autonomous loop never gets to drive the desktop unless the operator deliberately disables this. **`athena/computer/tools.py:_gate_for(cfg)` simplified** — no longer constructs a default-deny confirm-callback shim; just `return PermissionGate(cfg=cfg)`. Approval prompts surface via whatever's bound to `approval_callback.get_approval_callback()` at the moment of the prompt: interactive `ui.confirm` in REPL sessions, ACP `permission_request` in IDE sessions, gateway adapters (Slack/Discord/Matrix/email) in their respective sessions, `AUTO_DENY` inside any fork (though the background-origin check catches forks BEFORE the prompt fires). **Test infrastructure** — `tests/computer/conftest.py` adds an autouse fixture that snapshots + restores both ContextVars (approval_callback + approval grants) per test plus clears the panic flag, since ContextVars survive across pytest function calls within the same task. **52 new + rewritten tests** across `tests/computer/` and `tests/safety/`: 4 in `tests/safety/test_approval_guard.py` pinning `request_approval_sync` (FOREGROUND prompts + caches under stable resource_id; background raises `ApprovalDeniedInBackground`; background + `auto_approve_in_background=True` bypasses the raise + skips the prompt entirely; `clear_grants` drops every cached grant); 17 in `tests/computer/test_gate_tiers.py` pinning the T6-04R invariants (observe no approval + callback NEVER invoked + no cache pollution; input requests then caches under `"computer_input"`; input denial also caches so the user isn't re-prompted with the same question; destructive never cached per-action — different destructive actions get distinct resource_ids AND a "Delete file" grant can't satisfy a "Submit form" check; click without target classified destructive; destructive-verb regex catches Delete/Submit/Pay now/Sign out/Erase all; background-origin denies both input AND destructive without invoking the callback; **the load-bearing panic test** — engaging panic drops cached grants + further input/destructive refuse WITHOUT burning a prompt + disengage re-prompts the next action; goal-loop deny when `_goal_loop_active(cfg)` returns True with the approval callback NEVER consulted; opt-out via `computer_deny_during_goal_loop=False` short-circuits the check; denylist + observe_only + not-in-allowlist all refuse without burning prompts; callback exception treated as denial; `reset_session` clears the input grant); 22 in `tests/computer/test_permission.py` (legacy invariants rewritten for the new API — every test now binds an approval callback via `set_approval_callback` and asserts what was asked; pins the T6-04R semantic change that input ALWAYS caches now: `test_input_caches_after_first_grant` documents per_action mode behaves like the legacy per_session for input while destructive still freshly prompts; every other legacy invariant preserved — denylist wins, allowlist required, observe-tier free, observe_only blocks input but allows observe, destructive always confirms in any non-restrictive mode, callback raise → denial); 8 in `tests/computer/test_input_tools_gated.py` pinning the load-bearing **no-tool-bypasses-the-gate** invariant via a spy backend whose `perform_calls` list records every backend invocation — **the denied input/destructive tests prove `backend.perform_calls == []`**; the `test_background_origin_blocks_every_tool` test cycles through ALL four input tools (click/type/key/scroll) under `BACKGROUND_REVIEW` origin and asserts both `backend.perform_calls == []` AND `callback_calls == []` (the background-deny fires before the prompt; defense-in-depth proof that the autonomous-run-cannot-drive-input guarantee holds at the tool boundary); audit log records tier for every action; `cfg.computer_use_enabled=False` refuses without backend contact AND without approval prompt; 5 in `tests/computer/test_observe_no_approval.py` pinning that observe-tier MUST stay free of any approval surface across the refactor (succeeds under AUTO_DENY callback / under BACKGROUND_REVIEW origin / under CURATOR origin / NEVER caches a grant / disabled gate short-circuits without burning a prompt). **Test pollution fix surfaced during the refactor**: `tests/computer/test_tools_observe.py::test_observe_no_vision_backend` was failing intermittently in full-suite runs (passing in isolation) because `monkeypatch.setattr("athena.providers._REGISTRY", {})` doesn't propagate to the `from .. import _REGISTRY` reference inside `athena.media.registry`. Fixed by patching both module attributes — sibling positive test was already doing this; the negative test was the outlier. Reference: `docs/reference/computer-use.md` rewritten to lead with the safety model. Sections in order: the inverse-of-sandbox warning, the permission model with the per-tier cache policy table, the background-deny guarantee (autonomous runs can't drive input; `auto_approve_in_background=True` is intentionally NOT used for computer use), the goal-loop guarantee (the FOREGROUND-origin gap + the computer-use-specific extra check), the kill switch (panic + hotkey + Ctrl+C), safe defaults table including the new `computer_deny_during_goal_loop`, where prompts actually show up (the existing approval-callback binding is the single source of truth — interactive / ACP / gateway adapter / AUTO_DENY in forks), tier classification by example, audit log shape (screenshot SHA-256 not bytes — same calculus as vision/hashlog and browser/capture), explicit non-goals (not a sandbox / not UI testing coverage / not a CAPTCHA bypass / not always-on), and the resource_id glossary. `athena/computer/RECON.md` documents the recon findings + the gap analysis for the goal-loop case + the refactor plan (T6-04R).
- Persistent CDP browser tools via Playwright: nine `browser_*` tools sharing **one Chromium context per athena session** — cookies + localStorage + open pages survive across tool calls so a multi-step web workflow (search → click results → paginate → screenshot → extract) reasons over the SAME browser state rather than relaunching per call. The persistence is the whole point; pinned end-to-end by `test_persistent_context_keeps_cookies_across_calls` which sets a cookie on one navigation, navigates elsewhere, and asserts the cookie is still present in the next `browser_get_cookies()` call (T4-03). `athena/browser/session.py:BrowserSession(session_id, cfg)` is the persistent-context manager — sync (athena's runtime; spec was async). Constructor is **lazy**: NO chromium launch until `ensure_started()` runs. The whole point of lazy launch is that an unused browser pays no Chromium cost; an athena session that never calls a browser tool sees zero subprocess overhead (pinned by `test_unused_browser_never_launches` which patches `sync_playwright` to AssertionError("must NOT fire") and runs a full construct → bind → close cycle — if any path accidentally ate the lazy contract the assertion fires). `ensure_started()` is idempotent; `close()` is idempotent; `BrowserUnavailable` is raised with a clear install hint ("pip install -e \".[browser]\" && playwright install chromium") when Playwright isn't importable, when `.page`/`.context` are accessed before start, or when a tool is called outside an athena session that bound a browser. `_active_browser` ContextVar mirrors T3-03's checkpoint manager pattern — `get_active_browser` / `set_active_browser` thread the session through to tools without a thread-the-needle argument cascade; forks inherit via ContextVar copy at task spawn so a fork uses the parent's browser by default. Per-session user-data dir at `<browser_user_data_root>/<session_id>/` (default `~/.athena/browser/<session_id>/`) — two concurrent sessions get distinct cookie jars; persists after `browser_close()` so a future T2 resume can reattach to the same state. Realistic desktop Chrome UA by default (`Mozilla/5.0 (Windows NT 10.0; Win64; x64) ... Chrome/124.0.0.0 Safari/537.36`) so legitimate research targets that serve different HTML to stock Playwright UAs see a normal browser; override with `cfg.browser_user_agent`. `accept_downloads=False` by default — automated browsing rarely needs them and they're a footgun. `athena/browser/capture.py:CaptureLogger(path, *, min_interval_s, sleep_fn)` is the accountability + politeness surface. Capture log: every navigation appends a JSONL row `{ts, session_id, url, final_url, status, title, screenshot_path, content_sha256}` to `<profile_dir>/browser_capture.jsonl`; the page bytes are NOT stored, only a SHA-256 (same calculus as T4-01.2 vision hash-log and T6-04.4 computer audit — provenance over volume). Allowlisted in `tests/safety/test_no_raw_writes.py` with the same justification (operational metadata, append-only, not agent-driven content mutation). Politeness throttle: per-domain minimum interval between navigations (`cfg.browser_min_interval_s`, default 1.0s) — the persistent browser model lets the agent navigate fast; without the throttle, "search → click first result → click another result" hammers a single site. Cross-domain navigations don't sleep; each domain has its own "last seen" timestamp in `_last_nav_by_domain`. `sleep_fn` is injectable so tests assert "sleep was called with N seconds" without actually waiting. `min_interval_s=0` disables entirely. `athena/browser/tools.py` registers nine `@tool(toolset="browser")` functions: `browser_navigate(url, wait="load")` (with capture log + throttle), `browser_screenshot(full_page, selector, analyze_prompt)` (PNG to disk; optional `analyze_prompt` routes through T4-01 vision_analyze describe in one call — pinned by `test_screenshot_analyze_routes_to_vision` that stubs vision_analyze._run via sys.modules monkeypatch and asserts the right mode+prompt), `browser_extract_text(selector?)`, `browser_extract_links()`, `browser_click(selector)`, `browser_fill(selector, value)`, `browser_wait_for(selector, timeout=10)`, `browser_get_cookies()`, `browser_close()`. Every tool returns JSON (str) so the model can parse it; gates on `cfg.browser_enabled` FIRST with NO Playwright contact when disabled; reads the active `BrowserSession` from the ContextVar with structured "no active browser session" payload when absent; calls `session.ensure_started()` inside the gate (first tool launches chromium, subsequent calls reuse); returns `{"error": "..."}` JSON for Playwright exceptions rather than raising into the model loop. The screenshot path with `analyze_prompt` is the multiplier — a lot of sites render data into the DOM in ways that are painful to extract as text but trivial for a multimodal model to read off a screenshot. `athena/agent/core.py:Agent.__init__` binds the persistent `BrowserSession` for the athena session right after the cross-session cache init: when `session_id` is allocated and `cfg.browser_enabled=True` it constructs `BrowserSession(session_id, cfg)` + `set_active_browser(...)`. Best-effort: any exception in this path debug-logs + leaves `self.browser_session = None` — a bad browser init must never block session start (same calculus as the cross-session cache). `Agent.close` tears down: `self.browser_session.close()` + `set_active_browser(None)` after the existing session-store teardown; idempotent on never-started sessions; user_data_dir on disk persists for future resume. NOT a CAPTCHA solver, NOT a login-wall bypass: if a target requires auth, the operator provides credentials explicitly via `browser_fill`. The realistic UA + capture log is the tradeoff: legitimate public-target research isn't trivially bot-blocked, and the capture log is the accountability surface that makes this a tool an analyst can defend in a review. Test fixture server (`tests/browser/conftest.py`): stdlib `http.server` on an ephemeral port serving an HTML test page (`<title>Test Page</title>`, intro paragraph, 3 links, a form with input/submit, a hidden div with target-text) + `/set-cookie` endpoint (Set-Cookie: athena=ok, used by the persistent-context cookie test) + `/page-a` minimal page. Browser tests don't hit the live internet. `[browser]` optional dep adds `playwright>=1.40`; `playwright install chromium` fetches the browser binary. Config additions (every default safe): `browser_enabled=True`, `browser_engine="chromium"`, `browser_headless=True`, `browser_user_data_root=None` (None → `~/.athena/browser`), `browser_capture_path=None` (None → `<profile>/browser_capture.jsonl`), `browser_screenshots_dir=None` (None → `<profile>/browser/shots`), `browser_nav_timeout_s=30`, `browser_min_interval_s=1.0`, `browser_block_downloads=True`, `browser_user_agent=None`. 41 new tests across `tests/browser/`: 3 local-server fixture sanity (serves test page, /set-cookie returns Set-Cookie header, /page-a serves minimal page); 7 session (ContextVar fresh-by-default, set+clear round-trip, constructor doesn't launch — `.started False right after construction`, `.page` raises before start, ImportError → BrowserUnavailable with install hint, live: ensure_started idempotent + creates user_data dir + close releases resources + double-close no-op); 12 capture (append jsonl row + 64-hex content_sha256, empty content → empty hash, parent dir created lazily, status=None recorded as 0, tail returns recent in order, missing file → [], malformed lines skipped, throttle sleeps within interval [~1s sleep captured], no sleep across domains, no sleep after interval elapsed, min_interval_s=0 disables, invalid URL handled without raising); 14 tools (4 always-running: gate browser_enabled=False with NO Playwright contact, no-active-session structured error, missing-url error, close-with-no-session no-op) + 10 live: navigate returns title, **persistent cookies survive nav** (the load-bearing test), extract_text returns body, extract_links finds the 3 fixtures' links, screenshot writes a PNG to disk, screenshot+analyze_prompt routes to vision_analyze describe with the right mode+prompt, fill+click form submission, navigate logs a capture row with sha256 content hash, all 9 tools registered under toolset=browser, schema enums correct); 5 lifecycle (lazy-by-construction never launches, set+clear ContextVar round-trip, tools see bound session via ContextVar, unused browser never launches — **the load-bearing tripwire patches sync_playwright to AssertionError and proves the lazy contract**, close is idempotent across triple-call). Reference: `docs/reference/browser-tools.md` covers the tools table, the persistence model with "one Chromium context per athena session" + per-session user-data dirs, lazy-launch contract with the test that pins it, capture log accountability surface, politeness throttle, the realistic-fingerprint-NOT-evasion legitimacy framing (no CAPTCHA solving, no login-wall bypass, credentials operator-provided), screenshot→vision multiplier, tool result JSON shape, config block, dependencies, and explicit non-goals (T4-03).
- `video_analyze` tool: one tool with seven modes (`probe` / `atoms` / `gop` / `encoder_fingerprint` / `inspect` / `frames` / `analyze`) implementing the two-layer container-vs-elementary-stream discipline. The `inspect` mode returns `container_layer` and `elementary_stream_layer` as SEPARATE objects with a co-emitted `note` field stating "Container observations and elementary-stream observations are reported SEPARATELY on purpose: a remux pass can touch the container (atom reorder, metadata restore, faststart) while the underlying stream is authentic. Do not collapse these into a single is-it-real verdict — surface signals; weigh them; never auto-conclude." The note travels in the model's reasoning context every time inspect runs; the "SEPARATELY" + "never auto-conclude" strings are pinned by `test_inspect_keeps_layers_separate` so a future refactor can't drop the hedge. Every interpretation string is hedged: `moov_before_mdat=true` → "consistent with a qt-faststart (web-optimised) remux pass"; `moov_before_mdat=false` → "typical of original camera capture and recorder output"; `software_encoder_likely=true` → "produced or re-encoded by a software encoder"; `software_encoder_likely=false` → "consistent with hardware encode, BUT absence is NOT proof: many camera muxers omit the encoder field entirely. Cross-check with GOP regularity and atom ordering before drawing any conclusion" (T4-02). `athena/video/atoms.py` is the pure-Python MP4/MOV top-level box parser — handles all three box-header shapes (32-bit size, size=1 → 64-bit extended size in next 8 bytes, size=0 → box extends to EOF), `max_atoms=64` cap so malformed inputs can't loop forever (a normal MP4 has 3-8 atoms), defensive on every shape (32-bit size smaller than its own header → stop without raising; non-MP4 byte stream → return cleanly; declared size off the end → stop). `parse_top_level_atoms(path) → list[Atom]`, `is_likely_isobmff(atoms) → bool` (cheap "is this a real MP4/MOV" — first atom must be ftyp/moov/skip/free), and `faststart_remux_signature(atoms) → dict` (atom_order + moov_index + mdat_index + moov_before_mdat + the hedged interpretation string). Why pure Python: the moov-vs-mdat ordering is the single most useful container-tampering signal AND it costs nothing to keep working on a host without ffmpeg. The atoms mode remains available everywhere — the rest gracefully degrades. `athena/video/probe.py` is the ffprobe-backed inspection surface: `ffprobe_json(path)` runs `-show_format -show_streams` with `FFprobeMissing` raised when the binary isn't on PATH (clear install hint naming brew/apt/scoop), timeout → `{}` (warning), non-JSON output → `{}` (warning); `codec_summary(probe)` pulls the one-row {codec_name, profile, level, pix_fmt, width, height, frame_rate, bit_rate, duration, format_name, format_tags, stream_tags} or returns `{"error": "no video stream"}` when the probe has no video track; `encoder_fingerprint(probe)` pulls format + stream-level encoder tags + handler names + the hedged interpretation (matching the documented "presence is strong evidence, absence is NOT proof" discipline); `gop_structure(path, *, limit=300)` samples the first N frames + reports I/P/B counts + keyframe-interval list + `regular_gop` bool — hardware vs software encoders leave different GOP signatures; a regular GOP with a software encoder tag tells a different story than an irregular GOP consistent with camera capture; the tool returns the raw signal, not a verdict. `athena/video/extract.py` is the three-mode ffmpeg frame extractor: `extract_keyframes(path, out_dir, *, max_frames=200)` uses `-skip_frame nokey` (cheap, and keyframes are where scene content lives); `extract_sampled(path, out_dir, *, interval_s, max_frames=200)` uses `-vf fps=1/N` (for long videos where keyframes are too sparse to summarise); `extract_range(path, out_dir, *, start, end, max_frames=200)` uses `-ss` / `-to`. All three require ffmpeg on PATH (`FFmpegMissing` raised cleanly when absent — pinned by tests patching `shutil.which → None`), cap output at `max_frames` (default 200 — long videos would otherwise explode disk), write PNG files to `out_dir` returning the sorted path list, 300s default timeout (timeout → empty list with warning log; the analyze layer above composes this alongside the inspect modes, one extraction failure doesn't break the whole call). `athena/video/analyze.py` is the model-facing entry point — sync (athena's runtime; spec was async). `_run(mode, path, extract, interval_s, start, end, prompt, _cfg, _provider_fn)` is the body factored out so tests call it with stubs without going through @tool. Every read sha256s the source video and writes a JSONL row to `<profile_dir>/video_audit.jsonl` via the T4-01.2 `HashLogger` (same shape; no parallel implementation — reuses the vision provenance log infrastructure). The `analyze` mode composes mode=frames + per-frame `vision_analyze` describe — the vision provider is an injectable `ProviderFn = Callable[[Path, str], str]` callable so tests stub it; production wires through `_default_provider_fn(cfg)` which calls T4-01's vision_run in describe mode for each frame. When `cfg.vision_enabled=False`, the analyze mode still extracts the frames + returns the path list + surfaces "vision provider not available — frames extracted but per-frame describe was skipped" rather than crashing (the two layers stay composable). Config additions in athena/config.py: `video_enabled=True`, `video_ffmpeg_path="ffmpeg"`, `video_ffprobe_path="ffprobe"`, `video_frames_dir=None` (None → `<profile>/video/frames`), `video_max_frames=200`, `video_default_extract="keyframes"` (keyframes | sampled | range), `video_sampled_interval_s=5.0`. Registered via `@tool(name="video_analyze", toolset="vision")` — same toolset as `vision_analyze` so the model sees the pair together; athena/tools/__init__.py imports the module to trigger registration on agent startup, mirroring T4-01 / T6-02-06's wiring pattern. Test fixtures synthesised by `tests/video/fixtures/__init__.py` via ffmpeg's deterministic `testsrc` filter (camera_original.mp4 with mdat-before-moov, faststart.mp4 with moov-before-mdat, x264_encoded.mp4 with the libx264 encoder tag, sample.mp4 for frame-extraction tests, generated.mp4 combining x264+faststart, short.mp4 for fast smoke tests) — gitignored, built lazily at test-collection time when ffmpeg is available; consumer tests use `pytest.mark.skipif(not have_ffmpeg(), ...)` so a CI node without ffmpeg keeps the pass green. 58 new tests across `tests/video/`: 17 atoms (parser shape — ftyp/moov/mdat happy path, 64-bit extended size, size=0 last-box marker, max_atoms cap, malformed-size-smaller-than-header stops without looping, empty file, non-MP4 doesn't raise; is_likely_isobmff acceptance/rejection; faststart signature positive/negative synthetic + missing moov/mdat/neither hedged interpretations; real-fixture moov-vs-mdat validation skip-if-ffmpeg); 14 probe (FFprobeMissing when shutil.which→None, real fixture returns streams+format, codec_summary extracts h264 + width/height + audio-only error key + empty-probe error key, encoder_fingerprint flags format-tag x264 + stream-level x264 + x265 + collects handler_names + the load-bearing "NOT proof" string when absent + real x264 fixture detected, gop missing-ffprobe raises + real fixture I-frame count >= 1 + synthetic-clip near-regular GOP); 8 extract (missing-ffmpeg raises for all three modes, sampled rejects interval_s<=0 up front, real keyframes / sampled / range produce PNG output, max_frames=1 cap honored); 19 analyze (gate video_enabled=False short-circuits with no audit row, unknown mode rejected, missing path rejected, missing file rejected, unknown extract rejected, atoms on camera_original moov-last + faststart moov-first + hedge string, atoms writes audit row, probe summary extracts codec, gop counts I-frames, encoder_fingerprint flags x264 fixture, **the load-bearing `test_inspect_keeps_layers_separate`** proving container_layer + elementary_stream_layer both present + populated + the "SEPARATELY" / "never auto-conclude" strings pinned in the note, inspect's atom-layer interpretation matches "camera capture" on camera_original, frames mode keyframes + sampled, analyze mode routes every extracted frame through the injected provider_fn AND records seen_paths == frame_count, analyze with vision_enabled=False returns frames + clear error rather than crashing, tool registered under vision toolset with all 7 modes in the schema enum). Reference: `docs/reference/video-analyze.md` covers the modes table with per-mode ffmpeg/ffprobe requirements, the two-layer discipline with sample inspect output showing how container and stream layers can disagree, the signals-vs-verdicts hedging table, frame-extraction strategies, the analyze multiplier (frames + per-frame describe), the hash-log shape, config block, dependencies, graceful-degradation matrix per host capability, and explicit non-goals (no audio inspection, no DRM streams, no live streams, no verdicts) (T4-02).
- `vision_analyze` tool: one tool with seven modes — `describe` (provider passthrough; caption / answer about the image; tiles oversized inputs rather than downsampling them), `exif` (read camera Make/Model/DateTime/Software/Lens/GPS tags), `ela` (Error Level Analysis for spliced-region detection — bright patches = different compression from neighbours), `crop` (write sub-region to disk as PNG), `histogram` (per-channel intensity for exposure/clipping/cast reads), `phash` (perceptual hash; phash/dhash/ahash/whash), `compare` (two-image metadata-strip + pHash distance). Every read SHA-256s the file and writes a JSONL row to `<profile_dir>/vision_audit.jsonl` — provenance trail with mode/path/sha256/bytes/ts (T4-01). `athena/vision/hashlog.py` exposes `sha256_file(path, *, chunk_size=1MB)` (streamed so 4K screenshots don't pull into RAM) and `HashLogger(path)` with `.log(mode, path, sha256, size_bytes, extra?)` returning a `HashLogEntry` plus `.tail(limit)` that defensively skips malformed lines. Matches `athena/computer/audit.py` shape — same open-append pattern (allowlisted in `tests/safety/test_no_raw_writes.py` with the same rationale: operational metadata, append-only, not agent-driven content mutation). `athena/vision/imageops.py` ships the pure-Pillow/imagehash primitives: `extract_exif` (friendly keys when known, numeric tag fallback, JSON-safe values via `_normalise_exif_value` coercing bytes/Fraction/tuple/dict), `error_level_analysis(quality=80, threshold=15)` (16×16 grid patches with `max_diff` + `mean_diff` per tile and per-frame; returns SIGNAL not VERDICT — docstring + reference doc make the heuristic limits explicit: flat regions, double-compressed forgeries, and quality-matched edits all won't show), `crop_region` (atomic write to out_dir; clamped box rejection — Pillow's silent zero-pad on out-of-bounds is exactly the kind of "model reasons about the wrong frame" failure we want loud), `histogram` (bin count must divide 256), `perceptual_hash(algorithm="phash", hash_size=8)`, `phash_distance(h1, h2)`, and `metadata_strip_check(orig, suspect)` (verdict ∈ stripped/intact/different/indeterminate — `indeterminate` when the original itself has no EXIF, since "suspect missing keys" can't be diagnosed without an original baseline). `MAX_INPUT_PIXELS=80_000_000` bomb guard refuses oversize input before Pillow decodes; pinned by a test that monkeypatches the cap to 100 px. `athena/vision/passthrough.py` produces provider-shaped content blocks: Anthropic uses `{"type":"image","source":{"type":"base64","media_type":...,"data":...}}`, OpenAI/openai-compat uses `{"type":"image_url","image_url":{"url":"data:image/...;base64,...","detail":"<tile-label>"|"high"}}`, Ollama returns a sentinel `{"type":"image","media_type":...,"data":...,"label":...}` dict the T4-01.7 Ollama provider will unwrap onto the message's top-level `images=` list. Policy is TILE not DOWNSAMPLE — a downsample irreversibly destroys forensic detail; tiling preserves every pixel as N labelled pieces. Per-provider long-edge caps from each vendor's input recommendation: Anthropic 1568 px, OpenAI 2048 px, Ollama 1344 px. `_tile_2d` recurses (2×2 / 2×1 / 1×2 splits) until every tile's long edge ≤ cap; each tile carries a distinct `tile_R_C` label (stored in OpenAI's `image_url.detail` field, on a sibling `__tile_label__` key for Anthropic, and as `"label"` on the Ollama sentinel) so multi-turn discussion has a stable handle. Single-block passes preserve the input format when supported (JPEG-in → JPEG-out via `prefer_original_format=True`); tiled passes always emit PNG since a crop's original-format identity is meaningless. `athena/vision/analyze.py:_run(mode, path, paths, prompt, box, quality, threshold, bins, algorithm, hash_size, return_b64, _cfg, _provider_factory)` is the body of the @tool registration — sync (the spec was async; athena's runtime is sync throughout). Errors surface as `{"error": "...", "mode": "..."}` JSON rather than raising into the model loop; the gate (`cfg.vision_enabled=False`) short-circuits with no audit row written. `describe` mode dispatches via an injectable `provider_factory` callable so tests stub a `_StubProvider` with a `.describe(messages) -> str` method without touching `MediaRegistry`. Production callers use `_default_provider_factory` which routes via `MediaRegistry.backend_for("vision")` and wraps the resolved provider in `_StreamingDescribeAdapter` (consumes the provider's `stream_chat` iterator, concatenates content chunks, returns the finished text). `_passthrough_provider_for(cfg)` picks the right block shape from `cfg.provider`: "anthropic"/"claude" → Anthropic shape, "openai"/"gpt" → OpenAI shape, otherwise Ollama (athena's default deployment). Registered as `@tool(name="vision_analyze", toolset="vision")`; the schema describes all 7 modes in the `mode` enum + per-mode arg semantics in the description so the model knows e.g. `crop` requires `box=[x0,y0,x1,y1]`. `athena/tools/__init__.py` imports the module to trigger registration on agent startup, mirroring how T6-02/T6-03/T6-04/T6-05/T6-06 wire their tools. Config additions (every default safe): `vision_enabled=True`, `vision_max_input_pixels=80_000_000`, `vision_ela_quality=80`, `vision_ela_threshold=15`, `vision_phash_algorithm="phash"`, `vision_phash_size=8`, `vision_long_edge_cap=None` (None → per-provider default from `LONG_EDGE_CAP`), `vision_crop_dir=None` (None → `<profile>/vision/crops`). New optional-dependency `vision = ["Pillow>=10", "imagehash>=4.3"]` — Pillow already in athena's runtime, imagehash is the only new dep. piexif is documented as an optional EXIF fallback for legacy formats; `exiftool` is documented as the external binary to install when GPS sub-IFD / MakerNote visibility matters (both kept as documented gaps rather than hard deps). Test fixtures (`tests/vision/fixtures/__init__.py`) are SYNTHESIZED at pytest-collection time via Pillow + ImageDraw with seeded RNG — six deterministic PNG/JPEG files (original.jpg with EXIF Make=AthenaCam Model=T4-01 DateTime, stripped.jpg same pixels no EXIF, edited.jpg with bright spliced patch at (200,200), recompressed.jpg at q=70, large.png 2400×1200, small.png 320×240); not checked in (.gitignore) so they're reproducible across Windows/Linux without binary churn on Pillow upgrades. EXIF block uses `Image.Exif()` not raw bytes — Pillow's JPEG encoder silently strips raw byte blobs (first pass shipped that mistake and lost the Make/Model/DateTime keys on round-trip; pinned). 70 new tests: 6 fixture sanity (all 6 exist, original carries EXIF triple, stripped carries none, edited's patch region differs from original at (230,230), large ≥2000px, small <500px, recompressed.jpg smaller than original.jpg); 10 hash-log (sha256 matches hand-computed digest, multi-chunk streaming, FileNotFoundError on missing, one-row append, optional `extra` round-trip, ordered tail, lazy parent-dir creation, missing-file tail returns [], malformed-line skip in tail); 22 imageops (EXIF Make/Model/DateTime extraction, EXIF empty on stripped, EXIF values JSON-safe, MAX_INPUT_PIXELS rejection before decode, ELA shape + direction-of-signal `edited >= original` + quality + threshold validation, crop happy path + b64 opt-in + OOB rejection + inverted-box rejection, histogram shape + per-channel total = total_pixels + bin-divisor check, pHash hex length 16 + robust-to-recompression `≤4` + distinguishes different scenes `>8` + alt algorithms + unknown-algo rejection, strip-check stripped/intact/indeterminate verdicts); 10 passthrough (per-provider single-block shape Anthropic/OpenAI/Ollama with decodable b64 round-trip via Pillow, large fixture tiles for Ollama+Anthropic, every tile's long edge ≤ cap, original.jpg under OpenAI's 2048 cap → no tile, distinct `tile_R_C` labels across tiles, unknown provider rejected, custom cap forces tile path on small fixture); 20 analyze (exif mode extracts camera fields + writes audit row, ela mode returns patches + honours overrides, crop mode writes artifact + missing-box error, histogram mode returns bins, phash mode hex length 16, compare mode detects stripped + missing-keys + requires-two-paths + strong-match label on recompressed pair, describe mode passes text+image to provider + tiles large input + clean error on no-provider + provider-exception surfaces as error, vision_enabled=False short-circuits with no audit row, unknown mode rejected, missing file rejected, tool registered under vision toolset with all 7 modes in schema enum). Safety allowlist addition in `tests/safety/test_no_raw_writes.py` for `athena/vision/hashlog.py` (the JSONL append for the provenance trail — same justification as `computer/audit.py`). Reference: `docs/reference/vision-analyze.md` covers the modes table, the hash-log shape, the describe-mode passthrough + per-provider tile caps, the EXIF Pillow-vs-piexif/exiftool gap, the ELA-is-signal-not-verdict caveats, the pHash distance reading table, the compare-mode verdict semantics including the indeterminate case, the config block, and explicit non-goals (no OCR backend, no C2PA verification, no GPS lat/lon decode, no verdicts) (T4-01).
- Self-update: `athena update` detects the install method (pip/pipx/git/editable), checks the latest release, previews the changelog, verifies integrity, and installs — with `--check` (report only), `--to <version>` (pin), `--rollback` (restore prior); never hot-swaps the running process (advises restart); offline-safe; optional startup auto-check notifies only (T6-07). `athena/update/detect.py:detect()` layers four signals to pick the right :class:`InstallMethod` (PIP / PIPX / GIT / EDITABLE / UNKNOWN): direct_url.json `dir_info.editable=True` (EDITABLE wins over GIT — never `git pull` behind a developer's working tree), git-repo ancestor walk (`.git` or bare-repo HEAD+refs/ shape), `pipx`-in-path heuristic (case-insensitive for Windows `%LOCALAPPDATA%\pipx`), then importlib.metadata lookup. Every helper is defensive — malformed direct_url.json / unreadable distribution / OSError walks all fall through to UNKNOWN rather than crashing. `athena/update/check.py:latest_pypi_version(pkg, *, channel, timeout)` queries the PyPI JSON API with `channel="stable"` filtering out `a1`/`b1`/`rc1`/`dev` shapes via `_is_prerelease`; OSError / URLError / JSONDecodeError / missing `"releases"` key all return `None` for the "offline" case the command surfaces cleanly. `latest_git_tag()` runs `git ls-remote --tags origin`, parses both peeled (`^{}`) and unpeeled refs, accepts `vX.Y[.Z]` and `X.Y[.Z]` shapes (the v-prefix is stripped from the returned version so comparison with `athena.__version__` works directly). `is_newer(current, latest)` uses `packaging.version.Version` when available (pip's dep, almost always present); falls back to a tuple-of-ints split where non-numeric chunks sort LAST so a corrupt PyPI payload doesn't surface as "latest". `latest_for(method, *, cfg)` is the method-aware dispatch (PIP/PIPX → PyPI, GIT → ls-remote, EDITABLE/UNKNOWN → None). `changelog_between(current, latest, *, method, changelog_path, repo_root)` previews what's changing: for PIP/PIPX it reads the packaged CHANGELOG.md (default location resolved relative to the package), finds version headings via `_find_version_headings` (recognises `## [1.2.3]`, `## [Unreleased]`, `## 1.2.3`, `## v1.2.3 - 2026-01-01` shapes), slices between latest's heading and current's heading exclusive; for GIT it runs `git log v<current>..v<latest> --oneline` (tries v-prefix first, then bare); always returns a non-empty string with a pointer fallback when neither path works. `athena/update/apply.py:install(method, *, version, repo_root, pkg, cfg)` returns an `ApplyResult` (status ∈ {done, refused, error}; method; version_installed; prior_version; stdout/stderr; message — same shape as T6-03's `DelegateResult` / T6-05's `GenerationResult`); composes the InstallMethod with the version into the matching subprocess invocation — PIP uses `sys.executable -m pip install --upgrade pkg[==V]` (pip's hash verification fires automatically), PIPX uses `pipx upgrade pkg` (or `pipx install pkg==V --force` for pin/rollback since `pipx upgrade` can't pin), GIT does `git fetch --tags` + `git checkout v<version>` (falls back to bare `<version>`) + `pip install .` (or `git merge --ff-only @{u}` when unpinned), EDITABLE refuses cleanly with a clear "update via your source checkout" message, UNKNOWN refuses with a help message naming all three supported paths. **THE LOAD-BEARING NO-HOT-SWAP INVARIANT** pinned by a tripwire test: install path never calls `os.execv` / `os.execvp` / `os._exit` / `sys.exit`; success message ends with "restart athena to use it"; the running session finishes on the old code, next launch is the new version. `record_prior(version, *, cfg)` writes the current version to `update_state.json` BEFORE upgrade (best-effort — a write failure logs + returns False, the install still proceeds; worst case is "rollback has nothing to restore"); `read_prior(*, cfg)` reads it back. `rollback(*, cfg)` detects the current method + installs the recorded prior version via the matching path. Every subprocess shells through `_run(argv, *, timeout=300)` which maps FileNotFoundError → exit 127 and TimeoutExpired → exit 124, capturing stdout + stderr so failures surface uniformly. `athena/commands/update.py` is the model-callable CLI: three mutually-exclusive flags (--check / --to <V> / --rollback), --yes to skip the confirm prompt; the default flow detects method → looks up latest → renders changelog → confirms → records prior → installs → "restart athena to use it"; --check reports without installing (pinned by test: `install` is NEVER called on the --check path); offline (`latest=None`) → warn + exit 0 with no changes (the install function is NEVER called — the load-bearing "no network never crashes" invariant); EDITABLE / UNKNOWN refuse with helpful messages (no subprocess fires). `startup_notice(cfg)` runs from athena's `__main__` at process start (after the legacy migration but before subcommand dispatch / REPL): when `cfg.update_auto_check=True` AND a newer version exists, prints a one-line "athena X.Y.Z is available; run `athena update` to install." notice. NEVER auto-installs (pinned by test that spies on the install function during startup_notice). 3-second timeout on the PyPI lookup so a slow network never blocks startup. Silent on every failure mode (offline / exception / EDITABLE / UNKNOWN / `update_auto_check=False`) — the notice is a courtesy, not a feature gate. Off by default. Config additions: `update_source` ("auto" / "pypi" / "git"; detection override), `update_channel` ("stable" / "pre"), `update_auto_check` (False — opt-in startup notice), `update_state_path` (None → `<CONFIG_DIR>/update_state.json`). 78 new tests: 14 install-method detection (every method's signal pinned with monkeypatched metadata + filesystem; editable beats git ancestor; bare-repo HEAD+refs/ shape recognised; walk-up finds repo; pipx case-insensitive; UNKNOWN when metadata missing; defensive — malformed direct_url.json falls through, missing direct_url is PIP); 40 version check + changelog (is_newer for upgrade/equal/downgrade/None/PEP 440 pre-releases; _is_prerelease parametrized across 7 pre + 4 stable shapes; PyPI returns highest stable, channel=pre includes pre-releases, channel=stable excludes them; offline / URLError / malformed JSON / missing releases all → None; latest_for dispatch + cfg.update_channel propagates; ls-remote highest tag, no-v-prefix works, git-missing → None, non-zero → None; changelog: bracketed + bare heading shapes parsed; PIP slice from latest to current exclusive; missing file → pointer fallback; GIT uses git log --oneline; git-failure → pointer fallback); 24 apply (install routes argv through `python -m pip` for integrity; PIPX uses upgrade unpinned + force pinned; PIPX missing PATH → error; GIT three-step argv; falls back v-prefix → bare; unpinned uses ff-merge; git-missing → error; fetch failure → error short-circuit; EDITABLE refuses with NO subprocess fired; UNKNOWN refuses with help; record_prior + read_prior round-trip; corrupt state-file → None; record_prior + rollback installs prior via PIP; rollback with no prior → refused; --to pins exactly that version; **NO-HOT-SWAP TRIPWIRE**: os.execv/os.execvp/os._exit monkeypatched to assert-raise — install completes without calling any; failure surfacing carries stderr; timeout exit 124 surfaces as error); 20 command (--check installs NOTHING — `install` NEVER called; --check up-to-date / offline; default install with --yes records prior + installs; --to skips the latest-lookup; EDITABLE / UNKNOWN refused with exit 1; --rollback with no prior → refused; rollback installs prior; mutually-exclusive flags via argparse; startup_notice: off by default → zero lookups; on + newer version → one-line notice + install NEVER called; on + EDITABLE skips lookup; on + offline → silent; on + exception → silent — courtesy not gate). Safety allowlist addition in tests/safety/test_no_raw_writes.py for `athena/update/apply.py` (update_state.json — machine-managed prior-version metadata for --rollback). Reference: `docs/reference/self-update.md` covers method detection with the order-of-precedence table, the full install flow diagram with explicit "never hot-swaps" callout, integrity verification (pip's automatic hash check; the git-signed-tag gap), changelog preview heading-shape table, `--to`/`--rollback` semantics + the rollback-target-record-on-failed-upgrade behaviour, offline-safety behaviour table, the auto-check notify-only contract with its silence conditions, explicit non-goals (no hot-swap, no auto-install, no install-over-editable, no automatic downgrade, no signed-tag verification today), and a smoke script (T6-07).
- Auto kanban: the task tracker is persisted and rendered as a board (`athena board` TUI + `board_show` tool; todo/doing/blocked/done); the agent maintains it automatically as it works; goal-loop subgoals (T5-07) project into the same backing store as cards (one model, not two); existing task tool API unchanged; persists across restart with scheduled archiving (T6-06). `athena/tasks/model.py:TaskStore` is the SINGLE backing store. Task dataclass carries id (`t-<uuid12>`) + title + status (canonical `todo|doing|blocked|done`) + order (explicit per-column for stable renders + future drag-reorder) + parent_id (subtasks; reserved) + goal_id (set when projected from a /subgoal — same store, different filter) + session_id + workspace + note + created_at/updated_at. TaskStore surface: create/get/update/delete (idempotent; returns bool)/list (filters by workspace, goal_id, status; sorted by (status_column, order) so the board projection is a slice)/archive_done(older_than_days)/clear. status-change updates re-order to END of new column with the new_order computed BEFORE writing the new status (so the lookup doesn't count the task itself in its destination column — bug-fixed during T6-06.1). JSON persistence at task_store_path (default `<profile_dir>/tasks/tasks.json`) via secure_write_json (atomic-replace + fsync, mode 0o600); corrupt JSON file → start empty + next save overwrites. archive_done(0|-1) is a no-op (prevents accidentally archiving everything via threshold 0). threading.Lock on every mutate (single-threaded today, belt-and-braces for forks + the TUI's refresh thread). `athena/tools/task.py` (TaskCreate / TaskUpdate / TaskList) now routes through the persisted store while keeping the agent-facing API byte-identical: parameters unchanged (subject, description, activeForm, taskId, status), return-string formats unchanged ("Task <id> created: …" / "Task <id> updated: status=…" / "(no tasks)"), status enum unchanged (pending|in_progress|completed|deleted). Internal _EXT_TO_INT / _INT_TO_EXT mapping at the boundary translates the legacy vocabulary to the canonical store names. The one visible change: task id format moves from incrementing ints to "t-<uuid12>" (required for cross-restart stability — incrementing ints collide across processes). Note round-trip preserves description + activeForm via store.note encoded as "description\nactiveForm: …"; TaskUpdate(description=X) sets description to X (existing contract preserved). _resolve_store / _resolve_workspace lazily resolved + cached per process; workspace pulled from file_ops at call time so a /cwd change moves new tasks to the new workspace's board. `athena/tasks/board.py:project_board(store, *, workspace, goal_id)` is pure — slices the store into ALL FOUR columns (todo/doing/blocked/done present even when empty so renderers don't handle missing keys); card shape carries id/title/goal_id/parent_id/order/note/created_at/updated_at; sort within each column comes free from store.list(). `column_counts(cols)` → `{col: N}` for status renders. `board_show` @tool (toolset="tasks") returns JSON `{workspace, goal_id, counts, columns}`; reads the SAME TaskStore the TaskCreate/Update/List tools share (the single-store invariant at the tool surface — pinned by test that asserts TaskCreate places a card in todo AND board_show sees it with the matching id). Optional goal_id parameter filters to one goal's subgoal-cards. `athena/commands/board.py` ships `athena board [--goal G] [--profile P] [--static]` CLI + `/board [goal:<id>]` slash; static text render is the default (color-coded columns + counts + 🎯 marker for goal-linked cards + first note line under titles); when stdout is a TTY AND textual is installed, an interactive TUI lights up (Header / 4-column Horizontal layout / Footer, BINDINGS for q/quit + r/refresh); textual gracefully optional via `athena[board]` extra. Goal-loop projection (T6-06.4): GoalState gains a stable `goal_id` field minted at /goal `<text>` time as "g-<uuid12>"; Subgoal gains a `task_id` back-pointer for round-tripping. `/subgoal <text>` now does state.subgoals.append(Subgoal) AND store.create(goal_id=state.goal_id, workspace=resolved_ws, status="todo") — every subgoal write goes through the same store so the board and the goal's view of subgoals can never disagree. `/subgoal done` flips state.subgoals[i].done=True AND store.update(matching_task_id, status="done"). `/goal <new>` clears the prior goal's subgoal-cards from the store BEFORE writing the new state (no stale cards accumulate across goals). `/goal clear` removes every store card for the cleared goal_id. All projection writes are best-effort try/except — a store failure logs at debug + returns gracefully (the in-state subgoal still works, the board just won't show that one). System-prompt nudge: `cfg.board_auto_maintain=True` (default) appends a "# Task board (auto-maintained)" section reminding the agent to TaskCreate when planning + flip in_progress on start + completed on finish; threaded through `build_system_prompt(board_auto_maintain=...)` from `Agent._build_system`. Config additions: `task_persist=True`, `task_store_path=None` → `<profile_dir>/tasks/tasks.json`, `board_auto_maintain=True`, `task_archive_done_after_days=30.0`. 48 new tests across tests/tasks/: 17 model (create persists across reload, create rejects empty title + bad status, update status moves to END of new column with the order-computed-before-status bug-fix pin, update unknown id raises KeyError, update partial fields, delete idempotent, list filters by workspace/goal_id/status independently and combined, sort order matches column display contract, **SINGLE-STORE invariant pinned**: regular tasks + goal subgoals coexist with goal_id the only discriminator, archive_done 30d threshold archives older done tasks only, archive_done(0)/(-1) no-ops, clear() removes all live tasks, corrupt JSON starts empty + next save overwrites, default_task_store_path resolution); 11 task-tool API parity (legacy return-shape preserved across create/list/update/delete/unknown-task/invalid-status/no-changes/subject+description rename, persistence across `_reset_for_tests` simulating restart, workspace scoping for multi-project boards); 13 board (every column present + correct assignment + correct sort within column + card shape; filters; column_counts; **SINGLE-STORE invariant at TOOL surface**: TaskCreate puts a card in todo, board_show sees it with matching id; goal_id filter isolates a single goal; TaskUpdate(status=in_progress) → board_show shows card in doing on next read; empty store → all 0 counts); 7 goal projection (/subgoal creates task with goal_id set + task_id back-pointer; /subgoal done flips matching store task to done; SINGLE-STORE invariant at integration level — state.subgoals titles + done flags match store.list(goal_id=); /goal `<new>` clears prior goal's subgoal-cards; /goal clear removes all store cards for that goal_id; workspace propagates to subgoal-cards; board_auto_maintain=True nudge appears in system prompt and False omits it). Reference: `docs/reference/board.md` covers the status mapping at the tool boundary, the store + persistence shape, the single-store invariant ("the board and the goal's view of subgoals can never disagree" — they're the same data), the TUI optional-extra installation, archiving semantics, workspace + profile scoping, and explicit non-goals (no external sync, no auto-reorder, no enforced subgoal-completion order) (T6-06).
- Native video generation: `video_generate` (text→video) + `animate_image` (image→video), backed by the media broker (T5-05) so the backend is pluggable + capability-resolved (local-preferred); async submit→poll→fetch with a cost/latency guard confirming long/expensive jobs; outputs hash-logged with an optional vision frame-check (T6-05). `Capabilities` gains a new opt-in `video_generation: bool = False` field — same conservative-default + manifest-routing pattern as T6-02's `social_search`. `athena/videogen/job.py` defines `GenerationRequest` (mode + prompt + image_path + motion_prompt + duration_s + aspect + seed), `CostEstimate(seconds_est, cost_est)` with `.needs_confirm(cfg)` using OR semantics (EITHER threshold trips; cost_est=None skips the cost dim; threshold=0 disables that dimension), `JobHandle` (backend-opaque ID + status/progress/error/extra), `GenerationResult` (status ∈ {done, declined, timeout, error, cancelled} + path/sha/duration/seconds_taken/cost_est/estimate/backend/frame_check/error + .to_dict), and the `VideoGenerationBackend` Protocol (estimate/submit/poll/fetch). Adapted to sync — athena's runtime is sync throughout; the spec's async signatures are simulated via an injectable `sleep` so tests run instantly and production uses `time.sleep`. `run_generation(request, *, backend, cfg, confirm, vision_check, sleep)` is the orchestrator: estimate (exception → error, never submits) → if `needs_confirm` → confirm callback (False / exception / no callback → `status=declined`, **submit NEVER fires** — the "never silently spend" load-bearing invariant pinned by tests) → submit (exception → error, never polls) → poll loop until done|failed|cancelled or 3600s timeout (sleep between polls is injectable in tests) → fetch (exception → error) → SHA-256 + append a `media_log.jsonl` row in the output dir carrying ts/kind/mode/backend/path/sha256/duration_s/seconds_taken/cost_est/prompt → optional `vision_check(path)` (T4-01 hook; exception is debug-logged and `frame_check` stays None — the check is advisory and never breaks generation). `run_generation` never raises into the agent loop; every failure maps to a status. `resolve_backend(cfg)` uses `MediaRegistry.backend_for("video_generation")` with `cfg.video_backend_prefer` (default `"local"`) overriding `media_backend_prefer` for this capability lookup; returns None when no provider declares the capability OR the chosen class can't be instantiated (hosted without credentials → falls through cleanly, caller surfaces `status=not_configured`) OR the resolved class doesn't satisfy the Protocol (missing one of estimate/submit/poll/fetch → warning + None rather than handing back an instance that crashes on first use). `athena/videogen/backends/stub_local.py:StubLocalVideoBackend` ships as the in-tree default — declares `Capabilities(video_generation=True, is_local=True, tool_calls=False, streaming=False)`, registered via `@register_provider` so `best_provider_for({"video_generation"})` finds it out-of-the-box; chat methods raise NotImplementedError (capability-only — same pattern as T6-02's social provider; added to `_NON_CHAT_PROVIDERS` in the parity test); the Protocol implementation writes a placeholder file (`ATHENA-STUB-VIDEO` header + request payload bytes for hash-distinguishability between runs) so the full pipeline can be exercised end-to-end in CI without a hosted vendor. Real vendor adapters (`runwayml.py`, `pika.py`, `ollama_video.py` etc.) land alongside at build time — each owns its vendor specifics in one file. `athena/videogen/tools.py` registers `video_generate(prompt, duration_s=5, aspect="16:9", seed?)` and `animate_image(image_path, motion_prompt, duration_s=4)`; both check `cfg.video_generation_enabled` FIRST (disabled → structured `not_enabled` payload, NO backend resolution attempted — opt-in invariant pinned by spy test); both return identical JSON shape across every status branch so a downstream consumer doesn't need to branch on which tool fired. Config additions (every default SAFE): `video_generation_enabled=False`, `video_backend_prefer="local"`, `video_confirm_over_seconds=60.0`, `video_confirm_over_cost=1.0`, `video_output_dir=None` → `<profile_dir>/videos`, `video_poll_interval_s=5.0`. 36 new tests: 6 cost-guard (every threshold combination + OR semantics + cost-unknown skips + zero disables + boundary at-threshold doesn't trip + partial thresholds independently); 13 job orchestration (polls-until-done with status sequence, hash-logged, declined-when-confirm-false NEVER submits, approved-submits-and-completes, under-threshold callback NEVER asked, default-deny when no callback, confirm-raises = denial, estimate/submit/fetch failures = error, backend reports failed = error, frame_check optional + exception-safe); 9 backend resolution (capability defaults, stub declares correctly, broker finds stub_video_local, empty registry → None, local-preferred wins over hosted, missing Protocol methods → None, stub end-to-end Protocol cycle, stub chat methods raise); 8 tool handlers (refuses when disabled with NO backend contact for both tools, requires prompt, animate_image requires source file existence + motion_prompt, no-backend payload, full happy path writes file + media log for both tools, long-job declined by default no file written). Allowlist addition in tests/safety/test_no_raw_writes.py for `athena/videogen/job.py` (writes the fetched video + media_log.jsonl — generated artifacts with provenance, not agent-driven mutations of user content). Reference: `docs/reference/video-generation.md` covers the cost-guard semantics, the broker resolution + local-preference, the output + media log format, the optional frame check, the JSON tool-result shape across every status branch, and how to write a new vendor adapter (T6-05).
- Computer use (desktop control): pluggable desktop backend (screenshot + mouse/keyboard); observe-only co-pilot mode plus gated control; three-tier permission model (observe / input / destructive) with conservative classification and destructive-always-confirms; app allow/denylist (denylist wins); global kill switch; action audit log with screenshot hashes; screenshots routed through the vision capability (local-capable); off by default, opt-in per machine (T6-04). The safety boundary is the **entire** feature — computer use is the inverse of T5-02's sandbox; the permission gate + kill switch are all there is. Build order intentionally safety-first: permission model and kill switch land BEFORE any input backend can touch the OS. `athena/computer/contract.py` defines `Action` (type + coords + text + key + target_desc + app + .is_input + .describe()) and `Screenshot` (png_bytes + width + height + scale for retina/DPI mapping) and the `DesktopBackend` Protocol (screenshot / active_app / accessibility_tree / perform). `athena/computer/permission.py:classify(action)` maps every proposed action to observe/input/destructive — destructive triggers on (a) target_desc / typed text matching the destructive-verb regex (delete|remove|send|submit|pay|buy|purchase|order|confirm|overwrite|replace|discard|erase|format|wipe|sudo|trash|reset|restart|shutdown|reboot|uninstall|drop|destroy|sign out|log out|don't save|close without saving|...), (b) sensitive key chord (Alt+F4, Cmd+W, Ctrl+Alt+Delete, Delete, F5), or (c) click-style verb with NO readable target_desc — the conservative "we don't know what this button does, assume the worst" default. `PermissionGate(cfg, confirm)` is the single decision point: observe always passes; denylisted app always blocks (no prompt); empty allowlist or app-not-in-allowlist blocks; observe_only mode blocks every input; destructive ALWAYS asks confirm (every mode, every session, no exception — the load-bearing invariant); per_action prompts each input; per_session decision (yes OR no) persists tristate to prevent nagging; unknown mode + raising confirm callback both → False (no open-fail). `athena/computer/killswitch.py` lands BEFORE any input backend: thread-safe module-level engagement, polled at the TOP of every loop iteration. Two paths — SIGINT handler installed by `arm()` engages on Ctrl+C then re-raises KeyboardInterrupt so existing turn-cancel paths still fire; optional pynput-backed global hotkey (default `ctrl+alt+k`) degrades silently when pynput isn't installed. `disengage()` on loop exit restores the prior SIGINT handler + stops the hotkey listener; `arm()` resets stale engagement so a previous session's halt doesn't immediately stop the next one. `athena/computer/detect.py` auto-selects by platform with lazy imports + a no-op fallback that always succeeds + reports unavailable. `athena/computer/backends/windows.py` is the first concrete backend — stdlib only (ctypes + GDI for screenshot via BitBlt, GetForegroundWindow for active app, SetCursorPos + SendInput for click/type/key/scroll); drag stays unimplemented (composed from primitives, drag-and-drop safety is its own design); type uses KEYEVENTF_UNICODE per-char so arbitrary text works without a per-layout VK table; key chord parser handles `ctrl+c` / `alt+f4` / `shift+f5` with modifier hold + LIFO release. `athena/computer/audit.py:ActionAuditLog` is per-profile JSONL with thread-safe append + tail; the screenshot SHA-256 lands in each row but the pixel bytes NEVER do (test pins a sentinel string in pixels and asserts it doesn't appear in the file text); each row carries ts/type/target_desc/coords/app/tier/confirmed/executed/screenshot_sha256/result. `athena/computer/tools.py` registers four observe + four input tools — every one checks cfg.computer_use_enabled first (disabled → structured "not enabled" payload, the backend's is_available NEVER fires when disabled — pinned by tripwire test). Observe tools: `computer_screenshot` returns base64 image + dims; `computer_observe(question)` captures + routes via MediaRegistry.backend_for("vision") for local-preferred + offline-capable description. Input tools (`computer_click` / `computer_type` / `computer_key` / `computer_scroll`) route through `_single_action_path` which is shared with the loop — classify → gate → audit → ONLY-then perform. `athena/computer/loop.py:computer_do(task, *, backend, gate, propose, audit, cfg, dry_run=False)` is the orchestrator and **the single call site of backend.perform in the entire codebase**. Per-iteration order: poll kill switch (top) → check max_actions cap → screenshot → no-op detection (4 unchanged → stuck) → vision proposes → done signal stops → classify → gate.check → audit.log → ONLY-if-allowed-AND-not-dry-run → backend.perform → killswitch-aware rate-limit sleep. Vision proposer is injected (`ProposeFn`) so loop.py has zero vision-provider imports. `dry_run=True` runs the whole loop producing the action plan in the audit log but NEVER performs — for safely testing a task. `map_coords(coords, *, screenshot)` clamps out-of-bounds proposals to screenshot bounds with a warning log (a mis-mapped click is a safety issue). Cleanup `finally` resets the per_session decision so the next task re-confirms + disengages the kill switch. `athena/commands/computer.py` ships `/computer [status]` slash + `athena computer status [--tail N]` CLI surfacing the enabled flag + mode + allow/denylist + kill hotkey + caps + active backend + every known backend + audit-log path + colour-coded tail. Config block — every default SAFE: `computer_use_enabled=False`, `computer_permission_mode="observe_only"`, `computer_app_allowlist=[]` (empty → no app may be controlled), `computer_app_denylist` defaults to the sensible password-manager + finance set (`1password`, `bitwarden`, `lastpass`, `keychain`, `keepass`, `banking`, `wallet`, `ledger live`, `metamask`), `computer_kill_hotkey="ctrl+alt+k"`, `computer_max_actions_per_task=40`, `computer_max_actions_per_sec=2.0`, `computer_backend="auto"`, `computer_dry_run=False`, `computer_audit_path=None` → `<profile_dir>/computer_audit.jsonl`. 141 new tests (2 platform-skipped on non-Windows): 28 classification (every destructive-verb shape parametrized + sensitive keys + unreadable-click-defaults-destructive + routine input verbs); 24 permission (observe never asks; denylist wins no prompt; denylist beats destructive confirm; empty allowlist refuses; observe_only blocks all input; destructive always confirms per_action AND per_session; per_session decision persists; reset_session re-prompts; unknown mode refuses; confirm-callback-raises → denial); 15 kill-switch (initial disengaged; engage idempotent first-reason wins; disengage clears; loop polls and halts mid-iteration; Ctrl+C engages via SIGINT + re-raises; arm idempotent; arm clears stale; hotkey parse round-trip; hotkey silent when pynput absent); 15 backend (detector auto + noop + unknown-fallback; NoOp contract; Windows screenshot returns valid BMP + non-zero geometry + scale; active_app returns str|None; supports() includes input verbs post-T6-04.5 except drag; drag raises NotImplementedError); 15 audit (hash stable + sensitive; pixel bytes NEVER in log — sentinel-string assertion; tail empty/skip-malformed; default_audit_path resolution); 7 observe-tools (refuses when disabled with NO backend contact — tripwire counter == 0; observe routes through MediaRegistry; vision-absent surfaces clean reason; payload NEVER in audit log); 15 observe-act loop (done stops; killswitch halts mid-loop; max_actions cap; **gate denial → zero performs**; observe_only blocks input entirely; no-op detection; propose+perform exceptions become error states; dry_run logs but NEVER performs; no-action proposal → stuck; coordinate clamping); 11 hardened-defaults end-to-end (fresh Config disables every tool with zero backend contact; default mode is observe_only; default allowlist is empty; default denylist includes credentials/finance apps; observe_only allows screenshot but blocks every input; denylisted app refused no prompt; per_action destructive always prompts; kill switch halts end-to-end; dry_run never performs even with all permissions; single-action tools refuse without a confirm UI). Allowlist addition in tests/safety/test_no_raw_writes.py for `athena/computer/audit.py` (append-only operational audit log, not user content). Reference: `docs/reference/computer-use.md` states plainly that computer use has NO isolation (inverse of the sandbox) and that the permission model is the only boundary, with the safe-defaults table, every tier rule, the kill-switch contract, the dry-run flag, what the feature does NOT do, and an honest "how safe is this, really" section (T6-04).
- External coding-CLI delegation: `delegate_to_cli` hands a scoped task to a configured external agentic CLI in an isolated git worktree, captures the diff for review, never auto-merges; delegated code execution runs under the sandbox (T5-02); timeout-bounded; target specifics isolated to one adapter; OpenAI-compat reverse-proxy option (via T3-01) documented (T6-03). `athena/delegate/adapter.py:DelegateAdapter` is the single point where vendor-specific CLI invocation lives — `cli_delegate_command` is a `{task}`-templated string tokenised via shlex BEFORE substitution (so a task containing spaces / quotes / shell metacharacters stays one argv element); two paths picked at construction: direct `subprocess.run` (default) or wrapped via the T5-02 sandbox runner (constructor-injected `sandbox_run`) for the same bwrap semantics T5-04 uses for `verify_command`. `DelegateResult` maps every failure mode to a status the caller can surface (`done | timeout | error`) — timeout → exit_code=124 with the kill-after message, spawn failure → exit_code=127. `athena/delegate/cli.py:prepare_worktree(repo_path, *, base_ref, worktree_root, branch_prefix)` mints `delegate/<uuid12>` branch + `<root>/delegate-<uuid12>` worktree via `git worktree add -b <branch> <path> <base>` — git's own worktree machinery enforces "no overlapping with the main checkout"; pre-flight rejects non-repos with `DelegateError`. `capture_diff(handle)` runs `git diff <base> HEAD` (committed) plus `git diff <base>` (working) and dedupes when they're identical; empty when the delegate didn't change anything (still a valid outcome). `cleanup_worktree(handle, *, repo_path)` is opt-in (not automatic) — the caller decides whether to merge first; runs `git worktree remove --force` then `git branch -D`. `_git(cwd, *args)` raises `DelegateError` on FileNotFoundError ("git not on PATH") and TimeoutExpired so pre-flight failures stop the delegation before any side effect. `@tool("delegate_to_cli")` is the model-callable entry point — hard invariants enforced in code AND pinned by tests: SCOPE REQUIRED (empty task → rejected, NO git ops fire — stub.calls == []), config gating (`cli_delegate_enabled=False` or no command configured → rejected, NO git ops fire), WORKTREE ISOLATION (every successful path goes through prepare_worktree), NEVER AUTO-MERGES (forbidden-verb assertion across every git call: no merge/push/commit/rebase/checkout/reset against the main repo cwd; the only ops are worktree add + diff), SANDBOXED WHEN CONFIGURED (`cli_delegate_sandbox=True` → DelegateAdapter constructed with the T5-02 runner as sandbox_run; spy test pins both the True and False branches), TIMEOUT-BOUNDED (overrun → status=timeout cleanly surfaced), UNTRUSTED OUTPUT NOT APPLIED (response status ∈ {done, timeout, error, rejected} — never "merged" / "applied"; the diff + `next_step` ("review the diff; `git merge <branch>` to land, `git worktree remove ... && git branch -D ...` to discard") are the only outputs). Worktree prep failure → status=error with the underlying git stderr; adapter never invoked (spy class that raises if reached). `athena/tools/__init__.py` imports `athena.delegate.cli` (and `athena.social.search`) so the @tool decorators run at agent startup — same registration path file_ops / recall_tools / etc. use. Config additions: `cli_delegate_enabled` (default False), `cli_delegate_command` (None — vendor template), `cli_delegate_timeout_s` (600.0), `cli_delegate_worktree_root` (None → tempfile.gettempdir()), `cli_delegate_sandbox` (True). 21 new tests: 9 worktree (argv shape of `git worktree add`, isolation outside main tree, non-repo / git-failure rejection, diff capture including committed+working dedupe, MAIN-TREE-UNTOUCHED forbidden-verb pin, cleanup runs remove + branch -D), 12 tool (scope required, repo_path required, both config-gate branches reject without firing git, full happy path with diff capture, forbidden-verb pin across the delegation path, response carries "review the diff" with merge AND discard suggestions, status not in {merged, applied}, timeout surfaces exit_code=124, sandbox_run non-None when sandbox enabled and None when disabled, worktree prep failure surfaces as error with adapter never reached). Reference: `docs/reference/cli-delegation.md` includes the subprocess vs reverse-proxy choice with config snippets for routing an OpenAI-compat delegate through athena's T3-01 proxy (T6-03).
- Social/X provider with OAuth + capability routing: a social model registers as a provider declaring `social_search`; `search_x` (and an opt-in router heuristic) route social-search requests to it via the T5-01 manifest even when another model is primary, folding normalised results back into the primary model's context; switch surfaced + logged; tokens via secret storage, never logged; graceful when no social provider configured (T6-02). `Capabilities` gains a new opt-in `social_search: bool = False` field (T6-02.1) — the conservative-default pattern the manifest was designed for; only providers that explicitly declare it become candidates for the broker's `best_provider_for({"social_search"})` lookup. `athena/social/oauth.py:SocialOAuth` implements the authorization-code + refresh flow: tokens persist via `athena.safety.secure_files` at 0o600 (atomic-replace + fsync, mode asserted on POSIX); `TokenStore.__repr__`/`__str__` redact (`access_token=<redacted>`, `refresh_token=<set>|<none>`) so any third-party logger that f-strings a token instance never leaks; `_redact_for_log` is the only function permitted to format token info for logs and returns "Ns remaining" — the load-bearing `test_token_never_in_logs` runs a full exchange + access cycle with caplog at DEBUG and asserts the secret tokens never appear in the captured log text. 60s pre-emptive refresh leeway avoids the use-then-fail-then-refresh race; expired token + no refresh_token raises `RuntimeError` with a "re-run authorize" hint (no silent fall-through). Transport is injectable so tests stub the token endpoint completely (no live network). ALL vendor specifics (authorize/token URLs, scopes, client id, redirect URI, client secret file path) live in `cfg.social_oauth_*` — a vendor swap is a config edit. `athena/providers/social.py:SocialProvider` is the first capability-only provider in the registry: declares `Capabilities(social_search=True, tool_calls=False, streaming=False)` — honestly NOT a chat backend; tests/providers/test_supports_parity.py skips it via a new `_NON_CHAT_PROVIDERS` set. `stream_chat()` raises NotImplementedError (explicit > silent miss); `parse_tool_calls` is no-op. `social_search(query, *, max_results)` calls the vendor endpoint with the OAuth bearer token + maps the response via `_normalise_response` to the `{author, text, timestamp, url, metrics?}` shape — the one method that needs editing on a vendor format change. Vendor URL / query-param name / extra params / post-URL template all config-driven. Failure modes (no token, no URL configured, transport failure, non-JSON, missing "data") all return [] with a warning log; the agent gets "no results" rather than a crashed turn. `athena/social/search.py:search_x` (aliased `social_search`) is the explicit tool: resolves the social provider via `best_provider_for({"social_search"})` + `get_provider_class(name)()` + `is_available()`, surfaces "searching X via <provider>" via `ui.info` and logs the routing decision at INFO; returns JSON `{available, provider, results, reason}` so the model sees a structured payload regardless of branch. Four graceful-degrade branches (no provider, declared-but-not-ready, provider raised, empty query) all return `available=false` with a clear reason — never propagates an exception. `athena/social/router.py:should_route`/`extract_query` is the OPT-IN phrase-detecting heuristic (off by default — `cfg.social_router_heuristic=False`): eight conservative regex patterns anchored at line-start covering "search X for/about ...", "look up X for ...", "what's on X about ...", "what is X saying about ...", "any tweets about ...", "latest tweets/on X about ...", "find tweets about ..."; pure functions, no I/O, no module-level state mutation (pinned by `test_primary_model_unchanged_after_search` — routing is a pure read); long negative-cases parametrize prevents misfires on programming queries that mention "search"/"tweets" incidentally. Config additions: `social_provider_enabled` (default False), `social_search_max_results` (20), `social_router_heuristic` (False), `social_search_url`, `social_search_query_param` ("query"), `social_search_extra_params` ({}), `social_post_url_template`, plus six `social_oauth_*` knobs. 60 new tests: 2 capabilities (defaults False + supports when declared), 15 OAuth (secure persistence, refresh on expired + on refresh failure, has_valid_token states, the load-bearing token_never_in_logs across exchange/access/repr, TokenStore.__repr__ redaction, authorize_url shape, fallback_refresh handling, clear() idempotence), 14 search (capability declaration, broker resolution, normalisation, empty/no-token/no-URL/transport-failure all return [], visible switch via ui.info, no-provider degrades with reason, declared-but-not-ready, provider raises returns structured error, empty query reason, cfg max_results default), 29 router (off-by-default, 15 parametrized positive shapes, 9 parametrized negative shapes including programming-query false-positive guards, degenerate input, query extraction across all positive shapes, trailing "?" stripping, pure-read invariant). Reference: `docs/reference/social-routing.md`. (T6-02).
- Semantic recall: embedding-based retrieval over prior turns + memories via the T5-01 embeddings capability (local-preferred → offline-capable); hybrid RRF ranker fusing FTS5 + vector results (mode=keyword|semantic|hybrid, hybrid default); incremental embedding on write + `athena recall backfill`; keyword-only fallback when no embeddings provider (T6-01). `athena/recall/embeddings.py` resolves an `Embedder(provider, model_id)` via `MediaRegistry.backend_for("embeddings")` — local-preferred, returns None cleanly when no provider declares the capability. `Embedder.embed(text)` normalises response shapes across providers (bare list / `{"embedding": ...}` / OpenAI's `{"data": [{"embedding": ...}]}`). `athena/recall/hybrid.py:rrf_fuse(keyword_ranked, vector_ranked, *, k=60)` is a pure RRF: `score = Σ 1/(k + rank + 1)` — a doc in both lists outranks a doc in only one, no score normalisation needed. `athena/recall/vector_store.py:VectorStore` is JSON-backed + pure-Python cosine (matches athena's per-user storage weight — no numpy, no server-class vector DB); `.add` is doc_id-upsert (rewrite-friendly), `.search` ALWAYS filters by `model_id` (silently mixing embedding spaces is the worst-case failure for semantic recall) and optionally by workspace, `.backfill(items)` bulk-adds. `athena/recall/manager.py` adds the ContextVar binding (`get/set_active_vector_store` mirroring T3-03's checkpoint manager pattern), doc-id conventions (`session_id#turn_index` for turns, `memory:<profile>:<name>` for memory entries), `build_vector_store(*, cfg, profile_dir)` once-per-session construction, and `record_turn` / `record_memory_entry` best-effort embed-on-write (no active store → silent no-op; skips empty content, role="tool", and non-text content lists). `athena/tools/recall_tools.py:search_sessions` gains a `mode` parameter (`keyword|semantic|hybrid`); `_resolve_mode` reads `cfg.recall_default_mode` (default "hybrid"); `_ranked_hits` dispatches: keyword → store.search; no active vector store → fall through to keyword (semantic+hybrid degrade gracefully); semantic → top-k vector ids → hydrate via store.load; hybrid → FTS5 candidates as the canonical hydration source + vector candidates → RRF fuse → top-k preferring the FTS5 SearchHit when available, else hydrate from store.load. Candidate pool is `max(k*3, k+5)` so the fuser has rank signal. Output shape unchanged — every mode flows through the same `_format_hits`. `Agent.run_turn` lazy-builds `self._vector_store` via `build_vector_store` and binds it on the ContextVar inside the same try/finally scoping checkpoint manager + skill metrics; sub-agents / forks see None (foreground-only signal). `Agent._persist_message` calls `record_turn` after every successful `append_turn` (turn_index = `len(self.messages) - 1`); a recall-side exception is debug-logged + swallowed so a recall bug never blocks a session write. `athena recall {backfill,status,clear}` CLI: backfill walks `<profile_dir>/sessions/*.jsonl` + `<profile_dir>/memory/**.md` and embeds doc IDs not already in the index (idempotent); status lists totals + by_model + by_workspace; clear drops the index file. Config additions: `semantic_recall_enabled` (default true), `recall_default_mode` (default "hybrid"), `embedding_model_prefer` (default "local"), `embedding_model` (None → provider default), `vector_store_path` (None → `<profile_dir>/vectors.json`). 25 new tests: 6 RRF (combines ranks across both lists / disjoint / empty / one-empty / intra-list dedupe / k smooths decay with order-flip at the boundary); 10 vector store (add+search nearest-first / no embedder = empty / model_id isolation as load-bearing safety / workspace filter / workspace=None unscoped / backfill counts skipping empty / add overwrites same doc_id / persistence roundtrip across instances / corrupt file recovers); 9 semantic+hybrid integration (the headline paraphrase-finds test, hybrid combines both signals, degrade to keyword when no embeddings, output shape parity, mode resolution including invalid fallback, record_turn writes to active store with correct doc_id, no-store silent, skips role=tool). Allowlist additions in tests/safety/test_no_raw_writes.py for `athena/recall/vector_store.py` (machine-managed embedding index, not user content). Reference: `docs/reference/recall.md` (T6-01).
- `/goal` autonomous continuation loop: the persistent goal can now drive synthetic continuation turns until achieved or exhausted, not just shape the prompt; subcommands `set`/`status`/`pause`/`resume`/`clear` + `/subgoal`; `GOAL ACHIEVED`/`GOAL BLOCKED` sentinels; turn + token caps with resume; Ctrl+C pauses and real user messages / `/steer` always take precedence; persisted goal state survives restart; gateway parity (T5-07). `athena/goal/state.py` introduces `Subgoal`, `GoalState` (text + status `active|paused|achieved|exhausted` + turns_taken/max_turns + ordered subgoals + created_at/updated_at + `.can_continue()` + `.first_pending_subgoal()`), and `load_state` / `save_state` / `clear_state` persisting to `<profile_dir>/goal_state.json` — separate from human-editable `goal.txt`. `athena/goal/loop.py` adds `scan_sentinels(text)` (case-insensitive, markdown-tolerant regexes for `GOAL ACHIEVED` and `GOAL BLOCKED: <reason>` — achieved wins over blocked when both appear) and `maybe_continue_goal_after_turn(...)` returning a `ContinuationDecision` (`should_continue`, `synthetic_prompt`, `stop_reason`, `blocked_reason`) with the full state-transition table: achieved sentinel → status=achieved, blocked → status=paused + reason, turn cap → status=exhausted, paused/achieved/exhausted state → that status as stop_reason, else bump turns_taken + return synthetic prompt. Persistence best-effort (disk error logged, never raised). `athena/commands/goal.py` extended: bare `/goal` / `/goal status` shows text + status + turns/cap + subgoals; `/goal pause` / `/goal resume` flip status (resume after exhausted bumps `max_turns` by `cfg.goal_max_turns` on top of `turns_taken` — anti-runaway: total work stays visible, never reset to 0); `/goal clear` removes both files. New `/subgoal` command: bare shows list with ✓/• markers, `<text>` appends, `done` marks first pending complete. `athena/goal/invariant.py:format_for_system_prompt` extended with `state=` — renders ordered subgoals + the sentinel contract (telling the model exactly how to signal achievement/blockage) into the goal block. `athena/agent/core.py:Agent.run_turn` now wraps `_run_turn_inner` in a continuation loop: after each real assistant turn, `_consult_goal_continuation` reads `_last_assistant_text` + `_last_turn_interrupted` (both set inside `_run_turn_inner`) and returns the next synthetic prompt or None. Interrupt-wins-over-everything invariant: Ctrl+C during streaming or during tool execution sets `_last_turn_interrupted=True` and the hook pauses the goal even when the cut-off text contained a `GOAL ACHIEVED` line; the interrupted turn doesn't count toward `turns_taken`. Token cap measures consumption since loop start (`tokens_at_loop_start` captured in `run_turn`) — pre-existing session tokens excluded; `goal_max_tokens=0` disables the cap. Gateway parity is inherited automatically — `gateway/agent_pool.py:155` already calls `await agent.run_turn(text)`, so the same caps + sentinels + interrupt semantics apply on gateway runs with no extra wiring (matters most where there's no human at the keyboard). Pairs with T5-04: when verified-execution is on, the verification report lands in the tool result the model sees BEFORE the continuation hook reads `assistant_text`, so each continuation factors in "did my last write break something" — "pursues a goal AND doesn't leave the codebase broken." Config additions: `goal_loop_enabled` (default `true`), `goal_max_turns` (default 25), `goal_max_tokens` (default 200_000; 0 disables), `goal_continuation_prompt` (default None → built-in that reinforces the sentinel contract), `goal_achieved_sentinel` / `goal_blocked_sentinel` (reserved for future custom-sentinel support). 73 new tests across `tests/goal/` (16 state — roundtrip with subgoals, can_continue boundaries, defensive load including unknown-status normalisation; 19 sentinels — markdown variants, BLOCKED reason extraction, ACHIEVED-beats-BLOCKED, degenerate input; 10 loop decision — achieved/blocked/exhausted/paused/disabled stops, continuation prompt content, persistence-failure resilience; 15 subcommands — set/replace/pause/resume bumps cap from exhausted/clear/show/subgoal add+done+requires-goal, invariant block rendering with subgoals + contract + back-compat shape; 13 loop integration — interrupt wins over achieved sentinel, token cap with loop-start offset, `goal_max_tokens=0` disables, paused/no-state/disabled silent stops, `_goal_loop_tokens_used` tracking). Allowlist addition in `tests/safety/test_no_raw_writes.py` for `athena/goal/state.py` (machine-managed bookkeeping). Reference: `docs/reference/goal.md` (T5-07).
- Cross-session prompt caching: a stable prefix (system prompt + pinned skills + durable context) cached in one session is reused in the next, keyed by a SHA-256 of the exact prefix (changed prefix = clean miss, never a wrong hit); caching mechanism chosen from the provider manifest (server-side TTL cache, local KV-cache reuse, or none); TTL-bounded; `athena cache status`/`clear` (T5-06). `athena/cache/cross_session.py` exposes `prefix_hash(text)` (SHA-256 hex), `CacheEntry` (workspace + prefix_hash + provider + provider_cache_id + ttl_s + created_at; `.alive(now=)` for TTL), `CachingPlan` (mode `"server" | "kv_reuse" | "none"` with optional ttl_s), and `CrossSessionCache(index_path, cfg)` with `.caching_plan(provider)` (manifest lookup), `.lookup/.record/.clear/.all`. Key is `"<workspace>::<hash>::<provider>"` — workspace prevents cross-project leakage; provider prevents cross-backend reuse. Size gate via `cache_min_prefix_tokens` (4-bytes-per-token estimate; default 1024 → ~4 kB minimum); below threshold → record returns None and lookup short-circuits. JSON-on-disk persistence at `cfg.cache_index_path` (default `<profile_dir>/cache_index.json`); corrupted file starts fresh. `Agent.__init__` calls `_init_cross_session_cache` right after building the initial system message — caching_plan, hash, lookup-or-record, set `cross_session_cache_entry`; best-effort (any exception is a debug log; session start never blocked). Reuse mechanics are the provider/backend's job: athena's existing T2-01 `cache_control` headers already replay the same prefix bytes, so when the prefix is byte-stable across sessions in a workspace the provider's server cache naturally hits. `athena cache status [--json] [--profile NAME] [--index-path PATH]` lists ALIVE/EXPIRED entries; `athena cache clear` empties the index. Config additions: `cross_session_cache_enabled` (default true), `cache_min_prefix_tokens` (default 1024), `cache_index_path` (default None → profile-dir derived). 28 new tests (16 module: hash properties, lookup correctness — the no-wrong-hits load-bearing tests for changed prefix / wrong workspace / wrong provider / expired entry; caching_plan resolution; size gate; persistence across instances; corrupt-file recovery; `.all()` ordering. 6 session-start integration: records on miss, reuses on hit with `created_at` preserved, skill-edit changes hash → miss, disabled = no-op, provider with no caching declared = skip, workspace isolation. 6 CLI: list + JSON + no-index, clear + no-index, `--index-path` override). Allowlist additions in tests/safety/test_no_raw_writes.py for `athena/cache/cross_session.py` (operational metadata) and `athena/mcp/differentiated.py` (T5-05 verified_write surface, routed through path_security + the verification loop — the surface the test is protecting). Reference: `docs/reference/cross-session-cache.md` (T5-06).
- Capability broker: media operations resolve their backend via the T5-01 capability manifest (local-preferred); `athena mcp serve` re-exposes athena's distinctive capabilities to other agents — `verified_write` (T5-04), `rollback_to` / `list_checkpoints` (T3-03), `analyze_image` / `analyze_video` (T4 / MediaRegistry-routed), `recall` — advertising only what the host actually supports; routing logged; remote writes still path-secured + verified (T5-05). `athena/media/MediaRegistry` exposes `backend_for(cap)` (class-level), `available_backend_for(cap, *, pool)` (credential-aware via `runtime_resolver.available_providers_with_capability`), `candidates(cap)` (sorted declaring names), and `can(cap)` (presence). Selection: filter to declarers, prefer `is_local=True` when `cfg.media_backend_prefer == "local"` (default), else alphabetical fallback. `athena/mcp/differentiated.py` `build_differentiated_tools(workspace, cfg, checkpoint_manager)` returns a `DifferentiatedTools` bundle whose `.descriptors` list is gated per host: `verified_write` and `recall` always advertised, `rollback_to` / `list_checkpoints` iff `checkpoint_manager` is non-None, `analyze_image` / `analyze_video` iff `MediaRegistry.can("vision")` is True. `cfg.mcp_expose` whitelist narrows further when set. `verified_write` over MCP routes path → `validate_path` (T1-07) → write → `VerifiedExecution.verify_write` → report appended; failure returns `isError=True` with the `/rollback-to <id>` hint inline so the calling agent can chain to `rollback_to`. T3-02 curation rule preserved: no `bash`, no raw `Write` / `Edit`, no `Execute` — pinned by `test_no_raw_bash_tool_advertised`. `AthenaMCPServer` extended with optional `differentiated:` field; `tools/list` concatenates base + differentiated; `tools/call` name-routes (no collision — base set is `athena_`-prefixed). Config additions: `media_backend_prefer` (default `"local"`), `mcp_expose: tuple[str, ...]` (default `()`). 26 new tests (11 media registry incl. credential-aware + 15 MCP differentiated). Reference: `docs/reference/capability-broker.md` (T5-05).
- Verified-execution loop: `athena/verify/` fuses the three Tier-5 pieces — T3-03 conversation/file checkpoints, T5-03 LSP diagnostics, T5-02 bubblewrap sandbox — into a single post-write orchestrator. `VerifiedExecution.verify_write(path)` captures a pre-write checkpoint, calls `diagnose_paths([path])`, optionally runs `cfg.verify_command` under `athena.sandbox.runner.run` when `verify_on_write="diagnose+run"`, and produces a `VerificationOutcome` in one of four states (`passed`/`failed_diagnostics`/`failed_run`/`skipped`). Failures land in the tool result inline: `✗ <path>: write introduced N error(s):` + the first 8 errors + `Roll back with: /rollback-to <id>` (or `(auto-rolled back to pre-write checkpoint)` when `verify_auto_rollback=true`). Wired into `athena.tools.file_ops` `Write` + `Edit` so every successful write that passes the syntax lint also runs the verifier; passing outcomes stay quiet on the green path, failures append the report so the model sees the rollback hint inline. `athena verify <path> [--command "<cmd>"] [--no-sandbox] [--timeout S]` is the one-shot CLI surface (exit codes: 0 passed/skipped, 1 failed_diagnostics, 2 failed_run, 3 path not found). Per-leg degradation pinned by tests: checkpoint capture failure → no rollback hint, write still verifies; diagnose exception / unknown-shape returns → treated as no errors; runner exception → `failed_run` with `exit_code=-1`; runner timeout → `exit_code=124`. Capped auto-retry via `verify_auto_retry`/`verify_max_retries` plus a caller-supplied `retry_fn` callback (default off — never loops without an explicit hook). `VerifiedExecution.latest_outcome` is the read-only accessor the deferred T5-07 goal-evaluation loop will consult. `athena.sandbox.runner.run(command, *, cfg, workspace, timeout_s)` is a function-call-shaped sandbox parallel to T5-02R's `_spawn` wrap — same `shell_policy` denylist floor + bwrap argv + `sandbox_fallback` semantics, but returns a structured `RunResult(exit_code, stdout, stderr, sandboxed)` rather than a streaming string. 53 new tests across `tests/verify/` (14 outcome model, 15 loop, 15 degradation + retry, 6 file_ops wiring) + `tests/sandbox/test_runner.py` (9) + `tests/cli/test_verify.py` (6). Config additions: `verify_on_write` (`off`/`diagnose`/`diagnose+run`, default `diagnose`), `verify_command`, `verify_auto_rollback` (default `false`), `verify_auto_retry` (default `false`), `verify_max_retries` (default 2), `verify_run_timeout_s` (default 120). Reference: `docs/reference/verified-execution.md` (T5-04).
- LSP diagnostics: a new `athena/lsp/` package launches a configured language server (default: `pyright-langserver --stdio` for Python) over JSON-RPC + `Content-Length` framing, opens documents, collects `textDocument/publishDiagnostics`, and returns a flat `list[Diagnostic]`. Surfaced to the agent as the `Diagnose` tool (alias `diagnose`, toolset "code") that returns a compact errors-first summary; consumed by the T5-04 verified-execution loop via the same `diagnose_paths(paths)` alias so a fresh error after an edit can gate the change. Opt-in via `lsp_enabled` (default `false`); `lsp_server_command` overrides the per-language argv map; `lsp_timeout_s` caps the per-call wait. Graceful by design — empty paths, disabled config, unmapped language, server missing from PATH, transport-spawn failure, mid-session exception, and timeout all return `[]` with a debug log; no exception escapes into the agent loop. The tool's output distinguishes "LSP disabled" / "server not installed" / "all clean" / "diagnostics found" so the operator can see whether the gate actually fired. 31 new tests (17 client over a `FakeTransport`, 14 tool surface + registry hookup) — every test runs without spawning a real language server (T5-03).
- Local sandbox: agent shell commands can run inside a bubblewrap (bwrap) jail (read-only system root, writable workspace only, no network by default; namespace-isolated PID/IPC/UTS/cgroup; `--cap-drop ALL`; `--die-with-parent` lifetime) — a defense-in-depth layer wrapping the Bash tool's execution at the single `_spawn` seam. The existing `shell_policy` denylist still runs FIRST as the security floor; a denylisted command never reaches the sandbox decision path. Opt-in via `sandbox_enabled` (default `false`); Linux-only with a configurable warn / error fallback (`sandbox_fallback`) where bwrap is unavailable. `sandbox_allow_network` (default `false`) toggles `--unshare-net`; `sandbox_writable_paths` extends the bind set beyond the workspace; `sandbox_backend = "bwrap"` reserves the spot for future backends. Streaming + timeout + kill semantics unchanged — bwrap returns a normal `Popen` the existing `_stream_*` code drives. 33 tests (21 backend argv shape + availability, 12 integration including denylist-precedence and timeout-preservation); existing 37 shell tests stay green (T5-02).
- Capability manifest: every provider declares a `Capabilities` manifest (tool calls, streaming, vision + max image edge, prompt caching + TTLs, KV-cache reuse, structured output, embeddings, max context tokens, locality, native format) via a new `classmethod static_capabilities()` on the existing `Provider` ABC. `supports_tools` / `supports_streaming` fold into delegators reading the manifest as the single source of truth — behaviour unchanged, pinned by a 15-assertion parity test that ran green before the fold and after. Per-provider declarations: ollama (maximal vision narrowed by `capabilities(model)` against an `_VISION_MODELS` substring set; `is_local=True`, `kv_cache_reuse=True`, embeddings, structured_output, native `"ollama"`), anthropic (vision 1568px, prompt_caching with (300, 3600) TTLs matching the T2-01 cache strategy, 200k context, native `"anthropic"`), openai (vision 2048px, embeddings, prompt_caching, 128k), google (vision 3072px, 1M context, native `"google"`), openrouter (broad pass-through), nous (caching, no vision), openai_compat (conservative — tool_calls + streaming only since the backend is host-defined). Capability queries over the existing `_REGISTRY`: `capability_matrix()`, `providers_with_capability(cap)`, `best_provider_for(needs, prefer=None)` are all class-level (no instances, no network); credential-aware `available_providers_with_capability(cap, *, cfg, pool)` in `runtime_resolver` reuses the existing `_build_provider` credential gate. `athena providers capabilities [--json]` (alias `caps`) renders the matrix; `add-key` / `list` / `test` / `models` unchanged. 35 new tests across `test_supports_parity` (15), `test_capabilities` (15), `test_capability_queries` (13). Keystone for T5-05 broker, T5-06 cache routing, T6-01 semantic recall, T6-02 social routing, T6-05 video gen (T5-01).
- Skill metrics: per-skill usage tracking (views, last-used, distinct sessions, optional outcomes) recorded at the progressive-disclosure hook (`athena.skills.manager.skill_view` + `athena.skills.loader.load_body`); persisted as append-only JSONL at `<profile_dir>/skill_metrics.jsonl` (same path T3-05R's suggestion enhancer already reads — two record shapes share the file without cross-contamination). Surfaced via `athena skill metrics [--top N --stale-days N --profile NAME --json]` and fed into the curator: a "Recent usage signal" section in the curator fork's prompt addendum, plus a "Per-skill usage" section in `REPORT.md` / `run.json` — metrics inform, they don't override the curator's existing hard rules (write_origin=foreground / pinned / migration-origin). Closes the gap where Phase-16 observability tracked tools but not skills. 47 new tests (19 store + 9 disclosure hook + 10 curator wiring + 9 the previously-shipped T3-05R suggestion enhancer that we now reuse cleanly) (T3-06).
- `athena.skills.metrics` module: `SkillMetric` dataclass with `days_stale` helper, `SkillMetricsStore` (record_view, record_outcome, get/all/top/stale/never_used) with thread-safe locked appends, `_NoopStore` for the disabled path, `metrics_path(profile_dir)` canonical accessor matching T3-05R, and a `ContextVar`-based active store with `get/set_active_store` + `record_view_active` so the disclosure hook fires without explicit threading (T3-06).
- `cfg.skill_metrics_enabled` (default `True`). Disabled installs a `_NoopStore` at session start — the hook fires but records nothing (T3-06).
- `Agent.__init__` builds a `SkillMetricsStore` for foreground sessions (skips forks); `Agent.run_turn` scopes the store on the ContextVar for the turn's duration so fork threads in their own context don't inflate parent metrics (T3-06).
- `docs/reference/skill-metrics.md` covers the disclosure hook, the JSONL format + T3-05R compatibility, `athena skill metrics` flags, curator integration (prompt addendum + REPORT.md), the metrics-inform-don't-override invariant, and the fork-isolation behaviour (T3-06).
- Training-data labeling TUI: `athena train review` gains a `textual` batch UI over the existing `LabelPrompt` seam (`athena/transform/review.py`) — hotkeys (y/n/p/s/space/Y/N/enter/h/l/ctrl+z/q), multi-select batch labeling, undo, progress + EWMA ETA, classifier-driven auto-suggestion enrichable by optional T3-06 skill metrics — to keep the trajectory pool labelled in real time for the SFT/DPO → LoRA → GGUF → `ollama create` loop. Writes the same flat-dict `<profile_dir>/labels/<session_id>.json` sidecar that `default_prompt` writes today (the TUI and the classic path share the `save_label` writer; switching between them is byte-safe). `--no-tui` keeps the original prompt and is the auto-fallback when textual isn't installed. `--keymap {default,vim,basic}` (basic avoids ctrl combos). Behind the `athena[train]` extra; suggestion enhancer cleanly degrades to classifier-only when `<profile_dir>/skill_metrics.jsonl` is absent. 31 new tests (14 textual TUI guarded by `pytest.importorskip`, 17 suggestion enhancer) (T3-05).
- `athena.transform.batch_driver` (`BatchReviewDriver`): headless logic layer over `ReviewSession.pending` + `save_label` with multi-select set, undo stack (50 entries), and a single-source-of-truth write path. 14 tests covering single labels, batch ops, skip semantics, multi-session sidecar isolation, undo with prior-label restoration, and a byte-for-byte format guard against the original `default_prompt` writer (T3-05).
- `athena.transform.suggestion.build_suggestion_fn(profile_dir)`: returns the `SuggestionFn` the TUI uses. Layers classifier output (always on) with an OPTIONAL metrics override read from `<profile_dir>/skill_metrics.jsonl` when the skill's prior labels are ≥ 95% one-sided across ≥ 10 decisive entries. T3-06 is genuinely optional (T3-05).
- `athena.transform.keymaps`: three named maps — `default`, `vim` (alias), `basic` (no ctrl combos). Action names are stable across maps so the TUI's dispatch path doesn't change (T3-05).
- `docs/reference/labeling.md`: TUI reference covering install via `[train]` extra, hotkey table per keymap, batch + undo + suggestion behaviour, the closed-loop flow `review → build-dataset → run → ollama create`, and limitations (T3-05).
- `athena audit {skill,memory} --content`: opt-in flag that extracts before/after file bytes from the SnapshotStore tarballs each audit row points at and emits a unified diff per event inline. Before-state comes from the audit row's own `snapshot_id`; after-state comes from the next mutation-for-same-path's snapshot, or the live file when none exists. Truncates at 200 lines per event. Closes T3-04 gap #2 ("hash-only diff"). Default behaviour is unchanged — `--content` is the explicit opt-in for the deeper recovery (T3-04.1).
- `athena.audit.content` module: `extract_file_from_snapshot(snapshot_id, target, ...)` walks the `SnapshotStore` date-tree to find the tarball and pulls the file's captured bytes; `unified_diff_for_event(snapshot_id, next_snapshot_id, target_path, ...)` builds a difflib unified diff with sensible truncation and graceful fallback when extraction fails (missing tarball / binary content / decode failure → empty string sentinel). 12 tests covering extraction round-trip, missing snapshot, missing target in snapshot, binary file rejection, two-snapshot diff, live-file fallback, identical content empty-diff, truncation, no-snapshots None-return, end-to-end through `collect_skill_events(with_content=True)` (T3-04.1).
- `athena audit skill <since> <until>` and `athena audit memory <since> <until>` CLI subcommands: replay athena's existing `MutationAuditLog` JSONL between two timestamps and produce human-readable or `--json` diffs of every skill / memory mutation in the window. Supports ISO 8601, relative ago-forms (`5m`, `2h`, `3d`, `1w`), and special tokens (`now`, `boot` / `session-start`, `last-checkpoint`). Cross-references T3-03 rollback / checkpoint events so the user knows when some shown changes may have been reverted. Tool-name classification matches what's actually written by `athena.skills.manager` / `athena.memory.providers.builtin_file` (`skill_create` / `skill_patch` / `skill_delete` / `skill_write_file` / `skill_rollback`; `memory_write` / `memory_delete`). Read-only — output to stdout, never mutates the audit log (T3-04).
- `athena.audit` package: `timestamps.parse_timestamp` (ISO + relative + special tokens with injectable `now=` for deterministic tests), `diff.collect_skill_events` / `collect_memory_events` / `collect_rollback_markers`, `diff.render_*` / `render_*_json` rendering helpers. 42 tests across the two test files (T3-04).
- `docs/reference/audit-diff.md` reference covers the parent-subcommand choice (`athena audit` rather than overloading the existing `athena skill diff <name>`), timestamp forms, output shapes, the `tool_name` → category classification table, the SHA-only content limitation, and the rollback cross-reference (T3-04).

### Changed
- `athena proxy` retargeted onto aiohttp + Phase-16 observability (T3-01R). Replaces the original FastAPI/uvicorn implementation: same OpenAI-compat surface (`/v1/chat/completions`, `/v1/models`, `/health`, `/v1/embeddings` stub), same `resolve_provider` routing, but the HTTP layer now uses the same aiohttp stack as the webhook/gateway listeners — single server family covers every athena-as-server role. The `[proxy]` extra drops FastAPI+uvicorn in favour of just `aiohttp>=3.9` (already pulled by `[gateway]`). Each call emits an `athena.proxy.call` span via `athena.plugins.bundled.observability.spans.start_span` with provider/model/stream/tokens/latency attributes; inert when the `[observability]` extra isn't installed. CLI is now `athena proxy serve` (matches the verb pattern of `athena mcp serve`). Resolver `RuntimeError` (credentials exhausted) surfaces as `503 type=provider_unavailable`. Translation logic lifted into the framework-free `athena/proxy/translate.py` with 9 dedicated unit tests; aiohttp server tests use `aiohttp.test_utils.TestServer` + `TestClient` so no new pytest plugin (T3-01R).
- Pure StreamChunk → OpenAI translation (`athena/proxy/translate.py`): `stream_chunks_to_openai_sse` sync generator (role chunk first, `[DONE]` last, finish_reason synthesised if upstream omits the `end` chunk, multiple tool_calls get sequential indices keyed by id) + `collect_chunks_to_openai_response` for the non-streaming path (T3-01R).

### Added
- Conversation-level rollback: `/checkpoint [label]` and `/rollback-to <label>` slash commands, plus `athena checkpoint {create,list,rollback,purge}` non-interactive CLI. Each checkpoint captures four things — session message-log offset, content-addressed file snapshot of tracked-modified files, skill registry state token (manifest + content blobs), memory store state token — and rollback reverts all four atomically. Rollback is itself undoable via an auto-created `pre-rollback-of-<id>` checkpoint snapshotting the current state before overwriting; this is what makes "no silent clobber" a real guarantee even without per-file diff prompting (which is reserved for a follow-up). `InFlightToolCallError` blocks rollback while a tool call is mid-flight. After rollback the slash command reloads `agent.messages` from the truncated session JSONL plus the synthetic system marker so the next provider call sees the rolled-back state cleanly (T3-03).
- `athena.agent.checkpoints` module: `Checkpoint` dataclass, `CheckpointManager` (per session), `CheckpointAuditLog` (per-session JSONL at `<checkpoint_dir>/audit.jsonl`), `snapshot_skills`/`restore_skills`/`snapshot_memory`/`restore_memory` content-hash helpers, plus a `ContextVar`-based `get/set_active_checkpoint_manager` + `track_modified_file` accessor pair. File-snapshot tarballs go through the existing `SnapshotStore`; skill/memory state lands content-addressed under `<checkpoint_dir>/state/{skills,memory}/<token>/`. 26 unit tests + a 3-test integration suite that drives the wedge through the real `Write` tool, slash dispatch, and on-disk truncation (T3-03).
- `athena/tools/file_ops.py` (`Write`, `Edit` verbatim + fuzzy paths) and `athena/tools/patch_apply.py` now call `track_modified_file(path)` after every successful write. Safe no-op when no checkpoint manager is active (forks, profile-less sessions, tests) — lazy imports avoid the `file_ops → agent.checkpoints → agent.*` cycle (T3-03).
- `Agent.__init__` builds a `CheckpointManager` for foreground sessions (skips forks); `Agent.run_turn` binds it to the `ContextVar` only for the duration of the turn so fork threads in their own `ContextVar` context can't inadvertently inherit it (T3-03).
- `docs/reference/checkpoints.md` reference: storage layout, capture/restore order, undoing a rollback, in-flight protection, what rollback can't do (external side effects), known limitations (T3-03).
- MCP request log (`athena/mcp/request_log.py`): every handled MCP request appends a summary line to `~/.athena/mcp.jsonl` with `{request_id, ts, client_name, method, params_summary, result_summary, latency_ms}` (`error` on failures). Tool args and resource bodies stay off-log by default — the client knows what it sent. `--log-path` flag on `athena mcp serve` overrides `cfg.mcp_log_path`. Wired into `AthenaMCPServer.handle_request` as a best-effort sink: a disk failure never blocks the response. Closes a gap from the original T3-02 spec list (T3-02 audit).
- Dedicated `tests/mcp/test_resources.py` (13 tests) covering per-URI resolution for the three resource roots — skills index/specific/missing, memories index/missing, audit index/specific/missing, plus unknown-URI-scheme and empty-URI error paths. Previously only `tests/mcp/test_handshake.py` exercised `resources/list` at the dispatch level (T3-02 audit).
- `athena mcp serve` subcommand: stdio MCP server exposing 7 curated tools (`athena_snapshot_files`, `athena_rollback_files`, `athena_list_skills`, `athena_read_skill`, `athena_list_memories`, `athena_read_memory`, `athena_audit_query`) and 3 resource roots (`athena://skills/`, `athena://memories/`, `athena://audit/`) to peer MCP clients (Claude Desktop, Claude Code, Cursor). Capability handshake per MCP spec 2024-11-05. Tools surface is intentionally curated — no `bash`, no `Write`, no arbitrary code execution exposed; rollback is the only mutation, and only from sanctioned snapshots. SSE transport is reserved for a follow-up release. 35 tests (handshake 9 + tools 20 + stdio transport 6); integration runbook against Claude Desktop in `tests/mcp/test_integration_with_claude_desktop.md` (T3-02).
- MCP server core (`athena/mcp/server.py`): `AthenaMCPServer` dispatcher with honest capability advertising (`tools` + `resources` only — not `prompts`, not `sampling`); JSON-RPC error codes -32601/-32602/-32603 per spec; notifications correctly return None per JSON-RPC (T3-02).
- Curated MCP tool surface (`athena/mcp/tools.py`): `AthenaMCPTools` adapts the 7 tool calls to existing athena modules — `SnapshotStore` for snapshot/rollback, `discover_skills` + `load_body` for skills, `memory.store.list_entries`/`read_entry` for memory, direct JSONL walk over `~/.athena/audit/mutations-*.jsonl` for audit queries (since the existing audit module is write-only) (T3-02).
- MCP resources surface (`athena/mcp/resources.py`): `AthenaMCPResources` resolves `athena://skills/[name]`, `athena://memories/[name]`, `athena://audit/[YYYY-MM]` URIs to markdown / NDJSON content (T3-02).
- Stdio transport (`athena/mcp/transport_stdio.py`): sync line-delimited JSON-RPC pump matching the existing `athena/mcp/demo_server.py` pattern. StringIO-injectable for tests. Logs to stderr (stdout is the wire). -32700 parse error on malformed JSON per spec (T3-02).
- MCP config block: `mcp_default_transport` (default `"stdio"`), `mcp_sse_port` (8765), `mcp_log_path` (`~/.athena/mcp.jsonl`), `mcp_allow_write` (False — reserved) (T3-02).
- `docs/reference/mcp-server.md` reference covers the seven tools, three resources, transports, configuration, security posture, and the smoke-test runbook (T3-02).
- `athena proxy` subcommand: local OpenAI-compatible HTTP endpoint with OpenAI ↔ Anthropic translation; provider routing by config or `X-Athena-Provider` header; transparent prompt caching, retry, and rate-limit tracking via existing provider machinery; FastAPI/uvicorn ship under the new `[proxy]` optional-extra so headless installs stay slim. `/v1/models`, `/v1/chat/completions` (streaming + non-streaming), `/v1/embeddings` (501 stub). Smoke runbooks for Aider and alternate clients in `docs/proof/`. 54 tests across translator (25), router (10), proxy logger (7), and server (12) (T3-01).
- Proxy translator (`athena/proxy/translator.py`): pure functions for OpenAI → Anthropic request shape (system hoist, tool unwrap, role=tool fold into tool_result blocks, tool_choice mapping) and Anthropic stream events → OpenAI SSE chunks (role chunk, text deltas, tool_calls fragments, finish_reason). Sync — adapts at FastAPI boundary (T3-01).
- Proxy router (`athena/proxy/router.py`): `route_request()` resolves a provider in three steps — `X-Athena-Provider` header (case-insensitive) → model-name match against `KNOWN_MODELS_PER_PROVIDER` → fallback to `proxy_default_provider`. Raises `RouteError` with available-provider list when the default isn't reachable (T3-01).
- Proxy traffic log (`athena/proxy/logging.py`): one JSONL line per completed request in `~/.athena/proxy.jsonl` (request_id, ts, client_ua, model_requested, provider_used, latency_ms, tokens_in/out, cache_read_tokens, request/response summary). Opt-in full request/response bodies under `~/.athena/proxy_bodies/<id>.json` behind `proxy_log_bodies = true` (T3-01).
- `athena/proxy/server.py` exports `make_app(cfg, pool, provider_factory)`, `stream_chunks_to_openai_sse`, `collect_chunks_to_openai_response`. The server bridges athena's sync `Iterator[StreamChunk]` providers to async SSE via FastAPI's threadpool — keeps caching/retry/rate-limit on the path; the dedicated Anthropic-format translator is reserved for a future `--no-translate` deep-integration mode (T3-01).
- `athena proxy` CLI flags: `--host`, `--port`, `--bind-public`, `--provider`, `--log-bodies`, `--no-translate`. Default binds 127.0.0.1:11434 (Ollama's port — slots in cleanly for tools already configured for Ollama). `--bind-public` is the only way to bind 0.0.0.0 and prints a warning (T3-01).
- Eight proxy config fields under `Config`: `proxy_default_provider` (default `"anthropic"`), `proxy_bind_host`, `proxy_bind_port`, `proxy_require_auth`, `proxy_log_path`, `proxy_log_bodies`, `proxy_bodies_dir`, `proxy_no_translate` (T3-01).
- `docs/reference/proxy.md` reference; `docs/proof/proxy-aider-runbook.md` + `docs/proof/proxy-clients-runbook.md` operator runbooks for the manual integration tests (T3-01).
- `clarify` tool: multiple-choice question for ambiguous requests. Foreground path is a stdin prompt with numeric / case-insensitive label / unique-prefix resolution. Background forks (curator, auxiliary, background review) AUTO_DENY via the `in_fork_context` ContextVar so the fork can't block on stdin it doesn't own. Pluggable `GatewayClarifyHook` for platform-native UIs in Tier 4 (Telegram inline keyboards, Discord button rows, Slack block kit) (T2-08).
- `clarify_default_timeout_seconds` (default 300) and `clarify_allow_freeform` (default False) config options (T2-08).
- `docs/reference/clarify-tool.md` documents the three resolution paths, input parsing priority, timeout behaviour, and the gateway-hook contract (T2-08).
- `patch_apply` tool. Accepts a unified-diff (the output of `diff -u`) and applies every hunk across every file atomically. Per-file backup + restore on partial failure means either the whole patch lands or no file is modified. `path_security.validate_path(intent="write")` runs on every target before any write. Useful for multi-callsite renames, multi-line refactors, and any edit that would otherwise need more than two `Edit` calls (T2-07).
- `patch_parser` module: pure `parse_patch(text)` → structured `Patch` and `apply_patch_to_text(original, file_patch)` → new content. Tolerates leading commit-message garbage; handles multi-file + multi-hunk diffs; processes hunks in reverse `old_start` order so earlier line numbers stay valid. Standalone of `patch_apply` so other tools can reuse the parsing (T2-07).
- `fuzzy_match` module: `find_fuzzy_matches(haystack, needle, *, threshold=0.95)` returns ALL near-matches above threshold (verbatim short-circuit returns score=1.0). Caller decides what to do with `len(matches) != 1` — the matcher never picks one of N. Backend: rapidfuzz if installed, difflib.SequenceMatcher otherwise (no hard dependency) (T2-07).
- `Edit` (str_replace) gains opt-in `fuzzy: bool = False` and `fuzzy_threshold: float = 0.95` parameters. When verbatim count is zero AND `fuzzy=True`, falls back to `find_fuzzy_matches`. Exactly-one match -> apply at the matched slice, return line carries `(fuzzy: score=..., matched N chars)`. Zero or multiple matches -> ERROR with a recovery hint. Default behaviour unchanged (T2-07).
- `docs/reference/patch-and-fuzzy-edit.md` documents the patch_apply atomicity contract, the fuzzy fallback's safety properties, and when to use each (T2-07).
- Out-of-band tool result storage. Tool outputs exceeding `tool_result_threshold_bytes` (default 1MB) are persisted as content-addressed blobs (SHA-256 first 16 hex chars) under `~/.athena/tool_results/` and replaced in conversation history with a short `[tool_result:<hash> — <size> output stored. Use read_tool_result to access.]` handle. `PostToolUse` hooks + plugins still see the raw result; only the version that lands in `agent.messages` / session JSONL is the handle (T2-06).
- `read_tool_result` tool. Accepts the full handle, the bare 16-hex hash, or the prefixed `tool_result:<hash>` form. Pagination via `offset` + `max_bytes` lets the agent stream large blobs without re-inlining (T2-06).
- `athena cleanup-blobs` CLI subcommand. Walks every `profiles/<name>/sessions/*.jsonl`, collects every handle/bare-hash reference, deletes unreferenced blobs older than `--older-than` (default 30) days. `--dry-run` counts but doesn't delete. Recent blobs always kept regardless of reference state (T2-06).
- Config: `tool_result_threshold_bytes` (default 1_000_000) and `tool_result_storage_path` (default `~/.athena/tool_results`) (T2-06).
- `docs/reference/tool-result-storage.md` documents how the storage works, the cleanup contract, configuration, and limitations (T2-06).
- Tool-call argument sanitizer. `athena.providers.schema_sanitizer.sanitize_tool_call_args(raw, *, tool_name)` recovers malformed JSON in tool-call arguments via a 5-pass pipeline (smart quotes → ASCII; single-quoted → double-quoted only when safe; trailing commas removed; unquoted top-level keys quoted; optional `demjson3` fallback if installed). Pure function, never modifies tool names, returns `None` rather than speculate when recovery requires semantic guesses (T2-05).
- `Agent._handle_tool_call` routes string-shaped tool arguments through the sanitizer before `json.loads`, gated by `cfg.tool_call_sanitize` (default True). When a fix applies, the REPL surfaces `sanitised tool-call args for <name>: <fixes>`; unrecoverable payloads fall through to the existing fallback (empty args dict) with a WARNING log line carrying the truncated payload (T2-05).
- `tool_call_sanitize` config option (T2-05).
- `docs/reference/schema-sanitizer.md` documents the pass order, contract guards, configuration, and known limitations (T2-05).
- Automatic context compression. `athena.agent.context_compressor` summarises the middle of a long conversation while preserving head (system prompt) and tail (most recent turns by token budget) verbatim. Triggered proactively when total tokens > `context_compress_watermark` × `context_window` (default 0.75) inside the agent's tool-round loop, or manually via `/compact` (T2-04).
- Structured summary template: Resolved questions / Pending questions / Decisions made / Tool outputs of lasting value / Remaining work (NOT "Next steps" — hermes-learned model-behaviour gotcha). Synthetic summary lands as a `role="system"` message starting with the `[Compressed summary of turns N–M, ...]` marker (T2-04).
- Iterative compression: earlier summaries are detected via the marker and carried forward as input to later compactions, so information survives multiple compressions at graceful fidelity decay (T2-04).
- Prompt-injection-safe summariser preamble: tool outputs containing injected directives are framed as SOURCE MATERIAL, not as instructions the summariser should follow (T2-04).
- Cheap tool-output pre-pass: `tool`-role messages in the to-be-compressed middle are pruned to `tool_output_prune_tokens` (default 200) before being fed to the summariser (T2-04).
- `/compact` slash command upgraded to use the new compressor (replaces the prior inline summary-then-replace-all-after-system implementation) (T2-04).
- Config options: `context_compress_watermark`, `tail_protection_ratio`, `tool_output_prune_tokens`, `summary_budget_ratio`, `summary_budget_cap_tokens` (T2-04).
- `docs/reference/context-compression.md` documents the watermark, head/tail layout, summary format, iterative carry-forward, injection-defence, persistence, and configuration (T2-04).
- Error classifier and retry/backoff. `athena.providers.error_classifier.classify(exc, ...)` returns a 5-action `Classification` (`retry` / `rotate_credential` / `fallback_provider` (reserved) / `compress_context` / `abort`) plus an `ErrorClass` for logging (`network`, `server_5xx`, `rate_limit`, `timeout`, `client_4xx`, `context_length`, `stream`, `parse`, `unknown`). Pure function, no I/O (T2-03).
- `athena.providers.retry_utils.with_retry(operation, ...)` executes recovery: exponential backoff + jitter capped at `max_backoff_seconds`, `Retry-After` header respected (also capped), `on_rotate_credential` / `on_compress_context` callbacks, `RetryBudgetExceeded` on cap (T2-03).
- Every provider (Anthropic, OpenAI, OpenRouter, Nous, OpenAI-compat, Ollama) wraps its `POST` + `raise-for-status` + rate-limit-header-capture in `with_retry`. The streaming body stays outside the retry boundary (yielded chunks can't be replayed). `ExitStack.pop_all` keeps the open response alive past the retry boundary (T2-03).
- `CredentialPool.rotate_to_next(provider_name) -> Credential | None` — the pool's contribution to the `on_rotate_credential` callback (T2-03).
- `max_retries_per_turn` (default 5) and `max_backoff_seconds` (default 30.0) config options (T2-03).
- `/status` "retries this session" section displays `<provider>: N retries, M aborts` per provider (T2-03).
- `docs/reference/error-handling.md` documents the taxonomy, priority order, configuration, interrupt propagation, and implementation seam (T2-03).
- Rate-limit header tracking. `athena.providers.rate_limit_tracker.RateLimitTracker` parses the standard 12-header generic schema (Nous, OpenRouter, OpenAI) and Anthropic's `anthropic-ratelimit-*` schema (ISO-8601 resets normalised to Unix seconds). Per-(provider, credential) trackers stored on each provider; `should_throttle()` decides preemptively, `throttle_seconds()` caps the sleep at 60 s (T2-02).
- `rate_limit_throttle_threshold` config option (default 0.95). Below the threshold a provider sleeps before the next request rather than waiting to trip 429 (T2-02).
- `/status` "rate limits" section surfaces `<credential_id>: <tracker.format()>` per provider when state is available (T2-02).
- `CredentialPool.get_credential_rate_state()` returns per-(provider, credential) cooldown / 429 / fail-count view, keyed by the same `...<last-4>` suffix the providers' tracker dicts use (T2-02).
- `docs/reference/rate-limits.md` documents the schemas, configuration, provider coverage, and persistence semantics (T2-02).
- Anthropic prompt caching: 4-breakpoint `system_and_3` layout
  (last system message + last 3 non-system messages), 5m default
  TTL / 1h opt-in via `prompt_cache_ttl = "1h"`, ~60-75% input-token
  cost reduction on multi-turn sessions against Anthropic-direct,
  OpenRouter, or Nous Portal. Pure-function module
  `athena.agent.prompt_caching` with `apply_cache_markers` and
  `strip_cache_markers` (T2-01).
- `cache_strategy` and `prompt_cache_ttl` config options. Defaults:
  `"system_and_3"` and `"5m"`. Disable via `cache_strategy = "none"`
  (T2-01).
- `Stats.cache_read_tokens` and `Stats.cache_creation_tokens`
  populated from each provider call's usage chunk; surfaced in
  `/status` under the `tokens` block when non-zero (T2-01).
- `docs/reference/prompt-caching.md` documents the layout, config,
  provider coverage, persistence, and limitations (T2-01).

### Changed
- Slash-command dispatcher refactored into a uniform table. `_handle_slash`
  in `athena/__main__.py` now routes 12 inline commands (`/help`,
  `/model`, `/models`, `/tools`, `/mcp`, `/clear`, `/cost`, `/status`,
  `/save`, `/dump`, `/hooks`, `/cwd`) through `_INLINE_SLASH_HANDLERS`
  rather than an elif chain. Module-based commands (`/compact`,
  `/goal`, `/init`, `/loop`, `/memory`, `/plan`, `/resume`, `/review`,
  `/steer`) still flow through `commands.get_command` unchanged. A
  future move of the inline handlers into `athena/commands/*` is now
  a pure rename per row.

## [0.2.0] - 2026-05-19

### Added

- `athena --version` flag reading `athena.__version__` (single source of truth via `scripts/verify_version.py` CI gate) (T1-02)
- `pytest-cov>=4.1` + `pytest-timeout>=2.3` in `[project.optional-dependencies].dev`; pytest configured with `timeout=60` so hung tests fail loudly instead of wedging CI runners (T1-01)
- `CHANGELOG.md` reorganized to [Keep-a-Changelog](https://keepachangelog.com/en/1.1.0/) format with `## [Unreleased]` + `## [0.2.0]` versioned sections; `RELEASE_v0.2.0.md` at repo root summarizes the release for the GitHub Releases UI; `docs/internal/release-process.md` is the operator checklist (T1-03)
- Agent core unit test suite under `tests/agent/` covering `core.run_turn`, `core.run_until_done`, `fork.Agent.fork()`, and `auxiliary_client` — 22 tests bringing `athena.agent` coverage to 62% (T1-04)
- _(planned; lands as T1-07 ships)_ Path security module sandboxing file operations to the workspace; outside-workspace writes require explicit approval (T1-07)
- _(planned; lands as T1-08 ships)_ SSRF defense on the web tool: blocks RFC1918, link-local, loopback, and cloud-metadata IPs by default (T1-08)
- **PyPI publishing via trusted publishing** (T1-02) — `.github/workflows/publish.yml` fires on every `v*` git tag push. Builds sdist + wheel once, then routes the artifacts to TestPyPI (for `v*-rc*` / `v*-beta*` / `v*-alpha*` tags via PEP 440 pre-release detection) or real PyPI (for everything else). Uses OIDC trusted publishing — no `PYPI_API_TOKEN` secret in repo settings. Both publish jobs target named GitHub environments (`testpypi` / `pypi`) so a maintainer can optionally pin a manual-approval gate per release. `workflow_dispatch` inputs let you re-run a failed publish without burning a fresh tag.
- **Project metadata for PyPI** — `pyproject.toml` now carries `readme`, `license` ("MIT"), `license-files`, `authors`, `keywords`, `classifiers`, and `[project.urls]` (Homepage, Repository, Issues, Changelog). Distribution name is `athena-coder` because both `athena` and `athena-agent` are taken on PyPI by unrelated active projects; the import name and CLI shims (`athena`, `ocode`) are unchanged.
- **LICENSE file** — MIT, 2026.
- **`docs/release.md`** — operator runbook covering the one-time PyPI / TestPyPI trusted-publisher registration, the standard release flow (`git tag v0.2.1 && git push origin v0.2.1`), pre-release staging via `*rc*` tags, manual re-dispatch on flaky publish, and yank/rollback procedure.

- **GitHub Actions CI** (T1-01) — five workflows running on every push to `master` and every PR: `tests.yml` (pytest matrix on Python 3.10/3.11/3.12/3.13), `lint.yml` (ruff check + ruff format --check, with mypy strict landing as advisory until the T1-04 cleanup), `coverage.yml` (pytest --cov; gates the build at 60%, current floor is 75%), `osv-scanner.yml` (google/osv-scanner-action reusable workflow on push + weekly Monday 08:00 UTC), `supply-chain.yml` (pip-audit, advisory-only for now). Each workflow has a `concurrency` group that cancels in-progress runs on new pushes to the same PR. Test workflow uses `fail-fast: false` so one Python version's failure doesn't hide others. No secrets referenced.
- **Dependabot** (`.github/dependabot.yml`) — weekly pip + github-actions bumps. Dev-tool updates (ruff, mypy, pytest*) grouped into one PR so unrelated bumps don't flood the queue. Limit 5 open PRs at a time.
- **README CI badges** — five SVG status badges immediately under the H1 linking to each workflow page on Actions.
- `pytest-cov>=4.1` in `[project.optional-dependencies].dev`.

- **Mechanical safety subsystem (Phase 17)** — content-addressed snapshot store, mutation audit log, ContextVar-scoped approval guard, word-boundary shell allowlist with denylist. Every agent-driven mutation (skill_manage, memory writes, background-fork tool calls) now wraps in a snapshot+audit context so any change is byte-exact rollback-able and forensically attributable. Snapshot tarballs are stored under `~/.athena/snapshots/YYYY/MM/DD/<ts>-<sha[:12]>-<origin>.tar.gz` with a sidecar JSON carrying `snapshot_id`, `write_origin`, `session_id`, `tool_name`, `tool_call_id`, `paths`, `created_at`, `athena_version`, `parent_session_id`, and pin state. Identical pre-states under the same write_origin at the same second collapse to one tarball (content addressing).
- **Append-only mutation audit log** at `~/.athena/audit/mutations-YYYY-MM.jsonl` — one compact JSON line per mutation with `timestamp`, `write_origin`, `session_id`, `parent_session_id`, `tool_name`, `tool_call_id`, `path`, `snapshot_id`, `sha_before`, `sha_after`, `byte_delta`. `threading.Lock` serialises appends; monthly rollover; existing files preserved across appends. (Phase 17.4)
- **ContextVar-scoped approval guard** (`athena.safety.approval_guard`) — foreground prompts cache approvals per-resource for the lifetime of the ContextVar scope. `Agent.fork()` now installs `scope_fresh_approvals()` so a background fork starts with an empty grant cache and any `request_approval` call from background origin raises `ApprovalDeniedInBackground` (unless the resource is explicitly marked `auto_approve_in_background=True`). The cache is never consulted from background even if a foreground grant for the same resource exists. (Phase 17.2)
- **Word-boundary shell policy** (`athena.safety.shell_policy`) replaces the previous substring allowlist. Allowlist entries compile to `^<escaped-entry>\b` and match the binary token after stripping environment-variable assignments — so `git` no longer matches `gitlab-cli` or `.git/hooks/...`. Always-on denylist covers `rm -rf` of system roots, `dd of=/dev/...`, `mkfs.*`, fork bomb, `chmod 777 /`, `sudo rm -rf`, `curl | sh`, `wget | sh`, and block-device redirects. Extendable via `cfg.bash_extra_denylist`. The Bash tool calls `evaluate_denylist_only()` before execution and returns a `BLOCKED by shell policy: <reason>` tool error on match. (Phase 17.3)
- **Rollback CLI** — `athena snapshot {list, show, pin, unpin, prune}` for store inspection; `athena skill {diff, rollback} <name>` and `athena memory {diff, rollback} <name>` for restoring a single resource. Rollbacks are themselves audited (the post-restore record carries `tool_name="skill_rollback"` / `"memory_rollback"`, sha_before/sha_after inverting the original change). `--to <snapshot_id>` selects a specific snapshot; `-y` skips the interactive confirm. (Phase 17.6)
- **Per-profile snapshot/audit singletons** (`athena.safety.context`) — `get_snapshot_store(profile_dir)` and `get_audit_log(profile_dir)` lazily construct one store per profile root, keyed by resolved path so two concurrent profiles in the same process see their own directories. Re-resolves home each call so tests that monkeypatch `Path.home()` get tmp-scoped stores. (Phase 17.5)
- **CI grep guard** at `tests/safety/test_no_raw_writes.py` — walks `athena/` and fails the build if any module that isn't on the snapshot+audit allowlist calls `Path.write_text`, `Path.write_bytes`, `open(..., "w"|"a")`, `shutil.copy*`, or `shutil.rmtree`. The allowlist is frozen as of Phase 17.5; new write sites must route through `athena.safety.mutation.snapshot_and_record` or get an explicit one-line justification in the allowlist. (Phase 17.5)
- `cfg.bash_extra_denylist: list[str]` config field for appending regex patterns to the default shell denylist. (Phase 17.3)
- `athena.safety.mutation.snapshot_and_record(paths, *, tool_name, ...)` — combined context manager that takes the pre-state snapshot, captures `sha_before`/byte size per path, yields a `MutationContext`, and lets the caller emit the matching `MutationRecord` via `ctx.record(path)` once the mutation is complete. Used at every skill/memory mutation site. (Phase 17.5)

- **Observability plugin (bundled, opt-in)** — OpenTelemetry tracing + metrics + JSON structured logging under `athena/plugins/bundled/observability/`. Activate with `athena plugins enable observability`. Heavy OTel deps live behind `pip install -e ".[observability]"` so headless installs stay light. Spans: `athena.session` brackets every session; `athena.tool_call.<name>` per tool dispatch with arguments redacted; latency + count metrics tagged by `tool_name`. When `[plugins.observability].otlp_endpoint` is set, exports via OTLP/HTTP instead of the default stderr console exporter. Plugin loads and no-ops cleanly when the optional deps aren't installed. (Phase 16)
- **PII redaction** for span attributes — pattern table for OpenAI / Anthropic / Google / GitHub (classic + fine-grained `ghu`/`ghs`/`ghr`/`gho`) / Bearer / Slack tokens; constant-time replacement with `<redacted>`; length truncation at 200 chars with `…` suffix; defensive handling of non-dict / None inputs. (Phase 16)
- **`/status` slash command + `athena status` CLI** — read-only view of live counters (per-tool histogram, fork / review / curator counts, prompt/completion tokens, elapsed). Snapshot is atomically written to `<profile>/.status.json` on every turn end; the CLI reads it from any terminal and is profile-aware (`--profile` flag honored). The renderer (`render_status`) is shared between the two surfaces so they stay byte-identical. (Phase 16)
- **Benchmark harness** at `scripts/bench/` — `runner.py` discovers any module exporting `run() -> dict`, runs them, writes JSON results, optionally compares against a baseline and flags regressions over a configurable threshold (default 10%). Ships with `tool_call_latency` (Read tool dispatch) and `skill_discovery` (100-skill catalog walk) reproducible benches. Initial baseline committed at `tests/fixtures/benchmarks/baselines/main.json`. (Phase 16)
- `[observability]` optional extras group (`opentelemetry-sdk`, `opentelemetry-exporter-otlp-proto-http`, `python-json-logger`). (Phase 16)
- `Stats.tool_call_counts`, `Stats.fork_count`, `Stats.review_fired_count`, `Stats.curator_run_count`, `Stats.record_tool_call`, `Stats.to_snapshot`. `Agent.write_status_snapshot()` atomic writer fired at every `_fire_stop`. (Phase 16)

- **Webhook HTTP listener** — `aiohttp`-backed at `http://<host>:<port>/webhook/<id>` hosted inside the gateway daemon when `[gateway.webhooks].enabled = true`. Per-request flow: subscription lookup (404 on miss/disabled) → auth (HMAC-SHA256 or Bearer, 401 on fail; `auth_type="none"` for trusted networks) → idempotency cache (200 no-op on duplicate `X-Webhook-Idempotency-Key` within 10 min TTL) → per-webhook 60s sliding-window rate limit (429 over) → JSON body parse (non-JSON wraps with `_raw_body` or `_raw_body_base64`) → async `asyncio.create_task` dispatch → 202 returned immediately. `GET /health` for operator probes. (Phase 15)
- **Webhook→skill and webhook→prompt-template bindings** — skill binding fires a `Run the <skill> skill` template with payload + filtered headers (X-GitHub-Event etc.) as JSON code blocks; auth headers stripped. Prompt template binding substitutes `{{ payload }}` / `{{ headers }}` (whitespace-tolerant). Each fire spawns a one-off Agent (stateless — no session pooling). (Phase 15)
- **Delivery routing** for webhook responses: `log` / `none` / `file:<path>` (append with `--- webhook <id> <iso-ts> ---` delimiters, parent dirs auto-created) / `gateway://<platform>/<chat_id>` (routes through the running daemon's adapter). Missing daemon / missing adapter / malformed target / send failure all log without raising. (Phase 15)
- **Idempotency keys + sliding-window rate limiting** per webhook. Idempotency cache keyed on `(webhook_id, key)` so the same idempotency value across different webhooks doesn't collide; lazy-purge implementation, thread-safe under contention. Rate limiter per id so one noisy webhook hitting its budget doesn't starve others. (Phase 15)
- **HMAC compare uses `hmac.compare_digest`** (constant-time). Accepts both `X-Webhook-Signature: <hex>` (athena native) and `X-Hub-Signature-256: sha256=<hex>` (GitHub) so the same URL serves a GitHub repo and a custom client without operator-side branching. (Phase 15)
- `athena webhook {add, list, info, remove, enable, disable, test}` CLI. `add` auto-generates a 32-byte hex secret when `--secret` is omitted (HMAC/Bearer modes). `test <id>` POSTs a synthetic (or `--payload-file`-supplied) payload to the configured URL, signed per the webhook's `auth_type`; surfaces the HTTP response so the operator can verify auth + dispatch work end-to-end before pointing a real source at the URL. (Phase 15)
- `aiohttp>=3.9` added to `[gateway]` extras explicitly. (Phase 15)
- `[gateway.webhooks]` config section: `enabled` (default False — opt-in), `host` (default 127.0.0.1), `port` (default 4747). (Phase 15)

- **Multi-profile isolation** under `~/.athena/profiles/<name>/`. Each profile is its own configuration, skill set, memory, session store, cron schedule, gateway routes, MCP server set, and goal — `personal` and `work` run side-by-side without crosstalk. Sessions in one profile can't be searched from another; memory and skills the same. (Phase 14)
- `athena profile {list, show, create, switch, delete, rename}` CLI — `create` supports `--copy-from <name>` to clone an existing profile's contents wholesale; `delete` requires a confirm-token equal to the profile name (anti-typo); `default` is protected against delete and rename. (Phase 14)
- `--profile <name>` global flag + `ATHENA_PROFILE` env var (legacy `OCODE_PROFILE` honored). Resolution precedence: CLI flag > env var > `~/.athena/active_profile` file (set by `athena profile switch`) > `cfg.profile` from config.toml > hardcoded `"default"`. Invalid names at any step fall through to the next source rather than crashing. (Phase 14)
- One-time migration of legacy single-profile layout — items at the top of `~/.athena/` (skills, memory, sessions, sessions.db, mcp.json, goal.txt, cron.db, gateway.db, training_state.json, labels/, datasets/, models/, config.toml) move into `~/.athena/profiles/default/` on the first invocation after upgrade. Per-item failure isolation (one bad move logs + continues with the rest). Idempotent (the presence of `profiles/` after a successful run permanently short-circuits the check). (Phase 14)
- Strict profile name validation: lowercase alphanumerics + `_` + `-`, must start with an alphanumeric, max 64 chars. Rejects path traversal, shell-special chars, uppercase, leading hyphens/underscores. (Phase 14)

- **ACP server for IDE integration** — `athena acp serve` exposes athena over JSON-RPC 2.0 stdio, the Agent Client Protocol that Zed natively supports (VS Code / JetBrains have community adapters). `ACPServer` handles framing, request/notification dispatch with per-message asyncio.Task parallelism, and client-bound `send_request` with pending-future tracking + timeout. Shutdown drains in-flight dispatches with a 5s grace then cancels, and resolves every pending client-bound future with `ACPError` so callers in `permission_request` unwind rather than hang. (Phase 13)
- `StreamingSender` — typed wrapper around `send_notification` for the ACP streaming primitives: `text_block_start` / `text_delta` / `text_block_stop`, `tool_call_start` / `tool_call_result`, `permission_request` (returns `"deny"` on any failure path: timeout, IDE error, malformed response, unknown decision), `turn_started` / `turn_completed`. (Phase 13)
- ACP method handlers via `register(server, agent_factory)`: `initialize` (returns capabilities), `session/new` (mint or use caller-supplied id, instantiate Agent in a worker thread), `session/end` (close in thread), `session/send_message` (run agent in `asyncio.to_thread`, stream final response, surface every tool_call_trace entry as a `tool_use` block, bridge approvals via `run_coroutine_threadsafe` into the loop), `session/cancel` (sets `Agent.cancel_pending`), `session/slash_command`, `models/list`. (Phase 13)
- ACP slash commands: `/steer`, `/queue` (+ `/queue clear`), `/goal` (set / show / clear) via the same `GLOBAL_STEER_QUEUE` and goal-invariant primitives the CLI's REPL slash commands use. (Phase 13)
- `Agent.cancel_pending` flag — checked between tool rounds in `_run_turn_inner`; cleared at the start of every new `run_turn`. Allows ACP `session/cancel` to abort an in-flight turn at the next safe boundary. (Phase 13)
- `athena acp serve` / `athena acp install-zed` CLI — `serve` is what the IDE spawns over stdio; `install-zed` prints the Zed `agent_servers` settings.json snippet. (Phase 13)

- **MCP HTTP/SSE transport** — `SSETransport` runs a synchronous façade over an async SSE listener + POST `/messages` channel. Auto-handles the legacy MCP SSE protocol's `event: endpoint` frame, dispatches JSON-RPC responses by id to pending sync waiters via a daemon-thread event loop, reconnects with exponential backoff (1s → 30s) on stream failure, refreshes the OAuth token in place on 401. Public API mirrors `MCPStdioClient` so the rest of athena's MCP code is transport-agnostic. (Phase 12)
- **OAuth 2.1 PKCE flow** (`athena/mcp/oauth.py`) — code verifier + S256 challenge, local one-shot HTTP callback on `127.0.0.1:<free-port>`, state validation for CSRF defense, code-for-token exchange + refresh-token grant with preservation of the prior refresh_token / scope when the provider omits them. `webbrowser.open` for the auth URL with stderr fallback. Token persistence at `~/.athena/mcp_tokens/<server_id>.json` with atomic writes (tempfile + os.replace) and POSIX mode 0600. `needs_refresh` proactive 2-minute grace window. Path-traversal-safe server_id handling. (Phase 12)
- **Transport resolver** (`athena/mcp/transport_resolver.py`) — `open_transport(server_id, config)` dispatches on the `transport` field of an mcp.json entry: `stdio` (default; constructs `MCPStdioClient`), `sse` / `http` / `http+sse` (constructs `SSETransport`, with optional OAuth config parsed from `config["oauth"]`). Loader refactored to use the resolver — existing stdio entries unaffected. (Phase 12)
- `athena mcp {list, auth, token-status, revoke, test}` CLI — operator surface for managing MCP HTTP/SSE servers. `auth` runs the OAuth flow; `token-status` humanizes expiry per server; `revoke` deletes stored tokens; `test` initializes one server and dumps its tool catalog for first-run validation. (Phase 12)

- `SignalAdapter` — Signal via signal-cli-rest-api (Docker-deployable bridge). HTTP-only; no new Python dep. Long-poll `/v1/receive`, post to `/v2/send`, base64 attachments. Text-reply approvals via `/allow` / `/deny` keyed on the sender's UUID. Exponential reconnect backoff. (Phase 11)
- `IMessageAdapter` — iMessage via BlueBubbles Server (macOS-host bridge). Socket.IO inbound via `python-socketio[asyncio_client]`, REST outbound. Handles chatGuid / handle.address normalization for both DM and group threads. Eager attachment download. Text-reply approvals. (Phase 11)
- `MatrixAdapter` — Matrix via `matrix-nio`. Reaction-based approval UI (✅ Allow / ✖ Deny) with the prompt event_id tracked back to the request id; pre-seeded reactions so users see them as tappable. E2EE supported when `matrix-nio[e2e]` (libolm) is installed; gracefully falls back to unencrypted-only otherwise. Per-profile `matrix_store/` for key persistence. Native typing notifications. (Phase 11)
- `EmailAdapter` — IMAP IDLE + SMTP via `aioimaplib` + `aiosmtplib`. text/plain preferred over text/html; HTML flattened via `beautifulsoup4` with scripts and styles stripped. Threading via `In-Reply-To` and `References` headers tracked per sender. Optional `allowed_senders` allowlist (canonical-address comparison). Approvals via `/allow` / `/deny` in the reply body. (Phase 11)
- `TextApprovalState` mixin — shared keyed (user_id → request_id) pending-approval store + `parse_approval_decision` token parser (`/allow`, `/deny`, ✅, ✖, yes, no, single-token only) for the three text-only platforms (Signal, iMessage, Email). (Phase 11)
- `athena gateway run` CLI extended to recognize all four new platforms; per-platform required-key validation reports missing settings with a clear message and skips the adapter without taking the gateway down. (Phase 11)
- `[gateway]` extras group extended: `python-socketio[asyncio_client]>=5.10` (iMessage), `matrix-nio[e2e]>=0.24` (Matrix), `aioimaplib>=2.0`, `aiosmtplib>=3.0`, `beautifulsoup4>=4.12` (Email). (Phase 11)
- `GatewayDaemon` — single asyncio-based daemon hosting platform adapters, exposing the agent to messaging platforms. Owns the session router, agent pool, approval router, and continuity manager. ``athena gateway run`` boots it in foreground. (Phase 10)
- `GatewayAdapter` base class with Hermes-faithful reliability primitives: `_active_sessions: dict[asyncio.Event]` guards + `_session_tasks: dict[asyncio.Task]` owner map, race-free guard install before `create_task`, stale-lock self-heal via `task.done()` (Hermes issue #11016), single `_pending_messages` slot with text-merging (issue #4469), interrupt-on-text vs queue-on-photo policy, bypass-command routing (`/stop|/new|/reset|/approve|/deny|/status|/restart`), command-scoped guard handoff preserving response ordering (Hermes PR #4926). (Phase 10)
- `SessionRouter` — SQLite-backed `gateway_routes` table at `<profile>/gateway.db`. Sticky `(platform, chat_id, user_id) → session_id` routing; routes persist across daemon restarts; `last_seen_at` bumps on every reuse. (Phase 10)
- `AgentPool` — async bounded LRU cache of warm Agents. Per-session instantiation locks ensure concurrent `get()` for the same id share one factory invocation; concurrent gets for different ids run in parallel. Eviction calls `Agent.close()`. (Phase 10)
- `ApprovalRouter` — async + sync bridge for dangerous-tool approvals. `request_async` (loop side) returns the user's decision via `asyncio.Future`; `request_sync` (agent worker thread side) submits via `run_coroutine_threadsafe` and blocks on `concurrent.futures.Future`. Per-platform renderer dispatch via `register_platform_renderer(platform, renderer)`. 300s default timeout with `"deny"` as safe fallback. `cancel_all` unblocks every waiter on shutdown. (Phase 10)
- `ContinuityManager` — bulk cross-platform user linking. `link_canonical(canonical_id, {platform: pid, ...})` is atomic; `unlink_canonical` drops all bindings for a user. Routing path uses these so a Telegram + Slack pair linked to the same canonical user lands on one session. (Phase 10)
- `TelegramAdapter` (aiogram>=3) — long-polling, inline-keyboard approval buttons, eager attachment download to `<profile>/gateway_attachments/telegram/<chat>/`, Markdown body rendering. (Phase 10)
- `SlackAdapter` (slack-sdk>=3.27 Socket Mode) — no public HTTPS endpoint required, Block Kit primary/danger approval buttons, bot-self filtering via `auth.test`-discovered `_bot_user_id`, file download via httpx with the bot token in Authorization. (Phase 10)
- `DiscordAdapter` (discord.py>=2.4) — `discord.ui.View` approval buttons (callbacks bound to methods, not opaque strings), `Intents.message_content` enabled, `/athena` slash command via `app_commands.CommandTree`, `channel.typing()` for indicators. (Phase 10)
- Gateway agent factory (`build_agent_factory`) — pool factory that constructs an Agent bound to the daemon's shared SessionStore, replays the session's JSONL into `Agent.messages`, returns warm. (Phase 10)
- `Agent.resume_session_id` constructor kwarg + `Agent.load_history_from_session(session_id)` method for gateway resume. (Phase 10)
- `_process_message_background` impl — pool warm, typing-heartbeat task, gateway-bridge approval callback installed via ContextVar (copied into the worker thread by `asyncio.to_thread`), `agent.run_until_done` on the worker, final response chunked on paragraph/sentence/word boundaries and sent back, pending drain into a fresh task. (Phase 10)
- In-process `gateway.registry` — keyed by profile, populated on `daemon.start`. Used by cron's gateway-delivery path to find the daemon without IPC. (Phase 10)
- `athena gateway {run, routes, link, unlink, canonical-users}` CLI subcommands. (Phase 10)
- `[gateway]` optional dependencies group (aiogram, slack-sdk, discord.py) so headless installs don't pull SDKs. (Phase 10)
- `GatewayConfig` — `max_warm_agents` (50 default), `continuity` (off default), `platforms` per-adapter credentials dict. (Phase 10)

- Per-(provider, model_glob) tool-call parser registry under `athena/providers/parsers/` — first-match-wins with `register()` and provider-default fallthrough via `register_default()`; `resolve_parser(provider, model)` returns the function `Provider.parse_tool_calls` delegates to (Phase 9)
- Native-format parsers: `anthropic_xml` (content-block array → text + tool_use), `openai_function` (legacy `function_call` for gpt-3.5*/gpt-4-0613), `openai_tools` (current `tool_calls` for gpt-4*/4o/o1/o3/o4 and every OpenAI-compatible service), `ollama_native` (provider-default for Ollama) (Phase 9)
- Content-leak recovery parsers: `qwen_xml_leakage` (`<tool_call>{...}</tool_call>` XML), `harmony` (GPT-OSS three-channel analysis/commentary/final), `code_fenced_json` (` ```json ``` ` blocks), `json_block` (whole-content bare JSON) — model-specific globs route Qwen and GPT-OSS variants to the right parser regardless of host provider (Phase 9)
- `fallback_parser` — last-resort native `tool_calls` extractor that never raises and tolerates malformed JSON args, non-dict tool calls, missing names, and non-string IDs (Phase 9)
- Agent loop recovers leaked tool calls from streamed text via `_recover_tool_calls_from_text`, which calls `provider.parse_tool_calls` first and falls through to the generic recovery only on hard failure (Phase 9)
- Parser fixture corpus at `tests/fixtures/tool_call_outputs/<provider>/<model>/{normal,content_leakage,malformed}/` + parametrized corpus-driven test asserting every parser reproduces the captured `.expected.json` (Phase 9)
- Fuzz test: 12 (provider, model) combos × 1000 random strings + 200 random nested raw_response dicts, fixed seed, asserting parsers never raise and always return well-shaped output (Phase 9)
- `Provider` ABC, `StreamChunk` shape, and name-keyed registry under `athena/providers/` (Phase 8)
- `OllamaProvider` (replaces `OllamaClient`) on the new ABC; `ollama_client.py` is now a back-compat shim (Phase 8)
- `AnthropicProvider`, `OpenAIProvider`, `GoogleProvider` first-class providers with respx-mocked SSE parsing (Phase 8)
- `OpenAICompatProvider`, `OpenRouterProvider`, `NousProvider` — thin OpenAI-compat subclasses for vLLM/llama.cpp/openrouter.ai/portal.nousresearch.com (Phase 8)
- `CredentialPool` at `~/.athena/credentials.json` — per-provider round-robin with cooldown on 429, atomic JSON persistence, thread-safe, redacted listing (Phase 8)
- `resolve_provider(model, cfg, pool)` runtime resolver — prefix routing (anthropic/ openai/ google/ openrouter/ nous/), gemini- bare prefix, host:port/model → openai_compat, default ollama (Phase 8)
- Provider fallback chain via `providers.<primary>.fallback = ["openrouter", ...]` — resolver walks the chain when the primary has no credential or every credential is in 429 cooldown. Entries can be bare provider names (model string passes through) or `{provider, model}` dicts for cases where the model name needs to change too (Phase 8)
- `athena providers {list,test,add-key,remove-key,models}` CLI (Phase 8)
- `list_models()` on every hosted provider — `athena providers models <name>` queries the live catalog so users don't guess at stale model names (Phase 8)
- `respx>=0.21` added to `[dev]` extras for httpx mocking in provider tests (Phase 8)
- Trajectory extraction + auto-classifier (`good` / `bad` / `preference_pair` / `unreviewed`) (Phase 7)
- Trajectory extraction + auto-classifier (`good` / `bad` / `preference_pair` / `unreviewed`) (Phase 7)
- SFT and DPO dataset construction in JSONL with the qwen-coder chat template (Phase 7)
- Interactive trajectory review TUI with resume; labels persist to `<profile_dir>/labels/<session_id>.json` (Phase 7)
- Training runner wrapping `transform/scripts/train_lora.py` + new `train_dpo.py` (Phase 7)
- `transform/scripts/train_dpo.py` companion to the existing LoRA script (Phase 7)
- Ollama deployment helpers — Modelfile write + `ollama create`, `ollama list` parsing, model switch (Phase 7)
- `athena train {review,build-dataset,run,status}` — closed training loop CLI (Phase 7)
- `athena model {list,switch,info}` (Phase 7)
- `~/.athena/training_state.json` records every training run for `athena train status` (Phase 7)
- `[project.optional-dependencies.train]` extras group (trl, peft, transformers, datasets, accelerate, bitsandbytes) (Phase 7)
- APScheduler-backed cron with `agent` and `watchdog` modes (Phase 6)
- Cron output delivery to `log`, `file:<path>`, or `gateway://...` (gateway stub until Phase 10) (Phase 6)
- `/steer` and `/queue` in-flight redirection — synthetic user messages drained before each prompt, FIFO order (Phase 6)
- `/goal` Ralph-loop invariant persisted at `<profile_dir>/goal.txt` and injected at the end of every system prompt rebuild (Phase 6)
- `athena cron {add,list,remove,enable,disable,run-now,logs,daemon}` CLI (Phase 6)
- `GLOBAL_STEER_QUEUE` thread-safe singleton for cross-thread steer pushes (Phase 6)
- Plugin API with lifecycle hooks (Phase 5)
- `MemoryProvider` ABC; `BuiltinFileProvider` as default (Phase 5)
- Bundled `shell_audit` plugin (Phase 5)
- `athena plugins {list,enable,disable,info}` CLI (Phase 5)
- `Config.plugins` field + `~/.athena/plugins_state.json` for machine-managed enable state (Phase 5)
- Toolset-scoped tool registry (Phase 0)
- ContextVar provenance tracking (Phase 0)
- Agent.fork() as a core primitive (Phase 0)
- Per-thread approval callback for safe fork execution (Phase 0)
- tool check_fn for capability-based tool advertisement (Phase 0)
- agentskills.io-compliant skill format (Phase 1)
- Skill state machine (active/stale/archived) (Phase 1)
- Pinning and archive directory (Phase 1)
- Class-level umbrella architecture (references/templates/scripts) (Phase 1)
- skills_list, skill_view, skill_manage tools (Phase 1)
- athena import-from-hermes for Hermes Agent migration (Phase 1)
- Progressive disclosure of skill catalog in system prompt (Phase 1)
- SessionStore with JSONL persistence and SQLite FTS5 mirror (Phase 2)
- SearchSessions tool for recall (Phase 2)
- athena sessions {list,browse,search,purge} CLI (Phase 2)
- athena reindex command for rebuilding the session FTS5 index (Phase 2)
- Per-profile root at ~/.athena/profiles/<profile>/ (Phase 2)
- Complete Agent.fork() with auxiliary client and stdout/stderr capture (Phase 3)
- Post-write delta lint for .py / .pyi / .json / .yaml / .yml / .toml (Phase 3)
- ForkResult.actions extracted from structured tool results (Phase 3)
- Parent/child session lineage with fork-tree CLI browse (Phase 3)
- Per-turn background review fork (Phase 4)
- Curator with structured YAML output (Phase 4)
- Curator dry-run mode and run reports (run.json + REPORT.md) (Phase 4)
- Deterministic lifecycle transitions at session start (Phase 4)
- .curator_state persistence (Phase 4)
- Provenance enforcement in skill_manage by write_origin (Phase 4)
- athena curator {run, status, pause, resume, inspect-last} CLI (Phase 4)

### Changed
- README install instructions point at `pip install athena-coder` from PyPI (T1-02.7); source-install path retained for development under a separate sub-section
- Documentation reconciled with shipped code: MCP transport, sub-agent capability, phase-status markers, Modelfile system prompt (T1-05)

### Removed
- Legacy `ocode` console-script alias from `pyproject.toml` `[project.scripts]`. Update any shell aliases or scripts that called `ocode` to call `athena` instead. (T1-02)

### Security
- Credential file writes use `os.open(O_EXCL, 0o600)` atomically via the new `athena.safety.secure_files` module (T1-06)
- TOCTOU windows closed across `credential_pool.py`, `mcp/oauth.py`, and `migration/config_translator.py` (T1-06)
- `~/.athena/` subdirectories holding secret material created at `0o700` via `ensure_secure_dir`; existing wider-mode directories emit a `WARNING` rather than being silently re-chmodded (T1-06)
- Path security module sandboxes `file_ops.py` to the workspace by default; outside-workspace reads and writes require explicit approval (`(tool_name, args) -> "allow"|"deny"` callback). Absolute-deny patterns refuse `/proc/*/mem`, `/proc/kcore`, `/dev/{mem,kmem,port}`, `/dev/sd[a-z]*`, `/dev/nvme*`, `/sys/firmware/efi/efivars/*`, and the Windows raw-device equivalents regardless of approval. (T1-07)
- `athena.safety.allow_external()` context manager for legitimate outside-workspace use (test fixtures, foreground tool implementations operating on user-supplied absolute paths) (T1-07)
- Forks re-pin `path_security` workspace inside their daemon thread; combined with `AUTO_DENY` they cannot escape the parent's workspace (T1-07)
- SSRF defense on the web tool: `WebFetch` and SearxNG search backends route through `athena.safety.url_safety.validate_url`. Block list covers RFC1918 (10/8, 172.16/12, 192.168/16), loopback (127/8, ::1), link-local (169.254/16, fe80::/10), carrier-grade NAT (100.64/10), AWS IMDSv2 IPv6 metadata, IPv6 ULA (fc00::/7), multicast (224/4, ff00::/8), and IPv4-mapped IPv6 variants. (T1-08)
- Redirect `Location:` headers re-validated through `validate_url` via an httpx event hook (T1-08)
- `athena.safety.allow_external_urls()` context manager for legitimate private-IP fetches (self-hosted SearxNG, test HTTP servers) (T1-08)


- Project renamed `ocode` → `athena`. The Python package, the `athena`
  CLI command, `~/.athena/` config home, and the `ATHENA.md` project
  context file all move together. The legacy `ocode` CLI entry stays
  as an alias for one release; `~/.athena/` falls back to reading
  `~/.ocode/` when the new home doesn't exist; `ATHENA.md` falls back
  to `OCODE.md`; `OCODE_*` env vars (`MODEL`, `SESSIONS_FSYNC`,
  `SEARCH_BACKEND`, `SEARXNG_URL`, `WEB_TIMEOUT`, `WEB_USER_AGENT`,
  `HOOK_EVENT`, `TOOL_NAME`) are still honored alongside the canonical
  `ATHENA_*`.

- Legacy `~/.athena/{skills, memory, sessions, ...}` now live under `~/.athena/profiles/default/`. Auto-migrated on first run; no operator action required. `credentials.json`, `mcp_tokens/`, `plugins/`, `plugins_state.json`, `logs/` deliberately stay at `~/.athena/` (user-scope, not profile-scope) — a user's API key is a user resource, and cooldown state shared globally mirrors upstream rate-limit reality more accurately than per-profile isolation would. (Phase 14)
- Forks inherit the parent's `profile` field via `dataclasses.replace`. The invariant is now locked in by a regression test (`test_fork_inherits_parent_profile`). (Phase 14)

- `cron.delivery` — `gateway://<platform>/<chat_id>` delivery target now dispatches through the running `GatewayDaemon`'s adapter via the in-process registry. Falls back to log on missing daemon / missing adapter / loop not running. (Phase 10)

- `tomli-w>=1.0` added as a runtime dep (used by `athena model switch`) (Phase 7)
- `apscheduler>=3.10` + `sqlalchemy>=2.0` added as runtime dependencies (Phase 6)
- Agent loop drains pending steers via `_inject_pending_steers()` before each user prompt (Phase 6)
- `build_system_prompt` accepts an optional `goal` parameter and appends the invariant block last (Phase 6)
- `athena/memory.py` refactored into the `athena/memory/` package; legacy workspace-keyed API preserved (Phase 5)
- Agent loop fires plugin lifecycle hooks (`on_session_start`, `on_user_message`, `pre_tool_call`, `post_tool_call`, `on_assistant_message`, `on_session_end`); the existing settings.json hook system in `athena/hooks.py` is unchanged and runs alongside (Phase 5)
- Sub-agent dispatch tool now calls Agent.fork() under the hood
- athena/agent.py split into athena/agent/{core,fork}.py
- athena/skills/ (slash-command handlers) renamed to athena/commands/ to free
  the name for the new file-based skill format
- Agent.run_turn now persists every user / assistant / tool message to the
  session store (JSONL + SQLite FTS5)
- Hermes sessions_importer drives SessionStore so imports land in
  `athena sessions list` and FTS5 search

### Fixed

- **Anthropic message translation** — provider was sending Athena's Ollama-shaped message log (`role:"tool"` for results, `tool_calls` array on assistant turns) straight to `/v1/messages`, which 400'd with `"Unexpected role 'tool'"`. New `_translate_messages` pass converts to Anthropic's content-block shape: assistant turns become `[{"type":"text"},{"type":"tool_use",...}]`, consecutive tool results coalesce into one `role:"user"` message with `tool_result` blocks, missing `tool_use_id` values synthesize paired ids between adjacent call/result turns. Empty assistant turns are dropped entirely (Anthropic rejects empty content arrays AND empty-text blocks). 10 translation tests pin the contract. Surfaced driving Sonnet 4.6 through the calculator e2e and the curator dogfood; same task that took qwen3-coder:30b 130s now completes in 10s.
- **Cross-provider token usage accounting** — `Agent.run_turn` hardcoded Ollama's field names (`prompt_eval_count` / `eval_count`) when accumulating into `Stats`, silently zeroing the counters for every hosted provider whose usage chunk uses the OpenAI convention (`prompt_tokens` / `completion_tokens`). Accept both shapes so `athena status`, the `/cost` slash command, the audit-log cost attribution, and the closed training loop's session-efficiency scoring all keep working without per-provider branching at the call site. Caught by `athena status` reporting `0/0/0` after a Sonnet run that consumed real tokens.
- **Connect-side `OLLAMA_HOST=0.0.0.0` rewrite** — `0.0.0.0` is a server-side *bind* address (Ollama documents it for `ollama serve`); users routinely copy the same env into their client shell and hit `WinError 10049 — requested address is not valid in its context` (Windows) or `EADDRNOTAVAIL` (Linux). `_normalize_ollama_host` now rewrites `0.0.0.0` → `127.0.0.1` and `[::]` → `[::1]` while preserving scheme, port, and pass-through for already-valid hosts. 10 parametrized tests cover IPv4/IPv6 wildcards, scheme-less inputs, and remote-host passthrough.
- **cp1252-safe console output** — `ui.error`'s `✗` glyph (and `↳`, `▰`, etc.) crashed `console.print` with `UnicodeEncodeError` on legacy Windows consoles whose default stdout encoding is cp1252. `athena.ui` now reconfigures `stdout`/`stderr` with `errors="replace"` on win32 and falls back to ASCII variants of the colored unicode symbols when Rich detects the legacy Windows console, so unrepresentable characters degrade to `?` instead of taking down the process.
- **TOML loader merges into typed dataclass fields** — the naive `setattr` loop overwrote `Config.gateway` (a `GatewayConfig` dataclass) with a plain dict when the user had a `[gateway.platforms.telegram]` table; the gateway daemon then crashed on boot with `AttributeError: 'dict' object has no attribute 'continuity'`. `_assign_field` merges TOML tables into the existing dataclass instance field-by-field (recursing one level for nested dataclasses like `GatewayConfig.webhooks`), so unspecified options keep their defaults and downstream code keeps its typed attribute access. 4 new tests pin the merge behavior incl. partial override + unknown-key tolerance.
- **Tool-call parser handles "preamble + naked JSON"** — qwen2.5-coder:14b (and similar coder models) routinely emit a one-line natural-language preamble followed by `{"name": "...", "arguments": {...}}` with no `<tool_call>` wrapper or code fence. `_extract_text_tool_calls` previously handled `<tool_call>` tags, code fences, and whole-text JSON, but missed this exact shape and silently dropped the call. New brace-balanced scan picks up the first balanced `{...}` and accepts it when `_normalize_tool_call` recognizes the shape (name + arguments). Conservative: only matches objects with the tool-call shape, so prose containing literal JSON examples doesn't trip it.
- **Agent.__init__ strips routing prefixes when a provider is passed** — when `fork()` built an auxiliary client and reused the parent's prefixed model name (e.g. `"anthropic/claude-sonnet-4-6"`), the prefix went on the wire and Anthropic answered 404 not_found_error. Apply `_bare_model` unconditionally during `Agent.__init__` so the agent's on-the-wire model is always normalized, whether the provider was resolved internally or passed in.
- **Curator headless dogfood path** — `athena curator run --force` against a hosted provider surfaced three additional bugs: the `SimpleNamespace` agent shell was missing `.messages` (fork crashed with AttributeError), the curator fork was called with an empty `user_prompt` so hosted providers responded with empty content (rejected as malformed YAML), and the orchestrator's malformed-output diagnostic conflated "fork error", "empty response", and "schema drift" into a single warning that told the operator nothing. Shim now carries `messages=[]`, curator synthesizes a minimal "Begin the consolidation pass" user turn, and the rejection diagnostic splits the three failure modes so the operator sees which one fired. Validated end-to-end: Sonnet 4.6 fork under `write_origin=curator` correctly emits PRUNE for a placeholder skill while leaving foreground-authored skills untouched (Phase 4 hard rule respected).
- **Skill name validator emits actionable error** — kebab-case enforcement (`^[a-z0-9]+(?:-[a-z0-9]+)*$`) silently dropped skills with snake_case directory names at session start. Error message now detects underscores and uppercase letters in the rejected name and suggests the corrected form (e.g. `"use hyphens instead of underscores (try 'string-utils-style')"`). Behavior unchanged; purely diagnostic. Surfaced driving Sonnet through a multi-file workspace e2e where the workspace skill was being skipped and the model never followed its conventions.

## [0.1.x] - earlier

_See git history for pre-0.2.0 work._
